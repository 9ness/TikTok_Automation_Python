- Fruit presets devolvían 0: faltaba max_output_tokens en Gemini → JSON de 10 historias largas se truncaba (~8k) → generate_json ValueError tragado en warnings del tracker. Fix: max_output_tokens (generate_text 8192 / generate_json 32768) + log warnings a stdout + toast.error si created_count=0 + prompt prioriza cotilleo/infidelidad
- No se puede rejuvenecer a la chica desde el prompt si la generación es imagen-a-imagen: el modelo copia la cara de la referencia y con ella la edad (la del curso tiene ~35 y salían todas así, dijera el prompt 24 o 33). La única vía es crear la referencia de CERO con un prompt de TEXTO puro (`prompts/referencia_chica_<escenario>.md`, con la edad dentro) y subir esa foto como referencia del escenario.
- La edad de la chica va por ESCENARIO, no una para todo: 19-23 en la calle de noche (belleza, suplementos, tecnología, fitness), 19-26 en baño/coche/playa, 25-35 en cama y sofá, y 28-38 en casa, cocina y exterior — a una de 20 anunciando un tendedero o una barbacoa no se la cree nadie. Por eso `hogar` dejó de compartir escenario con belleza y tiene el suyo (`casa`). Las caras siempre son DISTINTAS: la misma chica en todos los carruseles no se puede sostener con imagen-a-imagen.
- En carruseles la chica es la MISMA dentro de un escenario y distinta entre escenarios: el prompt de tanda pasó de "sustituye a la chica por otra diferente" a "la MISMA chica de la referencia, cambia el sitio, la ropa y el encuadre". Así cada nicho tiene su cara reconocible (que es lo que hacen las cuentas que funcionan) sin depender de mantener una sola en 190 fotos. Cada escenario trae además su texto de BÚSQUEDA (`BUSQUEDA_CHICA`) para dar con la chica adecuada.
- Revisar textos cruzados: comparar la CAPTURA de la ficha con el título guardado (una llamada de visión por producto) caza lo que el detector de títulos duplicados no ve; va como job (`nicho_pov_bof_revisar`) para que su gasto quede en costes, con opción de rehacer de una en una las carpetas donde algo no cuadre.
- Al leer el DOM de la web del curso: es un ACORDEÓN (abrir una carpeta cierra la anterior) y además REPINTA la lista al abrir, así que el `div.carp` que tenga el bucle queda descolgado y su `.prod` no aparece nunca — hay que volver a buscar la carpeta por índice en cada vuelta. Síntoma: el acumulado se queda clavado en la primera carpeta y cada vuelta tarda el timeout entero.
