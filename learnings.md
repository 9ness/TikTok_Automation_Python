# Learnings

* [Fecha] Contexto: Lección / Resolución
* [2026-07-28] Nicho POV BOF Fase 2: recortar silencios de audios cortos (10-14s) NO necesita `editor_auto/tools/silence_cutter.py` (7000 líneas, varias pasadas de IA) — basta ffmpeg `silenceremove=start_periods=1:...:stop_periods=-1:...` + verificación ffprobe (recortado < original y > 3s, si no se descarta y se usa el original). `audio_bank.py` cachea el resultado en `_procesados/` junto al original (nunca se borra) comparando mtimes. Endpoints de descarga tipo `<a download>`/`<img src>` no necesitan lógica de auth propia: `get_current_user` (dependencia de router) ya acepta `api_key` por query ADEMÁS del header `X-API-Key`, y `FileResponse(..., filename=...)` ya pone `Content-Disposition: attachment` por defecto (Starlette).
* [2026-05-22] Windows + Python runners crashean en `print("🎙️ ...")` por cp1252 default → `UnicodeEncodeError: 'charmap' codec can't encode characters`. Pasa en cualquier emoji impreso en stdout (locutor.py L134 entre muchos). Fix global: `sys.stdout.reconfigure(encoding="utf-8", errors="replace")` al arrancar `src/api/main.py`, antes de imports pesados. Coste de la tarea afectada: 0 (el crash es ANTES de cualquier API call de pago — solo Gemini previo).
* [2026-05-22] Webhook listener se colgaba cada 24-48h. Root cause: `HTTPServer` single-threaded → cualquier conexión lenta (scanner de puertos, GitHub timeout, etc.) bloquea TODAS las requests siguientes; el proceso sigue "active" para systemd pero `/health` da timeout. Fix: `ThreadingHTTPServer` + `daemon_threads=True`. Safety net: `tiktok-webhook-healthcheck.timer` cada 2 min hace `curl --max-time 5 /health || systemctl restart tiktok-webhook`. Solo reinicia el LISTENER (proceso de 11MB en :9000) — los Docker containers tiktok-api/web/caddy son procesos separados, los jobs corriendo no se enteran.
* [2026-05-22] TikTok Shop URL share `vm.tiktok.com/XXXX` redirige a `www.tiktok.com/view/product/{id}?og_info={...JSON-encoded...}&share_region=ES`. El `og_info` trae title + image LIMPIO sin necesidad de scrape JS. Parser en `url_scraper.py:_extract_tiktok_share_params` saca de los query params con `urlparse` + `json.loads(unquote(...))`. Bypassa por completo el bot-detection del HTML de TikTok.
* [2026-05-22] TIKTOK_SHOP_URL_PATTERN debe aceptar cualquier subdominio: `^https?://([\w-]+\.)*tiktok\.com(/.*)?$`. La versión vieja solo aceptaba `www.` y `shop-xx.` y rechazaba `vm.tiktok.com` de los share URLs.
* [2026-05-22] Seedance i2v Atlas: `TIER_DURATION_LIMITS` (config.py) → mín 4s por clip en standard/advanced/pro. Para vídeos <8s solo cabe single_shot. Para ≥8s puede ser multi (2×4s, 4+5, etc.). `strategy="dynamic"` rota fotos (`rotate`) entre clips, `cinematic` no. Pro tier (ref2v) es 1 prompt único con multi-shot INTERNO (hasta 9 refs). Veo 3 nativo 1 toma 10s max.
* [2026-05-22] VideoPreset sub-objetos: TextOverlayStyle (overlay hook con duration_s), SubtitleStyle (karaoke con highlight_color, position siempre bottom_center safe-zone, size 40-52px), CtaArrowStyle (sticker `flecha_negra.mov` / `flecha_roja.mov` de `TIKTOK_EDITOR/Assets/flechas/`, position_x/y_pct, duration final). El runner ya soportaba `OverlaysConfig.cta_arrow` del Editor Auto — sólo había que mapear preset → payload en `applyVideoPreset` y `BulkGenerateDialog.applyPresetToPayload`.
* [2026-05-22] Async preset generation con progress: FastAPI BackgroundTasks + Redis tracker (`preset_gen:{gen_id}` con stage/percent/created_count) + polling 2s en frontend con `usePresetGenStatus`. gen_id persistido en `localStorage[preset-gen:{product_id}]` para sobrevivir refresh navegador. Stages: started → generating_music (5-50%) → generating_scripted (55-90%) → saving (95%) → done/error. Si el user cierra pestaña, la BackgroundTask sigue en el server hasta done.
* [2026-05-22] Gemini cost tracking automático: `_call_with_key` en gemini.py extrae `response.usage_metadata.prompt_token_count` + `candidates_token_count` y llama `record_gemini(input, output, model)`. Si no hay tracker activo el record es no-op (cost_tracking._add_line). Endpoints stateless (analyze-url, photo-grader, preset-generator, variants-generator) envuelven con `start_job(program="tiktok_shop", mode=...)` + `finalize_and_persist()` para que aparezca en `/costs`.
* [2026-05-22] DELETE /products/{id} ahora borra recursivamente la carpeta `TIKTOK_SHOP/_products/<slug>/` en Drive (shutil.rmtree, best-effort — si falla, soft-delete en Redis sigue). Drive Desktop sincroniza el delete a papelera Google Drive (30d recuperable).
* [2026-05-22] `ProductResponse` schema necesita listar EXPLÍCITAMENTE `video_presets: list[dict[str, Any]]`. Pydantic filtra campos no declarados al serializar — sin esta línea los presets se persistían en Redis pero NO llegaban al frontend (mostraba "0 presets" aunque había 28).
* [2026-05-14] Editor Auto: preview de stickers MOV ProRes en frontend requiere transcode → APNG (cache temp, `?as=apng`). Probado libvpx-vp9 con `-pix_fmt yuva420p -auto-alt-ref 0 -metadata:s:v:0 alpha_mode=1`: el encoder reporta yuva420p pero el ffmpeg Windows gyan-build NO escribe los BlockAdditions que Chrome necesita → browser muestra fondo opaco. APNG @ 15fps/200px = ~1.5MB con alpha real (verificado PIL extrema 0-255).
* [2026-05-14] Editor Auto: tool `sticker_arrow` añadida (weight=80, entre cortes 10 y subs 90). Overlay con FFmpeg `[1:v]scale=W*pct,format=rgba[s];[0][s]overlay=x:y:enable='between(t,T0,T1)'` + `-stream_loop -1` para que MOV/WebM cortos se repitan dentro de la ventana. Posición = CENTRO del sticker en % del frame (no top-left) usando `(main_w*px)-(overlay_w/2)`. Detección de gatillo via Whisper local re-transcribe el vídeo post-cortes (timestamps pre-cortes ya no son válidos). Assets en `TIKTOK_EDITOR/Assets/flechas/` (`arrows_folder()`). Endpoint `/stickers/arrows` listado con auth header; `/arrows/{filename}` con auth dual header+query porque `<video src=>` no admite headers custom (mismo patrón que `fonts/file/`).
* [2026-05-14] Editor Auto: tool `silence_cutter_scripted` añadida como hermana del cortador estándar (no flag dentro de la misma tool, para no contaminar el flujo sin guion). Usa Silero VAD + Whisper + `difflib.SequenceMatcher` sobre tokens normalizados (lowercase, sin acentos, sin puntuación) — opcodes `delete/replace` = regiones fuera de guión → cuts. LLM opcional solo para regiones largas ambiguas (≥3 palabras). Mismo `position_weight=10` que `silence_cutter`; coexistencia rechazada vía `TOOL_EXCLUSIVE_GROUPS` en `_validate_steps`. Guion se pasa como Form field en `/enqueue` → `job.params["script"]` → inyectado en `merged_config["script"]` por el orchestrator solo para esa tool.
* [2026-05-06] Presidentes Top N: añadido auto-calibrador de palabras (`src/word_calibrator.py`) que persiste `target_total_words` por tipo de vídeo en Upstash Redis (`tiktokCR:words:presidents:t{N}:hook{0|1}:cr{0|1}`). Tras TTS se mide duración con `ffprobe` y, si está fuera de 60-65s, ajusta proporcionalmente (paso máx ±30 palabras, clamp 120-280) y regenera (hasta 3 intentos). `runners.run_presidents` envuelve guion+audio en bucle de calibración.
* [2026-05-12] Subs sobre vídeo: `merge_edited_text_with_timings` reescrito en 3 iteraciones. Final: frases naturales por pausas ≥ 0.30s con cap 6s/frase (split recursivo) + **anclas matched dentro de cada frase como sub-fronteras**. Sub-segmentos entre anclas se distribuyen char-weighted con floor 80ms/palabra; si un sub-segmento es muy corto, merge bidireccional absorbe vecino. Sobrevive a +60 palabras añadidas con 96% de anclas preservadas dentro de 100ms drift. Path 1:1 directo cuando count idéntico.
* [2026-05-13] Cost tracking unificado en `src/cost_tracking.py`: ContextVar por job + helpers `record_openai_chat / openai_whisper / minimax_tts / atlas_cloud / custom`. `dispatch_job` envuelve cada runner en `start_job`/`finalize_and_persist`. Redis (prefijo `tiktok_shop:`): `cost:job:{id}`, `cost:index:{program}:{YYYY-MM}`, `cost:by_user:{u}`, `cost:by_product:{p}`. Endpoint `GET /api/v1/stats/jobs` con filtros mes/programa/modo/user/product → summary + lista. Página `/costs` en frontend con tabla expandible por job. Tarifas actualizables en constantes top del módulo. Convención obligatoria para nuevos modos documentada en CLAUDE.md.
* [2026-05-13] Editor Auto (Programa 3): herramientas componibles tipo plugin con `BaseTool` Protocol + REGISTRY + reorden determinista por `TOOL_POSITION_WEIGHTS` (silence_cutter peso 10 antes que subs_auto 90 porque cortar mueve timestamps). La UI consume `GET /tools` y genera formularios desde `config_schema` declarativo — añadir una herramienta nueva no requiere tocar frontend. Silero VAD ONNX (paquete `silero-vad`) evita PyTorch full. `cost_tracking.record_openai_chat` extendido con `_OPENAI_CHAT_RATES` (gpt-4o $2.50/$10.00 por 1M tokens) — match por prefix del model name.
* [2026-05-18] Construcción POV (nicho 4 CR): Gemini 2.5 Pro upload de vídeo vía Files API requiere polling de `uploaded.state.name` hasta `"ACTIVE"` (timeout 180s, step 2s) ANTES de `generate_content` — si llamas mientras está `PROCESSING` falla con "file is not in active state". `genai.delete_file(uploaded.name)` tras la llamada para no acumular cuota Files. Pipeline: 1) ffprobe duración → calcular `target_chars` (15.5 chars/s EN, cola silencio 4s, mín 200); 2) Gemini → guion EN 1ª persona; 3) MiniMax `generate_single_audio` con voice_id; 4) ffmpeg `-c:v copy -c:a aac -shortest` para mux audio nuevo sobre vídeo original; 5) Whisper sobre la narración con `initial_prompt=script_text` para boost; 6) ffmpeg `scale*1.08+crop+eq=saturation` + metadata strip `-map_metadata -1 -flags +bitexact`; 7) `render_subtitles_on_video` karaoke. Voice cloning expuesto en endpoint global `/api/v1/voices/clone` (sube MP3 → MiniMax `voice_clone` API → `VoiceRepo.save` persistente) + `/voices/{id}/sample` sin auth para `<audio src>` de audición. Preset EN expandido a 40 voces en `tiktok_shop/config.py:DEFAULT_VOICE_PRESETS_EN`.
* [2026-05-19] Construcción POV — fix "última palabra en bucle hasta fin del vídeo": MoviePy `VideoFileClip` + `write_videofile` re-encodea el audio con `AudioFileClip`; cuando el audio del input es más corto que el vídeo (narración 53s + cola silenciosa 8s) y le pide samples más allá del fin real, el reader devuelve el último buffer loopeado en lugar de silencio. Fix en `src/construccion_pov/pipeline.py:_mux_audio_over_video`: usar `-filter_complex "[1:a]apad[aout]"` + `-shortest` (en lugar de `-t video_dur` con audio sin padding) → el track de audio del MP4 queda exactamente igual a video_dur con silencio limpio tras la narración, y MoviePy no tiene que extrapolar.
* [2026-05-19] Fuentes en container API — `Dockerfile.api` no instalaba TTFs y `python:3.11-slim` viene casi sin ellos → en producción solo aparecían 3 fuentes en el selector (las 2 bundled del repo + 1 que arrastraba ffmpeg) y los presets con `C:\Windows\Fonts\impact.ttf` fallaban con "fuente no existe en el registry". Fix: añadir `fontconfig fonts-dejavu fonts-liberation fonts-noto-color-emoji` + repo `contrib` + `debconf-set-selections` para EULA + `ttf-mscorefonts-installer` al Dockerfile.api (paquete vive en contrib, no main). Adicional: `fonts_registry.find_by_path` usaba `os.path.basename` que en Linux NO trata `\` como separador → frontend mandando paths Windows guardados en presets viejos no matcheaban. Helper `_cross_platform_basename` divide manualmente por `/` y `\` antes de comparar.
* [2026-05-19] sticker_arrow FFmpeg crash — `main_w`/`main_h` son variables de `scale2ref`/`overlay`, NO válidas en `scale` plano. ffmpeg 5.x (debian-bookworm, el del Dockerfile.api) rechaza estricto: "Expressions with scale2ref variables are not valid in scale filter". Funcionaba en local con ffmpeg 6.x/7.x pero peta en producción. Fix en `src/editor_auto/tools/sticker_arrow.py:_apply_overlay_ffmpeg`: probe width del input con ffprobe (`_probe_video_size`) y usar literal en el `scale` del sticker (`scale={W*sw}:-2`). En `overlay` `main_w`/`main_h` SÍ son válidas → no se tocan. Fallback 1080 si ffprobe falla.
* [2026-05-19] sticker_arrow match policy + fuzzy — antes solo cogía la PRIMERA ocurrencia del trigger en el último 50% del vídeo → cuando el speaker decía "carrito" varias veces, solo se ponía 1 flecha y normalmente no en el momento esperado. Además Whisper transcribe "carrito"→"carito" (se come una RR) → keyword exact-match fallaba. Refactor: `match_strategy` config (`first|last|all`, default `last`) + `fuzzy_match` config (edit-distance ≤1 para keywords ≥5 chars). `_find_keyword_matches` devuelve lista, no single. `_apply_overlay_ffmpeg` acepta `windows: list[tuple[float,float]]` y concatena con `+` en el `enable=` expr (OR lógico ffmpeg). Helper `_merge_overlapping_windows` funde ventanas solapadas para no duplicar overlays.
* [2026-05-19] silence_cutter detección auto de estilo (monólogo vs conversación) — heurística gratis sobre output de Silero VAD: `mean_segment_dur` < 4s Y `turn_gap_ratio` (suma de pausas 0.4-1.5s sobre duración) > 0.05 → conversación; resto → monólogo. Cero coste extra (los datos ya están). Config `auto_detect_style: bool` (default True) + `manual_style: enum` para override manual. En conversación se sube `inter_word_gap_threshold_s` 0.5→1.2 y `inter_word_gap_keep_ms` 200→350: preserva turn-taking natural en vez de comerse cada gap entre turnos. Decisión y métricas se loguean explícitamente para que el operador entienda por qué se aplicó un modo u otro.
* [2026-05-19] subs_auto precisión Whisper — default `reference_text` poblado con vocabulario TikTok Shop ES (carrito, link, descuento, código, oferta, envío gratis, etc.) → se pasa como `initial_prompt` a Whisper y reduce errores de transcripción en palabras del nicho. Toggle nuevo `audio_denoise` (default OFF) que aplica ffmpeg `afftdn=nr=12:nf=-25` antes del transcribe — mejora 5-10% en audios sucios pero +5-10s de pipeline. Si falla, fallback al audio original sin abortar.
* [2026-05-19] silence_cutter_scripted fallback automático sin guion — antes la tool fallaba con RuntimeError si `job.params["script"]` estaba vacío. Ahora instancia `SilenceCutterTool()` con sus defaults + override de campos comunes (`vad_enabled, min_silence_ms, padding_ms, ai_language, whisper_model_size, output_aspect, post_audit_enabled`) y delega. Loguea el fallback claramente para que el operador vea en la cola "No se ha encontrado guion → fallback a modo SIN guion". Permite configurar siempre la versión scripted y olvidarse: si hay guion → diff transcript↔guion; si no → cortes VAD/IA normales.
* [2026-05-19] UserFlowEditor pickers — `FontPickerField` y `StickerPickerField` mostraban paths/filenames raw como label visible del `<option>` seleccionado. Si la config se creó en local Windows con `font_path=D:\Proyectos_Personales\…\Rubik-Bold.ttf` y se abre en producción Linux (paths `/usr/share/fonts/…/Rubik-Bold.ttf`), el `find(f.path===current)` fallaba y se renderizaba el path Windows entero como label. Fix: helper `crossPlatformBasename` (divide por `/` y `\`) + resolución por basename como fallback en FontPicker; helper `prettyFilename` que convierte `flecha_roja.mov` → "Flecha roja" en StickerPicker. Si nada matchea se muestra solo el basename con sufijo `·sin resolver`, no la ruta entera.
* [2026-05-19] Whisper default `large-v3` en todas las tools Editor Auto — antes era `small` (rápido, ~1GB RAM, pero menos preciso). Cambio aplica a `silence_cutter`, `silence_cutter_scripted`, `sticker_arrow` y `subs_auto`, tanto en `default_config` como en los `.get(..., "small")` defensivos del `run()`. Coste: ~3-5x más lento por job y ~6-8GB RAM con int8 quantization (faster-whisper). Beneficio: precisión notablemente superior en audio rápido, conversación, ruido de fondo. Si en producción Hetzner CX32 (8GB total) OOMea o tarda demasiado, el operador baja a `medium` por config UI (sin tocar código).
* [2026-05-19] Drive sharing `revoke_permission` con cascade — Drive API devuelve `403 cannotDeletePermission` si intentas borrar un permiso heredado en una carpeta hija. El `permission_id` que el frontend ve en `entrada/` puede ser el mismo perm que vive en `<usuario>/` (raíz del usuario): la herencia lo expone en hijos pero solo se puede borrar donde realmente está. Fix en `src/editor_auto/services/drive_sharing.py:revoke_permission`: cascade hijo→padre (subcarpeta → usuario → Usuarios → TIKTOK_EDITOR). En cada nivel intenta el delete; si Drive responde con `cannotDeletePermission`/`notFound` sigue subiendo. Devuelve el nombre del nivel donde se revocó realmente para que el frontend pueda avisar si cascadeó más arriba de lo esperado. Endpoint `DELETE /users/{id}/shares/{permId}` ahora devuelve `revoked_at_level` en la respuesta.
* [2026-05-19] Encolar dialog (Editor Auto) — guion ahora OPCIONAL en el modal. Antes era "obligatorio" con `disabled={!ready}` bloqueando el botón Encolar. Con el fallback scripted→non-scripted ya implementado (commit `0b6ee7f`), el guion vacío es válido: el backend cae al modo VAD/IA automáticamente y loguea la decisión. Textarea sigue ahí para quien quiera pegar guion manual, pero el placeholder ahora dice "vacío = modo sin guion". El backend ya detecta el companion `<stem>.txt` en entrada/ via `_script_companion` y lo aplica solo si existe.
* [2026-05-19] Batch enqueue Editor Auto — botón "Encolar todos (N)" en cabecera del tab entrada/ cuando hay ≥2 vídeos. Procesa secuencialmente con `mutateAsync` en loop (la cola del worker ya es secuencial, así que paralelismo en el dispatch no aporta). Cada vídeo se encola con `script=""` → backend usa companion `.txt` si existe, sino fallback a modo sin guion. Dialog muestra desglose previo: cuántos tienen companion (modo premium) vs cuántos no (modo VAD/IA). Toast resumen al final: success si todos OK, warning si fallos parciales, error si todos fallan. `e.preventDefault()` en `onClick` del AlertDialogAction evita cierre automático del modal antes de terminar el batch.
* [2026-05-19] Encolar SIN modal + sin validación backend — el botón "Encolar" individual ya no abre AlertDialog. Click → POST `/folders/enqueue` directo con `script=""`. Backend (`src/api/routers/editor_auto/folders.py`) ya NO bloquea con ValidationError si el flow es scripted y no hay guion: la lógica de fallback en `silence_cutter_scripted.run()` (commit `0b6ee7f`) detecta script vacío y delega al cutter sin guion automáticamente. Tooltip del botón explica el modo activo según haya companion `.txt` o no. Para usar modo con guion, el operador sube `<stem>.txt` junto al vídeo en `entrada/` (autodetect en el listing).
* [2026-05-19] Auto-enqueue watcher (Editor Auto) — toggle `auto_enqueue` por usuario en el modelo `EditorUser`. Cuando activo, un background task lanzado desde el `lifespan` de FastAPI (`src/editor_auto/services/auto_enqueue_watcher.py`) hace polling cada 30s a la carpeta `entrada/` de los usuarios con flag ON y encola los vídeos nuevos. Reglas de seguridad: (a) `mtime` mínimo 60s para evitar uploads en curso; (b) si el flow tiene `silence_cutter_scripted` y NO hay companion `.txt`, margen extra 180s por si el cliente sube el guion después del vídeo; (c) try/except en cada paso — si move falla, vídeo sigue en entrada/; si move OK pero `queue.enqueue` falla, rollback cola→entrada; (d) errores per-archivo se loguean sin detener el loop. Frontend: Switch + Zap badge en cada user list item.
* [2026-05-19] silence_cutter `ai_pass2_openai_enabled=True` por defecto — antes solo Gemini en pass2 (default balanced). Cambio a PRO consenso (OpenAI + Gemini ambos en pass2). Coste por vídeo sube ~+$0.008/min pero pillamos false-starts que escapan a una sola pasada y tenemos red de seguridad ante non-determinismo de un modelo. El usuario puede desactivar uno de los dos toggles desde la UI si quiere bajar costes.

<!-- Test retry -->

<!-- Test auto-deploy v3 - after NoNewPrivileges fix -->

- Apify clockworks/tiktok-scraper: usar `shouldDownloadVideos:true` → MP4 en su key-value store (`mediaUrls[0]`); esas URLs dan 403 sin `?token=APIFY_API_TOKEN`. Descargar directo de TikTok CDN (downloadAddr) da 403/404. Algunos records dan 404 (actor no los subió) → degradar saltando ese video.
- Cost tracking en BackgroundTasks: las `record_*` son no-op sin `start_job` activo en ese thread. El deep research corría en background sin `start_job` → Gemini+Apify no se registraban. Fix: envolver el background con `start_job`/`finalize_and_persist`. El `research_cost_usd` ahora sale de `get_active().total_usd` (real), no de un estimado hardcoded.
- Research motor TikTok Shop ampliado: comentarios de vídeos virales (actor `clockworks/tiktok-comments-scraper`, input `postURLs`) → Gemini extrae `audience_questions`+objeciones reales; `musicMeta` de clockworks agrega `trending_sounds` (dedupe por musicId). Feedback loop `PublishedVideo` (repo `published:`) → `winning_angles_block` inyecta ángulos ganadores en `_research_block` de preset_generator.
- EchoTik API (`open.echotik.live/api/v2`, Basic Auth user+password, ~€0.0001/req): `/product/list?region=ES&keyword=` SÍ devuelve ventas reales por PRODUCTO en España (total_sale_cnt/total_sale_gmv_amt). `/product/video/list?product_id=` da los vídeos del producto (play_addr MP4 descargable) pero `total_video_sale_cnt`=0 SIEMPRE (ES y US, incl. beauty/supplements) → ventas-por-vídeo NO accesible en trial (¿gated en plan pago?). TikTok atribuye ventas a LIVE/directo, no a vídeos. Cliente en `src/tiktok_shop/api/echotik_cloud.py` construido pero NO cableado (datos vacíos). Veredicto: ventas-por-vídeo no viable; mejor señal de fórmula = engagement proxy + multimercado.
- Veo3 modo `fruit_character`: mini-historia 8s con cabezas de fruta (casting por color/semántica + 4 enfoques drama/chisme/burla/aspiracional + CTA carrito) — flag fruit_mode en generate_veo3_prompt + preview/veo3 + toggle wizard
- Presets versión fruta: kind="fruit" en generate_presets → _generate_fruit (fruit_story_director.md, 10 mini-historias Veo3 originales) + VideoPreset.variant="fruit_story" (separa UI Original/🍉Fruta) + chips Versión + botón generar; solo veo3_prompt_only
- Deploy hang root-fix: Dockerfile.api CMD usa `exec uvicorn` (sh no era PID1→no reenviaba SIGTERM→graceful shutdown nunca corría→SIGKILL a los 30min) + init:true en compose + --timeout-graceful-shutdown 1800 + --no-deps en rebuilds api/web de deploy_safe (evita recreate cruzado por depends_on)
- Estilo fruta corregido: era "cuerpo humano hiperrealista + cabeza fruta fotorealista, NO dibujo" → generaba humanos reales/frutas sin cara. Real es ANIMACION 3D Pixar/Illumination, fruta-cabeza CON cara de dibujo, todos frutas-persona. Reescritos fruit_story_director.md + veo3_director.md (STYLE obligatorio + NEGATIVE live-action/human/plain-fruit)
- Fruit presets flujo 2 pasos: image_prompt (Nano Banana keyframe Pixar) + veo3_prompt como image-to-video ("Animate the attached reference image, keep exact style"). Campo VideoPreset.image_prompt + UI Paso1/Paso2 en card. Estilo fiable: imagen primero, luego animar en Flow
- Generador Veo3 modo fruta: selector 1/Lote(10)/A-B(4). Endpoint sync POST video-presets/generate-fruit (un call Gemini, guarda presets fruit_story + devuelve para modal). _generate_fruit con ab_mode/fruit_hint/angle_hint (_fruit_mode_directive). A/B = misma fruta+enfoque, varía gancho/escena
- Fruit video drift (caras humanas en planos secundarios): añadida CLAUSULA DE CONSISTENCIA al veo3_prompt (every character keeps fruit head ENTIRE clip/every angle, heads never turn human, no new humans) + single continuous shot + NEGATIVE (human face/head, character turning human, woman real face, head morphing)
- Editor Auto tool nuevo photo_insert (weight 70): Whisper→Gemini detecta famosos/marcas con timestamp→image_search descarga foto→ffmpeg overlay multi-imagen en su momento. 100% auto, no-op si no hay entidades. Config posicion/tamaño/duracion/max. Registrar tool: constante config + TOOL_POSITION_WEIGHTS + REGISTRY (UI schema-driven, sin frontend)
- Editor Auto UI: PhotoPreview9x16 (placeholder + zonas seguras + drag/resize) para photo_insert, wired en UserFlowEditor. Fix preview flecha: APNG transcode scale=200:-1 daba altura impar y reventaba (flecha roja en blanco) → -2 + format=rgba
- Fruit infieles + tema propio: fruit_story_director.md ahora exige MÍN. mitad del lote de infidelidad (regla dura ≥4 presets) + sección "TEMA DEL USUARIO" (convierte escena escrita por el user a personajes-fruta). _generate_fruit/_fruit_mode_directive aceptan custom_theme (single=1, batch/ab=variaciones de la idea). GenerateFruitRequest.custom_theme + UI wizard: opción enfoque 💔 infiel + textarea "Tu idea/tema". UI movida del paso Resumen al paso Tier (FruitModeBlock: toggle Normal/🍉Frutas + botón "Generar prompts" propio que dispara generateFruitPrompts sin recorrer el resto del wizard)
- Veo3Card (generador NORMAL /tiktok-shop/generate, no el wizard /advanced) es el que usa el operador. Modo fruta ahí: selector workMode "📋 Usar presets"/"🍉 Usar fruta"; presets excluye variant=fruit_story, fruit los aísla. Sub-modo fruta: "existentes" (grid de fruitPresets reusa single/batch/variants via activeList) o "crear con idea" (FruitBlock → generate-fruit). ResultItemCard muestra image_prompt (Paso1) + veo3_prompt (Paso2)
- Quitar Copy 4 modos (clean_mode): "Subtítulos Virales" (blur subs orig+zoom1.10+subs nuevos), "Camuflaje Geométrico" (sin blur+zoom1.15/pulsos/firma+gancho IA), "Solo Limpiar (Sin Subtítulos)" (blur subs orig+zoom1.10, sin texto, prefix CLEAN_), "Limpiar Metadata (Sin Tocar Subtítulos)" (NO toca subs, pipeline geométrico zoom1.15/pulsos/firma sin gancho, prefix CLEAN_META_). video_remover.process flags: add_subs/is_camuflaje/clean_keep_subs/clean_mask_subs → use_geometric=camuflaje|keep, add_hook=camuflaje. Runner: keep_subs salta OCR+Whisper, mask_subs salta solo Whisper. Frontend: SelectItems + isCleanOnly oculta controles texto/fuente/overlay + nota por modo
- Veo3 rechaza ropa interior: para productos de ropa íntima/ajustada/baño, _sensitive_apparel_block (detecta keywords ES/EN/IT en name/category/subcategory/brand) inyecta guía en music/scripted/fruit/extend → reencuadra como ANUNCIO ACTIVEWEAR/SPORTSWEAR (evita la palabra "underwear", filtro más laxo), persona de cintura para abajo SIN cara, generar desde la foto del PRODUCTO (no de personas reales — el filtro de imagen de entrada las bloquea), si es PACK multicolor el modelo lleva UN solo color, frase de seguridad non-sexual/non-explicit/no-nudity. Truco manual extra: 2 pasos imagen(Nano Banana)→vídeo para mostrarlo puesto. Solo afecta presets generados DESPUÉS
- Bug "datos de entrada inválidos" al guardar identidad: slug del producto >150 chars (slugify NO capaba longitud, nombres largos tipo "ropa_interior_calvin_klein...") y ProductCreate/Update.slug tenía max_length=150 → 422 string_too_long al reenviar el slug. NO relacionado con idioma. Fix: slugify capa a 120 en frontera de palabra + max_length schema subido a 200 (slugs viejos largos re-guardan sin renombrar carpeta Drive)
- Idioma producto it_IT (italiano): añadir idioma nuevo toca 3 sitios funcionales — preset_generator._LANGUAGE_INSTRUCTIONS (instrucción guion + directiva spoken-language para veo3/seedance), frontend/lib/language.ts LANGUAGE_OPTIONS+DEFAULT_VOICE_BY_LANGUAGE (selector UI lo lee ProductCreateDialog+ProductEditorTabs), research_service._LANG_TO_COUNTRY. Resto son comentarios. Voz default fallback English_EnergeticBoy (multilingüe) hasta clones IT. La directiva de idioma inyectada en seedance/veo3 ahora SOLO fija voz hablada + cláusula universal _NO_ONSCREEN_TEXT_DIRECTIVE (el modelo NO quema texto; hooks/overlays/subs los pone el editor en post). Antes pedía "on-screen text in X" → invitaba a quemar texto
- Fruit veo3_prompt contradicción resuelta: el modelo a veces escribía "quick montage/cuts" en los beats contradiciendo "single continuous shot" + NEGATIVE "hard cuts". Fix: director+extender prohíben explícitamente montage/cut/quick cuts/time jump en los beats (una sola acción continua, cámara 1 movimiento). NO se tocó la cláusula de consistencia (plano continuo = lo que evita que caras de fruta se vuelvan humanas en cortes). Mantener continuo > permitir cortes para fruta
- Ganchos texto fruta: VideoPreset.text_hooks (list[str], 3 opciones). fruit_story_director.md + fruit_story_extender.md generan 3 ganchos con REGLA DE ORO (plantea problema/curiosidad, NO nombra producto, 6-8 palabras, 2ª persona/POV, mezcla problema-real/curiosidad/drama). VideoPresetResponse + ExtendFruitResponse.text_hooks. ResultItemCard muestra "✍️ Gancho de texto" con copiar c/u (en extend van en Parte 1). Botón "Generar 3 ganchos"/"Regenerar" por tarjeta → POST video-presets/{id}/generate-hooks (sync, persiste preset.text_hooks via generate_fruit_hooks/fruit_hooks.md, invalida product detail). canGenerateHooks solo si el id es un fruitPreset real (no partes -pN de extend)
- Extensiones fruta persistidas en localStorage (lib/fruitExtensions.ts), NO Redis: clave tiktok_shop.fruit_ext.v1.<productId>, indexadas por preset base. loadFruitExtensions purga huérfanas (preset id ya no existe = se regeneraron → cascada) + caducadas (TTL 30d). Veo3Card extend guarda tras generar, lista "💾 Alargadas guardadas" + "📂 Ver guardado" (reabre sin regastar Gemini). Sobrevive recarga incl. móvil (mismo dispositivo)
- Alargar historia fruta: 3er sub-modo "⏱️ Alargar" en Veo3Card. Selecciona 1 fruitPreset + 2/3 partes (≈20/30s) → POST video-presets/{id}/extend-fruit (sync, NO persiste) → extend_fruit_story (fruit_story_extender.md) devuelve N {part,beat,image_prompt,veo3_prompt} continuos (final parte K = inicio K+1, CTA solo última). Mismas fotos producto en todas. Cada parte se muestra como ResultItem (Paso1 img + Paso2 vídeo) para generar y unir. Veo3 nativo=10s, por eso multi-parte manual en Flow
- Editor Auto: FlowPreview9x16 centralizado (subs+flecha+foto en un solo 9:16 con selector de elemento + drag/resize touch + rotacion flecha). Reemplaza previews individuales en UserFlowEditor; update por tool_id
- sticker_arrow multi-flecha: color_mode (fixed/auto_contrast/random) + candidate_stickers. auto_contrast extrae frame en la zona CTA, calcula luminancia y elige flecha negra (fondo claro) o clara/roja (fondo oscuro) por _arrow_tone (nombre). Widget multi_select_sticker en UI
- Editor Auto config por usuario: EditorUser.auto_enqueue_delay_minutes (cuenta atras antes de auto-encolar) + daily_video_limit_override + window_*_hour_override. quota_service y watcher usan overrides (ganan al plan). Sin gating de tools (admin gestiona). Expuesto en PATCH/response
- Editor Auto: email_notify (Gmail SMTP app-password, env EDITOR_SMTP_USER/APP_PASSWORD) + endpoint POST users/{id}/release-day (comparte carpeta salida con known_share_emails + email "videos listos"). No-op si SMTP/Drive sin configurar
- Editor Auto UI: config por usuario (delay + max/dia) en UserFoldersPanel + boton "Marcar dia listo" (useReleaseDay). quota_service respeta daily_video_limit_override aunque el user no tenga plan (sin plan + sin override = sin restricciones)
- Editor Auto auto-revoke salida: release-day guarda output_released_on (fecha UTC); watcher _maybe_revoke_output revoca acceso a salida de known_share_emails al cambiar de dia (auto_revoke_output_daily=True). UI muestra estado acceso
- Silence cutter clipaba finales de palabra ("bien"→"bie"): al fusionar cortes (IA noise_gap + inter_word_gap) el inicio caia en word.end exacto (Whisper marca fin pronto). Fix: _protect_word_boundaries sobre cortes fusionados — guard 120ms (_WORD_GUARD_S) tras fin de palabra previa y antes de inicio de siguiente. Cubre todas las fuentes
- Editor Auto enqueue from entrada: el move pesado entrada→cola (rclone) + creacion del job ahora en BackgroundTask → endpoint responde 202 al instante, sobrevive a recargar la pagina. Frontend: tipo {status,filename,message} + re-invalidacion diferida. La pagina Generar (upload directo) sigue limitada por la subida del navegador (usar flujo entrada para pesados)
- Editor Auto Generar (upload): barra de progreso REAL de subida via XHR upload.onprogress (fetch no lo soporta) + aviso "no recargues durante la subida" + tip de usar flujo entrada para pesados. useEnqueueEditorAuto acepta onProgress
- Silence cutter clipaba "resultado": Whisper cerraba la palabra en 34.44 (word-guard 150ms→34.59) pero el audio real (amplitud) sonaba hasta 34.88. Fix: _clamp_cuts_to_silence_edges ajusta cada corte a los bordes del silencio REAL medido por amplitud (amp_cuts) tras el word-guard. Nunca corta audio audible aunque Whisper marque mal
- Planes Editor Auto basicos por volumen: esencial_10 (10 vid/dia, 349EUR) + esencial_15 (15 vid/dia, 429EUR) en plan_seeder. Sin gating de tools (allowed_tools=[]). Se crean al arrancar (seeder idempotente)
- Silence cutter under-cut (cintiacbc 0/100, voz bajita -36dB): 2 bugs. (1) _protect_word_boundaries usaba palabra DENTRO del gap como limite (<=e / >=s) → encogia/dropaba cortes legitimos (incluido head silence). Fix: <=s / >=e. (2) clamp de amplitud encogia gaps a la parte detectada (amp infra-detecta en audio bajito) → fix: solo ajustar borde si recorte <=0.5s, sino mantener corte completo
- Silence cutter: en grabaciones de mala SNR (voz -36dB, ruido sala -27dB) el umbral `speech_rms-15` capado a -30 quedaba POR DEBAJO del suelo de ruido → no detectaba silencios. Fix: `_measure_noise_floor_db` (p25 RMS de huecos entre palabras) y umbral = floor+6dB (cap -18). Whisper además rellena gaps con palabras fantasma reales.
- Silence cutter: umbral silencio adaptativo = mean_volume(volumedetect)+10dB cap[-45,-24]. El p25-de-huecos medía micro-silencios limpios (-55) y no las pausas reales (~-27, con palabras fantasma dentro). mean+10 coincide con el umbral del auditor (-25) en grabaciones bajitas. Datos cintiacbc: mean -35.3, pausas detectadas -25→42 vs -30→24.
- Silence cutter: palabras-fantasma POR AMPLITUD. Whisper coloca palabras reales (no uh/um) con timestamp erróneo dentro de silencios acústicos reales (voz bajita); Silero no las caza pero la amplitud sí. `_drop_words_inside_silences` (≥80% del span bajo umbral) las quita de la protección → el corte acústico ya no se fragmenta alrededor y el silencio se corta. Resolvió las pausas para/disimule, tienen/mirad, os/una, rayadito/precioso en cintiacbc.
- Silence cutter: fantasma-por-amplitud SOLO si el silencio rodea la palabra por AMBOS lados (flank 0.2s). La regla anterior (≥80% del span bajo umbral) marcaba como fantasma palabras reales dichas muy bajo al final de frase (voz floja) → perdían protección y se clipaban (cintiacbc: "8 euros aquí" del cierre). El flanqueo bilateral mantiene cazar fantasmas varados (para/disimule) sin tocar cierres reales.
- Silence cutter: normalización loudnorm -16 LUFS auto cuando media pista < -24dB (grabaciones bajitas que "no se oían") — se aplica al output tras concat; no toca pistas a buen nivel. Flank fantasma-por-amplitud 0.2→0.05 (cortaba para/disimule varadas pero protege cierres reales: silencio solo por detrás no cuenta).
- Silence cutter pasada2: reconciliación kind-aware de cortes solapados entre OpenAI/Gemini. Unión ciega cogía el rango más ancho y un modelo con end_word_idx largo se comía palabras del kept_version (frases rotas "os los voy a [enseñar]"). Ahora: solape en false-start/repetición → INTERSECCIÓN (conservador); redundant_restatement → UNIÓN (quitar bloque entero). + prompt: el rango de corte nunca incluye palabras del kept_version.
- Silence cutter pasada2: `_clamp_cut_by_kept_version` reemplaza la reconciliación unión/intersección. Recorta de la cola del corte la mayor secuencia que casa el PREFIJO del kept_version → el corte queda solo en el 1er intento. Arregla a la vez over-cut (se comía "enseñar") y under-collapse de repeticiones ("es un tejido así" doblado). redundant_restatement queda intacto (su kept es la instancia anterior). Issue mirar tejido/estampado = no-determinismo LLM (recall variable).
- Silence cutter: limpieza de CONTENIDO holística (Gemini 2.5 Pro, 1 pasada sobre guion entero → keep_spans). Reemplaza el pegado frágil de cortes parciales (ngram+pasada2) que oscilaba entre over-cut ("enseñar") y dejar repeticiones (clamp). El modelo ve todo y decide coherente: 1 instancia por repetición/CTA/precio, frases enteras. Guarda anti-alucinación (rechaza si conserva <30%) + fallback al pipeline antiguo. Desactiva ngram+pasada2 si funciona. Prompt: prompts/silence_cutter_clean_script.md.
- Silence cutter holístico: DOBLE REVISIÓN. Pasada 1 limpia el guion (keep_spans); pasada 2 re-revisa SOLO los supervivientes y caza lo que la 1ª dejó (mitiga no-determinismo LLM). Final = intersección (pass2 ⊆ pass1, solo puede quitar más). Guarda <30% + fallback. 2 llamadas Gemini.
- Silence cutter holístico FALLABA (finish_reason 2 = MAX_TOKENS): analyze_transcript_json usaba max_output_tokens=8192 y Gemini 2.5 Pro agotaba el presupuesto PENSANDO la tarea holística antes de responder → caía al pipeline viejo (mismo coste, mismos bugs). Fix: subir default a 32768 y propagarlo. La pasada 2 (más simple) sí cabía en 8192, por eso esa nunca falló.
- Drive sharing: rol automático por carpeta. entrada→writer (cliente sube vídeos), salida→reader (solo descarga). Antes share_folders aplicaba un rol único (reader por defecto) → entrada quedaba solo-lectura y el cliente no podía subir. role=None ahora = rol por función; role explícito sigue forzando todas.
- Presidentes variante NÚMEROS: nuevo `src/numbers_overlay.py` overlay persistente (header fijo + lista 1..N que se rellena en orden countdown). Reveal timing = duraciones acumuladas de segmentos capturadas en `_render_presidents_video_headless` (ahora devuelve `(out_path, reveals)`); cada reveal {puesto,name,reveal_time}. Toggle por vídeo `numbers_variant` (sustituye al hook box animado); estilo global `numbers` (config request-level, persistido en presets flat `numbers_*`). Slot==puesto, lista top→bottom (1 arriba). Reusa render_hook_box para el header.
- Calibración Presidentes: gate pre-TTS por longitud de guion. Antes cada reintento regeneraba guion+6 TTS (5 intentos = ~$0.29). Ahora regenera SOLO el guion (OpenAI ~$0.0004) hasta que la duración ESTIMADA (chars/cps) cae en banda 60-66s, y solo entonces sintetiza TTS (1 tanda ~$0.06). `cps` (chars/seg) auto-ajustable por type_key en Redis (`tiktokCR:cps:*`, EMA). Acepta cualquier TTS ≥60s (re-sintetiza solo si quedó corto). MAX_TTS_ATTEMPTS=2, MAX_SCRIPT_TRIES=8. Header variante números ahora usa video_title completo, no hook_box_text corto.
- Editor manual (Fase 1): silence_cutter modo manual (config.manual_keep_intervals → salta detección, renderiza intervalos dados); persiste proyecto editable (input+words+keep_intervals) que run.py reubica en salida/.editproj/<output>.json. Base para el editor de retoque.
- Editor manual (Fase 2): run_flow/pipeline/runner aceptan manual_keep_intervals + output_override (reemplaza salida mismo nombre, sin tocar recuperacion). Endpoints GET /users/{id}/output/editproject (words+keep_intervals+original) y POST /users/{id}/output/manual-render (encola job retoque, actualiza .editproj). source omitido → runner no toca el ciclo cola/recuperacion.
- Editor manual (Fase 3): ManualEditor.tsx (overlay móvil-first) — player del original + línea de tiempo de unidades (palabra/silencio) verde/rojo tappable + transcripción con chips quitar/restaurar + preview que salta cortes + "Renderizar y reemplazar". Botón "Retocar" en cada vídeo de salida. Hooks useOutputEditProject/useManualRender.
- Whisper deadlock vuelve incluso en subproceso aislado (large-v3 en CPU, fichero largo). Fix: _transcribe reintenta con modelo más ligero (whisper_fallback_model, def small) si large-v3 da timeout (def 600s) → transcripción real en vez de corte degradado. Modelos que funcionan no penalizan.
- Silence cutter: detector stretched_filler — palabra corta (<=3 chars o filler tipo la/eh/y) que dura >=1.0s, o CUALQUIER palabra >=1.8s, se corta (Whisper etiqueta risa/ruido/micro-lejos como palabra estirada). Determinista, aditivo; palabras reales (0.1-0.6s) nunca disparan. Resolvió el "la" de 2s de Eunice.
- Whisper editor_auto colgaba large-v3 (deadlock ctranslate2/int8 multi-hilo en CPU del VPS): fix = subproceso fuerza OMP_NUM_THREADS=1 + WHISPER_CPU_THREADS=1 (env opcional leída en _get_whisper_model) → single-thread, más lento pero NUNCA cuelga; timeout primario 600→1200s.
- Quality score editor_auto rediseñado: antes 100-silencios*10 (degradado sin palabras daba 100 falso). Ahora pondera FALLOS REALES: transcripción fallida (cap 30), palabras sueltas (-12, clip<0.85s solo relleno), estirados supervivientes (-12), silencios -3. needs_requeue si <90 o degradado → aviso UI/log de reencolar.
- Protección del cierre (editor_auto): una capa AI/holistic podía comerse las últimas palabras (CTA final) dejando el vídeo truncado a media frase. Fix quirúrgico en run(): clipa cualquier corte que llegue hasta la última palabra hablada (voiced_words) → solo recorta el silencio posterior; dedups internos intactos. config protect_closing.
- Deadlock ctranslate2 sigue flaky incluso con cpu_threads=1/OMP=1 (15 hilos dormidos, 0 CPU). Watchdog mejorado: detecta cuelgue por INACTIVIDAD de CPU del subproceso (/proc/pid/stat utime+stime, stall 150s tras warmup) → mata en ~2-3min no en 20; _transcribe reintenta large-v3 1× (proceso fresco, flaky suele pasar) antes de caer a small.
- Box escalado a CPX42 (8 vCPU AMD EPYC-Genoa / 16GB). Whisper vuelve a multi-hilo con ESCALADO: large-v3 a primary_threads (default 4, config whisper_cpu_threads) → si watchdog lo mata por deadlock, reintenta large-v3 a 1 hilo → si sigue, small. OMP_NUM_THREADS=1 siempre (evita OpenMP anidado). QUEUE_WORKERS default 2 = 2 ediciones en paralelo (ya OK con 16GB).
- Multi-hilo Whisper (4) cambiaba la transcripcion vs 1 hilo (FP no-determinista en ctranslate2 paralelo) → a veces peor (perdia palabras como "naranja", cortes en sitios distintos). Revertido default whisper_cpu_threads=1 (calidad consistente, la version casi perfecta era a 1 hilo); throughput via QUEUE_WORKERS=2 (2 ediciones en paralelo, cada una 1 hilo). Plan _transcribe: 2 intentos large (primary + 1 hilo) antes de small.
- "du"/residuo tras la ultima palabra: fade-out del ULTIMO segmento alargado a 150ms (_FADE_TAIL_S) en _apply_cuts_ffmpeg → desvanece la cola del cierre (clic/boca/artefacto) sin clipar la palabra. Resto de segmentos siguen con micro-fade 20ms.
- Proteccion de contenido pegado: cortes de IA/dedup (ai_holistic/ai/ngram/ai_pass2) que empiezan en mitad de voz continua (no en silencio) mueven su inicio al primer silencio real del span → no se comen palabras pegadas sin pausa al false-start (ej "naranja" pegada a "aprovecha"). Si no hay silencio en el span, no corta. config protect_glued_content.
- Revertida la proteccion de contenido pegado (protect_glued_content): salvar palabras pegadas a un false-start traia el doble "aprovecha" + colaterales (saltaba 7 cortes, dejaba estirados) → bajaba de 97 a 85. Decision usuario: priorizar limpieza global (97); palabras pegadas sin pausa se pierden (caso raro, se evita si la creadora pausa). El cierre (du fade) y demas fixes se mantienen.
- Consistencia de score: corte determinista de cola de FILLERS alargados (tok en _FILLER_TOKENS y dur>=1.2s) en silence_cutter → conserva 0.15s cabeza, corta resto. Quita los "aaa"/"laaa" de relleno que sobrevivian segun la loteria de Gemini (score variaba 85<->97). NUNCA toca palabras reales (adidas no esta en _FILLER_TOKENS). config cut_stretched_filler_tails.
- Score: penalizacion de estirado superviviente ahora PROPORCIONAL a la severidad (overlap <1s = -6 menor; >=1s = -12). Un "aaa" de 0.6s pegado/inevitable ya no tira el score a 82-85 cuando el video es bueno. La opcion 3 (filler-tail cut) ya quita los fillers cortables; los pegados sin silencio (no cortables sin clipar vecinos) quedan penalizados de forma justa.
- ESTABILIDAD: holistic movida de Gemini 2.5 Pro (thinking, no-determinista ni a temp0) a GPT-4o + seed fijo (_HOLISTIC_SEED=7) + temp0 = casi 100% determinista. Mismo video -> mismo corte SIEMPRE (estable y AFINABLE). openai_client acepta seed. Gemini queda solo de fallback si no hay OPENAI_API_KEY. Toda la cadena (whisper 1-hilo + capas deterministas + holistic gpt4o-seed) es ahora repetible.
- Holistic determinista usa gpt-5.4 (verificado en server: acepta temperature=0 + seed = determinista; gpt-5.5 es razonador, rechaza temp). Cost-tracking: anadidas tarifas gpt-4.1 (2/8) y gpt-5.4/gpt-5 (1.25/10 estimadas, calibrar con /costs real).
- Holistic dejaba re-takes (frase repetida): "aprovecha que ahora estan/estamos" y "la marca de fajas... [micro]... la marca de fajas". Fix: reforzado prompt silence_cutter_clean_script.md con seccion RE-TAKES/RESTARTS (cortar primer intento, conservar el ultimo, aunque cambie una palabra o haya un aside en medio) + la regla SAFE ya no protege re-takes casi identicos. Determinista (gpt-5.4) => se aplica siempre.
- Repeticiones literales que el holistic IA se dejaba (ej "aprovecha que ahora.. aprovecha que ahora"): el detector n-gram (determinista) solo corria si el holistic fallaba. Ahora corre SIEMPRE (red de seguridad, gratis) con gap 1.0s. Caza restarts literales aunque el holistic los deje. El re-take con interrupcion (fajas) lo arreglo el prompt; el literal lo caza el ngram.
- Whisper no-determinista entre runs (n_words 392/353/372 mismo audio) por el fallback de temperatura de faster-whisper. Fix: temperature=0.0 (sin fallback) en subtitles_only → transcript repetible. + ngram restart: ahora caza repeticiones con 1-2 palabras interpuestas ("aprovecha que ahora ESTAN aprovecha que ahora") que el ngram inmediato se dejaba.
- A/B holistic gpt-5.4 vs gpt-4o (ambos temp0, mismo transcript): gpt-5.4=97 (corta el restart "aprovecha que ahora estan"), gpt-4o=94 (lo deja doble). Ganador gpt-5.4: mejor calidad + input mas barato ($1.25 vs $2.50/1M). Fijado gpt-5.4.

- Web→box upload: ticket HS256 firmado (WEB_UPLOAD_SECRET compartido), bytes directos al box (fuera de Vercel), streaming a disco + topes/día + rate-limit Redis. styleConfig(web)→tool_flow vía style_mapper (silence_cutter+subs_auto+sticker_arrow), sincronizado en send-to-edit. salida/<dia>/ espejo de entrada/<dia>/.
- FastAPI: ruta estatica (GET /settings) tapada por /{user_id} si se declara despues -> 404. Declarar rutas literales ANTES de las parametrizadas.
- "Failed to fetch" al subir desde web = preflight CORS 400: el contenedor api corria con API_CORS_ORIGINS viejo aunque .env estaba bien. Fix: docker compose up -d --force-recreate --no-deps api (restart NO recarga env, recreate si).
- Drive (drive_uploads): NO usar googleapiclient/httplib2 en endpoints concurrentes (no thread-safe + sin timeout) -> deadlock agota el threadpool y el server da 502 (CPU/RAM libres). Fix: cliente requests.Session con timeout duro.
- APIError no aceptaba status_code= (era atributo de clase) -> cada APIError(...,status_code=409) en web_upload crasheaba con TypeError 500 = "fallo al subir". Fix: __init__ acepta status_code/code override.
- CORS regresiona en cada auto-deploy: el webhook recrea el contenedor con el DEFAULT de docker-compose (no propaga API_CORS_ORIGINS del .env) -> navegador recibe respuesta sin ACAO -> "Failed to fetch". Fix: meter los origenes web en el DEFAULT de docker-compose.yml.
- Aprobacion manual (gate beta): web_output retiene vídeos hasta admin aprueba (Redis webday_approved); endpoints /web/admin/{pending,approve,stream}; stream auth por query key (video src no manda headers); posicion en cola solo para planes.
- subs_auto: ultima palabra se congelaba hasta el final del video (render extiende ultimo chunk a video_duration). Fix: pasar last_chunk_max_end = fin_ultima_palabra + 1s.
- Trial visible (nebulabs:user.trialVideos) no se descontaba al usar video; ahora send-to-edit lo resta por video encolado (solo sin plan). Es distinto de la cuota interna del box (quota_service).
- Herramientas base opcionales: styleConfig.cuts.enabled + subtitle.enabled (+ arrow.enabled) -> build_tool_flow las incluye condicionalmente. El cliente puede quitar subs/cortes aunque sean base.
- Reinicio NO rompe vídeos web: _reset_orphans re-encola (PENDING) los jobs editor_auto de subida web (output_subdir, sin guion), máx 2 reanudaciones; el resto FAILED. El input sigue en Drive, se reanuda solo.
- Cola: jobs de admin (todo lo que NO es edición web de cliente = EDITOR_AUTO con output_subdir) tienen PRIORIDAD sobre las ediciones de clientes pendientes. 1 worker, no satura; solo cambia el orden. Helper _is_client_edit_job en manager.py.
- Flecha CTA negra invisible sobre fondo oscuro: el asset flecha_negra.mov era negro puro/fino/alpha tenue. Fix: regenerada con contorno blanco (recolor de flecha_roja.mov → negro + silueta blanca dilatada+blur, prores_ks 4444 yuva444p10le) e instalada en Assets/flechas (original guardado como flecha_negra_original.mov). No requiere cambio de codigo.
- Set de flechas CTA: generadas variantes de color (blanca/amarilla/cyan/verde) recoloreando flecha_roja.mov + contorno, animadas, en Assets/flechas. _ALLOWED_ARROWS y web ARROW_SHAPES ampliados. Previews en web public/arrows.
- Editor web: edicion diferida por dia via Job.scheduled_for = apertura de ventana del plan (UTC); cuota por dia contada con day_jobs (no rolling counter); envios incrementales con webday_sentfiles; reset_orphans solo toca RUNNING asi que los PENDING programados sobreviven a deploys.
- Subs 'una palabra': animacion pop de entrada (escala 0.82->1.0 + fade ~160ms ease-out) via Job overlay clip.resize(fn)+crossfadein en subtitles_only; flag style entrance_anim (pop/none), seteado por style_mapper segun modo (word=pop). 'varias palabras' sin animacion (solo cambia resaltado).
- Flechas CTA animadas generadas con PIL+ffmpeg (scripts/gen_animated_arrows.py): trazo uniforme por dilatacion alpha (MaxFilter), ProRes 4444. Estilos avanza/pulso/rebote/triple-chevron x{blanca,roja}. style_mapper: _SMART_POOL (simples) separado de _ALLOWED_ARROWS (todas) para que el modo auto solo cambie color, no forma.
- Borradores web sin enviar: sweeper de fondo (draft_sweeper, loop en lifespan cada 30min) - recordatorio email antes del cierre, arrastra al proximo dia abierto con cuota libre, borra tras GRACE_DAYS por mtime. 'Enviado' = set webday_sentfiles.
- Sweeper duplicaba originales ya editados: el flujo web marca webday_sentfiles pero admin/watcher NO. Fix: _processed_files = sentfiles ∪ filenames en webday_jobs del dia. Asi no arrastra ni duplica lo ya encolado por cualquier via.
- Cortador silence_cutter: 4 fixes globales (1) _absorb_keep_islands quita micro-islas <0.28s sin contenido (fragmento media palabra); (2) quitado gate 'and not holistic_ok' (l.819) que apagaba pasada2 false-starts = regresion; (3) _reanex_orphan_phrases re-anexa frase a palabra suelta huerfana del holistico (aditivo); (4) audit _detect_word_fragments penaliza palabras partidas (antes daba 94/100 sin verlas).
- Word-guard dejaba la COLA de una palabra eliminada (false-start) pegada antes de la palabra buena ('empezó' antes de 'esto costaba'): el backoff de 150ms protege el INICIO de la siguiente palabra pero conserva basura cuando van contiguas. Fix: si una palabra eliminada termina justo en el borde del cut (habla contigua, sin silencio), no retroceder -> el cut acaba en el inicio de la palabra buena. Cortes de silencio intactos.
- Cortador 100% determinista: gpt-5.4 (no-razonador, temp 0 + seed) en TODAS las pasadas (holistic, pass1, pass2). Gemini OFF (es 'thinking' -> varia aunque temp 0). pass1 estaba en temp 0.2 (no determinista) = causa raiz de inconsistencia. Diag del holistic etiquetaba mal 'gemini' siendo gpt-5.4.
- Audit profundo automatico: re-transcribe el OUTPUT (mismo Whisper large-v3) y alinea tokens vs palabras esperadas (en keeps). Penaliza -8/bloque sobrante o perdido y dispara needs_requeue. Clave anti-FP: variantes insert<->miss ratio>=0.6 se cancelan (Whisper re-interpreta en bordes: bebida->beber/vida); sobrante >=2 tokens o >=6 chars; perdida >=3 tokens con contenido. Validado: 0 FPs en renders buenos + detecta fallo simulado.
* [2026-06-10] Deploy mató un job en curso: `docker rm -f` + `--force-recreate` del `api` MIENTRAS un job se editaba (peor con doble deploy seguido) dejó el job FAILED con `"No hay dispatcher registrado en JobQueue"` — race de arranque (el re-encolado de RUNNING se despachó antes de registrar el dispatcher). El re-encolado de orphans NO es 100% fiable bajo force-recreate. Regla: ANTES de desplegar comprobar que no hay job RUNNING (panel cola / `/web/admin/user-pipeline` label "Editándose") y esperar si lo hay; `rm -f` solo para ghost/"Created", nunca para el api activo con trabajo. Recuperar el vídeo perdido: `POST /web/admin/requeue {user_id, day, source}`.
* [2026-06-10] silence_cutter self-heal EMPEORABA vídeos (89→59→68): el bucle hacía os.replace(tmp,output)+keep_intervals=new_keeps SIEMPRE, sin comprobar si mejoraba, y encadenaba el siguiente intento sobre la versión dañada. Una restauración arriesgada (devolver sobre-corte que reintroduce silencio) hundía el score y se entregaba igual. Fix: _full_audit(kints, audit_path=...) audita el render CANDIDATO (tmp) ANTES de moverlo; solo os.replace si cand_score>best_score; si no mejora, descarta tmp y conserva el mejor. Escalado: 1º cortes (seguros), 2º restauraciones (arriesgadas). Garantía: jamás entrega algo peor que el render original.
* [2026-06-10] Mismo vídeo daba scores base distintos (84/89/92/94) entre runs → NO determinista. Root cause: el .mov tenía el stream H264 corrupto (cientos de 'Invalid NAL unit size'), el decode concela errores distinto cada vez → Whisper timings + render varían. Fix: _repair_corrupt_input() al inicio de run() decodifica 30s contando errores de decode y, si supera umbral (8), re-encodea a H264 limpio (crf18/cq19). Vídeos sanos intactos. Rotación: re-encode con autorotate por defecto (hornea→upright, rotate=0) → _ffprobe_meta lee 0 → misma orientación final.
* [2026-06-10] Objetivo "entregar sin fallos de palabra": (1) self-heal prioriza reducir sobrantes+perdidas (deep audit) por encima del score total — acepta una corrección si baja los fallos de palabra aunque el score baje un poco (restaurar una palabra puede dejar un mini-silencio, tolerable); helper _count_word_fallos. (2) Entrega bloqueada: audit marca needs_requeue ante CUALQUIER fallo de palabra (antes solo score<90 → 1 fallo=92 se entregaba = bug "92 con fallos"); web_upload._job_deliverable() retiene si needs_requeue, independiente del score; silencios solo bajan score, no bloquean. Diseño robusto: aunque la IA de corte varíe entre runs (gpt-5.4 no es 100% determinista pese a seed), el audit+heal+gate garantizan que solo se entrega 0-fallos-de-palabra; el resto va a re-edición. Validado: moto fallos 1→0, score 92→100, entregable.
* [2026-06-10] TRAMPA DE DIAGNÓSTICO: ejecutar `docker exec tiktok-api python -c "...get_queue()..."` para inspeccionar la cola ARRANCA 2 workers (JobQueue.__init__ los crea SIEMPRE) en ese proceso efímero, que leen el MISMO queue_state.json, cogen jobs PENDING y los procesan EN PARALELO con el app real → doble ejecución del mismo job_id sobre los mismos editor_step_<job>_NN.mp4 → un proceso borra el intermedio (orchestrator finally) mientras el otro lo lee → "FFmpeg falló: No such file". Rompió jobs de prueba (NO afecta a usuarios reales: en prod solo el app llama get_queue). Regla: para inspeccionar/controlar la cola usar SIEMPRE los endpoints HTTP (curl user-pipeline / queue summary) o leer el diagnostic como FICHERO (json.load); NUNCA get_queue() en un exec. Limpieza si ya pasó: `docker restart tiktok-api` (mata los workers rogue del exec + resetea a una sola cola; seguro si no hay job bueno corriendo).
* [2026-06-10] Restos SUB-PALABRA que el deep-audit NO puede ver (Whisper no transcribe una vocal de ~100ms): tras el snap a palabras completas seguian colandose colas de la palabra cortada previa al INICIO del keep ('o'/'as'/'os' de sedosit-O/florecit-AS/rayadit-O) y el final del keep clavado en el start-Whisper de la siguiente palabra clipaba la ultima buena ('ocho' sin terminar). Causa: Whisper marca limites ~100-300ms ANTES del audio real. Fix estructural (no audit): _refine_keep_edges_to_valley afina LOS DOS bordes de cada keep al valle de energia real (min RMS 20ms) en habla contigua; tmp_audio se mantiene vivo hasta el final de run() para esto (tambien lo usa el self-heal).
* [2026-06-10] Frases que suenan ROTAS en los saltos: (1) palabra funcional colgada al final de segmento ('...azul marino QUE |' o video acabando en '...bueno') → _trim_dangling_tail_words la recorta (lista _DANGLING_TAIL_TOKENS, deja ≥2 palabras); (2) final colgado a mitad de IDEA ('y estan solo por ocho' y salta) que es decision de contenido → _ai_completeness_review: gpt-5.4 temp0+seed ve los segmentos finales en orden y recorta las ultimas N palabras (solo quita, caps: ≤8 palabras, ≤40% del segmento, deja ≥3). Pipeline de bordes final: snap → completitud IA → trim colgadas → valle.
* [2026-06-10] Iteracion 2 de higiene de bordes — DOS defectos de las tecnicas nuevas detectados por re-transcripcion del output: (1) la revision IA de completitud recortaba finales de segmento aunque el siguiente keep CONTINUARA la misma frase al otro lado de un micro-corte de tartamudeo (mutilo 'la cintura es muy muy | muy elastica') → solo revisar segmentos seguidos de salto real >2s en input (o el ultimo), cap 3 recortes/video; (2) el valle del lado FINAL buscaba hacia atras dentro de la ultima palabra buena y la clipaba ('cintur|','rayadito|s') → ventana solo hacia delante desde lw_e (Whisper cierra pronto, la cola real suena despues).
* [2026-06-10] Cortes de tartamudeo deben ALINEARSE a la repeticion: el holistico (varianza run-a-run pese a seed — OpenAI seed es best-effort) corto 'es muy muy' del tartamudeo 'muy muy muy' llevandose el 'es' ('la cintura | muy elastica'). _reanex_stutter_lead_words: si un bloque eliminado (2-5 palabras) termina en N copias del token siguiente conservado, las ≤2 palabras funcionales de cabecera se re-anexan. La varianza de contenido del holistico entre runs es lo que queda de no-determinismo; las reglas deterministas post-holistico la van acotando.
* [2026-06-10] El culpable de 'la cintura | muy elastica' NO era el holistico: era _trim_dangling_tail_words — al incluir 'muy' en _DANGLING_TAIL_TOKENS se comia 'es muy muy' al final de un keep cuyo siguiente estaba a 0.07s (micro-corte de tartamudeo donde la frase CONTINUA). Regla universal aprendida: TODA operacion que recorte el FINAL de un segmento (trim de colgadas, completitud IA) solo puede aplicar cuando el siguiente keep esta a >2s (salto real de contenido) o es el ultimo — en micro-cortes la frase sigue al otro lado. Metodo de deteccion que funciono: re-transcribir el output tras CADA cambio y comparar palabra a palabra; cada round cazo la regresion del round anterior.
* [2026-06-10] Memoria de lecciones del MOTOR (no solo del dev): prompts/editor_lessons.md = errores reales escritos como reglas para la IA; _with_lessons() la inyecta en los 5 prompts de decision de corte (holistico x2, pass1 analyst, pass2 false-starts, completitud) con cache por mtime (editar el md aplica al siguiente job sin redeploy). Workflow al descubrir fallo: regla determinista si es codificable + SIEMPRE leccion en editor_lessons.md + detalle en learnings.md.
* [2026-06-10] Optimización de la memoria de lecciones (coste acotado para siempre): secciones del md etiquetadas `[pasada1|pasada2]` y cada pasada de IA recibe SOLO sus secciones (clean_script 4, false_starts 2, completeness 3, analyst 0 = cero overhead donde no hay lecciones); preámbulo humano nunca se envía; CAP duro 6000 chars/pasada con aviso de CONSOLIDAR al superarlo; cache por mtime; lecciones pegadas al system prompt (prefijo estable) → prompt-caching automático de OpenAI descuenta ~50% en repetidas. Peor caso a tope de cap: ~7.5k tokens extra/vídeo ≈ $0.02 — el coste no puede multiplicarse aunque la memoria crezca.
* [2026-06-10] Iteracion 3 bordes: (1) recorte de completitud valido SOLO si el bloque eliminado EMPIEZA en palabra funcional (limite de clausula: 'y estan solo por ocho' ✓, 'marino que' ✗ — partia la frase por dentro y mutilo 'azul marino'); (2) valle del lado final capado a +0.12s dentro de la palabra cortada siguiente (con 40% de su span, una consonante de baja energia como la 'm' de 'marino' hacia caer el valle DENTRO y dejaba el arranque audible). Validado ademas: leccion de rectificacion funciono a la primera (cuadrito naranja fuera) y el gate retuvo el roll malo (92, 1 fallo) sin entregarlo.
* [2026-06-10] Las lecciones de CONSERVAR ('frases enteras', 'no cortes a medias', 'ante la duda no toques') inyectadas al HOLISTICO lo sesgaron a conservar de mas → dejo de cortar correcciones que antes cortaba bien ('mirad este tejido / mirad este estampado', 'bueno no me lo creia' x2). Regla de routing: al holistico (cuya mision es CORTAR) solo lecciones de cortar (tartamudeos alineados + rectificaciones); las de conservar solo a completeness (cuya mision es proteger finales). No mezclar sesgos de mision en el mismo prompt.
* [2026-06-10] Subs 'saltan' aunque la 1a linea este anclada: el salto perceptual es el bloque cambiando de FORMA (1 vs 2 lineas). Fix definitivo: _split_chunks_to_one_line — pre-mide cada chunk con las MISMAS metricas que render_chunk_image (fuente/case/space_w/pad_x/max_line_w) y parte los que envolverian → todo chunk es 1 linea, altura y forma constantes.
* [2026-06-10] CIERRE del video = regla propia (lo que mas se nota): _complete_final_phrase — si la ultima palabra conservada va seguida inmediata (<0.5s) de mas habla cortada, la frase quedo a medias → EXTIENDE el ultimo keep palabra a palabra hasta la siguiente pausa real del hablante (caps 6s/15 palabras); sin pausa alcanzable → recorta hacia atras hasta la frase anterior con pausa. Las reglas de solo-recortar no podian arreglar un final elegido a medias por el holistico; extender hasta pausa si.
* [2026-06-11] "Vídeo input no encontrado" pese a subida OK: Google Drive PERMITE carpetas con el MISMO nombre. 4 subidas concurrentes (el navegador pide N upload-url casi a la vez) corrieron ensure_day_folder en paralelo; con consistencia eventual de Drive, la búsqueda de cada una no veía la carpeta recién creada por otra → se crearon DOS carpetas '2026-06-11'. El mount de rclone entra en una (por nombre) y las subidas fueron a las dos → el pipeline leía la carpeta equivocada. Fix raíz: (1) _find_child con orderBy=createdTime → elige SIEMPRE la más antigua (API y mount coinciden); (2) ensure_day_folder con lock + cache por (user,día) → nunca crea duplicados bajo concurrencia. Recuperación de un día ya duplicado: rclone move 'gdrive,root_folder_id=DUP:' 'gdrive,root_folder_id=CANON:' (la SA copia pero NO puede borrar si los archivos los posee la cuenta del usuario vía OAuth). Además run.py reintenta ~90s el input (consistencia eventual del mount).
* [2026-06-11] Subtitulos a alturas distintas pese al anclaje: base_h se calculaba pre-renderizando TODOS los chunks en un try/except; si fallaba (o daba 0), y_first_top_px=None y se caia al centrado-por-chunk que DERIVA. Fix robusto: base_h de un probe de 1 palabra (render_chunk_image([{word:'Mg'}]), nunca envuelve), y_first_top_px SIEMPRE definido -> 1a linea de todo chunk centrada en y_pct, constante. + log base_h/y_top.
* [2026-06-11] Flecha CTA (sticker_arrow): el fuzzy ya captaba carrito<->carito (edit-dist 1, >=5 chars), pero solo se buscaba en el ultimo 50% del video (anti-FP de 'aqui'). Si el CTA fuerte cae antes, se perdia. Fix: _STRONG_CTA (carrito/comprar/link/perfil/enlace/tienda/checkout...) se buscan en TODO el video; las debiles (aqui/abajo) solo en el ultimo tramo. En buga_2 la flecha SI salia (carrito@28.84s, ultimo tramo) — el fix la hace robusta a CTA en cualquier posicion.
* [2026-06-11] Flecha CTA solo 3.5s: default extend_to_end=False. El cliente la quiere persistente hasta el final del video (TikTok Shop). Cambiado default a True; build_tool_flow lo recoge via _with_defaults (arrow_overrides no lo pisa). Verificado tambien el fix de subtitulos: 3 frames de buga_2 re-render a altura identica (~78%, antes 73/78/80).
* [2026-06-11] Carpetas de USUARIO duplicadas por mayusculas: usuario web "bugalleitor" + admin viejo "Bugalleitor" -> dos carpetas en Drive. Drive busca CASE-INSENSITIVE pero el mount resuelve CASE-SENSITIVE: el upload de bugalleitor (via _find_child case-insensitive + orderBy oldest) caia en Bugalleitor (vieja) y el pipeline leia la ruta literal bugalleitor -> "input no encontrado". Fixes: (1) _find_child filtra al match EXACTO de nombre (API y mount coinciden); (2) unique_user_name case-insensitive (incluye borrados) -> nunca dos usuarios que difieran solo en mayusculas. Recuperacion: cp del video al folder correcto via mount (case-sensitive) + requeue; soft-delete del user viejo. Nota: la SA NO puede borrar la carpeta Drive vieja si sus archivos los posee otra cuenta (gemini outputs) -> queda huerfana pero inofensiva con el fix exact-case.
* [2026-06-11] OVER-CUT del holistico (lo mas grave de cara a publico): gpt-5.4 se comia contenido REAL — "proteina" (ingrediente), "asegurate de" (lead-in CTA), "te lo dejo aqui en" (CTA), "porque yo" (conector). El deep-audit NO lo detecta: solo ve over-cuts de palabras que el holistico queria mantener; si el holistico quita "proteina", no la "espera" -> score 100 y se entrega. Fix 3 capas: (1) prompt clean_script MUCHO mas conservador (default=KEEP, lista NEVER REMOVE: nombres/ingredientes/precios/CTAs/conectores, TEST: la idea exacta se repite donde conservo?); (2) editor_lessons.md leccion 'Nunca borrar contenido unico' con los ejemplos; (3) red determinista _protect_unique_content: re-anexa tramos eliminados que contengan una palabra de contenido UNICA (≥5 chars, no stopword/muletilla/verbo comun, freq==1 en toda la transcripcion). Repeticiones (freq≥2) y re-takes (formas verbales) no se protegen.
* [2026-06-11] OVER-CUT por fases ACUSTICAS (no el holistico): el holistico mantenia 109/109 palabras pero acoustic(15)/silero(10)/inter_word_gap(7) se comian habla real (proteina y, un ojo y, te lo dejo aqui en). _protect_word_boundaries solo guarda los BORDES de palabra, no impide que un corte ENGULLA una palabra entera que cae en un valle de energia (habla floja). _protect_unique_content solo corria DENTRO del holistico -> no protegia de cortes posteriores. Fix robusto (invariante global): _protect_words_from_acoustic_cuts tras invertir cortes -> una palabra Whisper solo desaparece si una fase de CONTENIDO (_CONTENT_CUT_SOURCES: stretched_filler/ai_holistic/ai/ngram_repetition/ai_pass2) la quito a proposito; si solo la engullo una fase ACUSTICA (auto_trim/inter_word_gap/acoustic/silero) se RE-ANEXA al keep. Solo anade, nunca corta -> en videos limpios n=0 (los cortes acusticos caen en silencio, sin centro de palabra).
* [2026-06-11] ROOT CAUSE real del over-cut de bugallo (no era acustico puro): _filter_phantom_words descartaba palabras de contenido como fantasmas de Whisper. Audio ruidoso (noise_db -24 > speech_rms -27) -> Silero VAD marca silencio [11.6-12.9] donde hay voz floja -> proteina (12.16-12.6) cae dentro -> se borra de voiced_words ANTES del holistico. Por eso _protect_words_from_acoustic_cuts (itera voiced_words) no la veia. Fix: _is_real_content_word -> palabra >=5 chars, no filler/stopword/credito-Whisper (gracias/suscribete) y CONTIGUA a otra (gap<=0.5s) = run de habla real, no alucinacion aislada -> no es fantasma. Aplicado en ruta Silero y amplitud. Los dos fixes juntos: phantom-guard la mantiene en voiced_words + acoustic-protection la re-anexa si silero_high la engulle.
* [2026-06-11] Nivelar audio (loudnorm -16 LUFS) antes de Whisper+Silero = root fix para voz baja. En grabaciones de mala SNR Silero marca silencio sobre habla floja -> palabras reales (proteina/carton) se pierden como fantasma. _level_audio sube toda la pista al nivel que esperan los modelos SOLO para deteccion (transcribe+VAD); amplitud/silencios siguen sobre el audio ORIGINAL -> no degrada SNR ni alarga el video. Resultado bugallo: proteina+carton+echarle un ojo KEEP, phantoms 9->6, ~16s cortados de 46s (no se alarga). Mejor que parchear downstream (phantom-guard/acoustic-protect siguen como red de seguridad). config: level_audio_for_vad (default True).
* [2026-06-11] Pasada FINAL de coherencia (el 100 ahora es fiable): el audit solo miraba residuos/silencios acusticos -> daba 100 con sin-sentidos (dice dos ingredientes y nombra uno). _ai_coherence_judge (gpt-5.4 temp0+seed) compara la transcripcion REAL del render (reusa la del _deep_audit_compare, coste Whisper 0) vs el original y detecta promesas rotas/referencias colgadas/saltos ilogicos/numeros alterados. Score se pliega con min() -> 100 solo si pasa acustico+palabras+SENTIDO. Defectos graves+arreglables entran al self-heal (restauran las palabras) via _derive_self_heal_actions; acceptance key lexicografica (coh_fallos, word_fallos, -score). Cada fallo grave se escribe en editor_lessons.md (idempotente por fingerprint) -> aprende. Pre-screen (match>=0.97 + sin fallos + sin enumeracion junto a corte) salta el LLM -> coste medio ~como hoy, tope ~0.05/video. Guards: cuts_summary al juez, token-verify (palabra perdida realmente ausente), citas deben existir, normalizacion SIN acentos (Whisper sin tilde). Gate _job_deliverable: tercer check de coherencia.
* [2026-06-11] Audio de OUTPUT: loudnorm uniforme NO basta para palabras dichas muy bajo (sube todo por igual, lo flojo sigue flojo). speechnorm (e=12.5:r=0.0001:l=1) sube los MOMENTOS de voz floja hacia el pico -> se oyen (recupera precio euros, etc). NO encadenar loudnorm despues: re-comprime y deshace el lift (verificado: speechnorm,loudnorm pierde euros; speechnorm solo lo recupera y deja -15dB). Solo cuando normalize_audio (media<-24dB). Caso limite: una palabra REALMENTE mumblada (proteina de bugallo) es irrecuperable ni con dynaudnorm m25 -> el juez de coherencia la marca y RETIENE el video (correcto: dice dos ingredientes y nombra uno). El juez pilla lo que el deep audit no veia.
* [2026-06-11] FALSO POSITIVO del juez de coherencia por recall de Whisper: comparaba contra la re-transcripcion del render (deep.out_text), pero Whisper a veces NO re-transcribe una palabra que SI esta conservada en el video (recall flaky en audio cortado) -> marcaba proteina/euros como faltantes aunque estaban en los keeps. Verificado: el input RAW transcribe euros/proteina/dias perfecto; el output re-transcrito los pierde. Fix: el juez evalua las palabras CONSERVADAS (centro dentro de un keep = la decision del EDITOR), no la re-transcripcion. Asi distingue conservada (no marca) vs cortada por el editor (marca+restaura). El deep audit sigue cubriendo el render-drift por separado. El audio NUNCA fue el problema (revertido speechnorm a loudnorm).
* [2026-06-11] Deep audit retenia videos buenos por ALUCINACION DE COSTURA: al re-transcribir el output, Whisper inventa palabras en la junta de un corte (un clic/glitch oido como aprende a) y las marca como inserted_block (residuo) -> needs_requeue falso. Fix: _is_real_residue -> un residuo REAL es la cola de algo dicho (estocost de esto costaba), asi que al menos un token de contenido debe parecerse (ratio>=0.7) o contener (substring>=4) una palabra del ORIGINAL. Si no se parece a nada dicho = alucinacion -> se descarta (solo evita el falso fallo, nunca corta mas). Caso bugallo: aprende a@17.9s en la junta donde se corto es -> descartado.
* [2026-06-11] El audit penalizaba el score por CONSERVAR una palabra de contenido con span inflado: asegurate (Whisper 2.64s = palabra + pausa absorbida) se contaba como surviving_stretched (-12) aunque el editor la dejo bien. Fix: _surviving_stretched_spans con words -> exenta spans que son palabra de contenido (>=5 chars, no filler/stopword); solo penaliza rellenos reales (laaa/eeeh). Cero cambio en cortes. La pausa absorbida dentro la coge el detector de silencio interno aparte (y el self-heal la corta sin pelear contra el penalti de stretched).
* [2026-06-11] ROOT CAUSE de proteina comida (el usuario oia silencio en su slot): Whisper MAL-ALINEA la palabra. proteina suena a ~10.5-11.5s (-25dB, voz clara) pero Whisper le pone timestamp en 12.16s (que es SILENCIO -56dB). El cortador ve un hueco sin palabras 8.3-12.16 y lo corta entero como pausa, llevandose el audio real de proteina; deja el slot mudo 12.16 + y crema de arroz. NO era recall de Whisper. Fix: _preserve_speech_islands_in_cuts antes de invertir cortes -> en cada corte >=1s mide RMS por ventanas y rescata islas (>=0.22s, >=14dB sobre el suelo de ruido del propio corte) partiendo el corte para conservarlas. Relativo al silencio local, asi capta voz floja (-25dB) que un umbral absoluto (-24) trataba como silencio. Verificado: corte [8.3-12.16] -> [8.3-10.48]+[11.38-12.16], proteina@10.9 rescatada. Solo reduce el corte, nunca corta mas.
* [2026-06-11] Corte acustico clipaba voz real pegada a sus bordes (naranja a medias, asegurate comido). Causa: Whisper se salta naranja e infla el span de asegurate [29.8-32.4] englobando naranja+pausa+asegurate; su CENTRO cae en el silencio, asi que el snap a palabras descarta la voz real del borde. Fix general _extend_keeps_to_voiced_edges (paso final de bordes): mide energia hacia fuera de cada borde de keep y lo extiende mientras siga habiendo VOZ (>= nivel_borde-22dB Y >= suelo_silencio_global+12dB) sin entrar en cortes de CONTENIDO, parando en el silencio real (max 0.6s). Puro audio, agnostico a timings de Whisper -> arregla cualquier palabra mal-alineada en un borde. Solo anade keep, nunca clipa. Verificado: keeps [26.76-29.9]+[32.4-34.32] -> [26.64-30.17]+[32.01-34.32], naranja y asegurate rescatados, silencio del medio intacto.
* [2026-06-11] Tras rescatar voz mal-alineada quedaban dead-air pegados: Whisper etiqueta como palabra slots mudos (proteina fantasma@12.16) o da spans inflados a un y de ~1s casi todo silencio -> el cortador no los quita (protegidos como palabra). Fix _trim_internal_silences_from_keeps (paso final): mide energia por ventanas y recorta tramos < suelo_global+10dB durante >=0.35s dentro de un keep, dejando 0.16s de pausa natural; respeta pausas cortas. Ademas _extend_keeps_to_voiced_edges ya NO extiende el inicio del primer keep ni el final del ultimo (bordes del video) -> evita capturar respiracion/click post-discurso (sonido raro al final). Hay fade-out de cola 0.15s para el residuo de loudnorm.
* [2026-06-11] Cierre de video con sonido raro residual (cola de boca/lookahead de loudnorm) resistente a recortar el keep -> es a nivel de RENDER. Fix combinado: _clamp_last_keep_to_last_word (recorta el ultimo keep al valle de energia tras la ultima palabra) + fade-out de cierre subido 0.15->0.28s que enmascara el residuo sin clipar la palabra. Ademas tras recortar silencios internos, coherencia daba FP por palabra mal-alineada (centro fuera del keep): cambiado a contar conservada si el span SOLAPA un keep >=30%.
* [2026-06-11] REVERT: el recorte de silencios internos (_trim_internal_silences_from_keeps) + clamp de cierre causaron 2 regresiones: (1) cortaba VOZ FLOJA tratandola como silencio (claro ya me cuentas perdio el inicio); (2) la fragmentacion de keeps hizo que el rescate de voz en bordes capturara un tartamudeo (muchisimo inflado 2.78s contenia el false-start). Leccion: recortar/extender por energia no distingue voz-floja/tartamudeo de silencio/palabra-clipada -> demasiado agresivo. Revertido a b211452 (bd29f723, bueno: proteina+naranja+asegurate via island-rescue + voice-edge-rescue, sin trim de silencios). Los 2 silencios pequenos + sonido final quedan como conocidos, a re-abordar con salvaguardas (no cortar si hay palabra Whisper aunque sea floja; no extender hacia false-starts).
* [2026-06-11] FIX DE RAIZ del mis-alignment de Whisper: _shrink_inflated_word_spans corre justo tras transcribir (sobre el audio nivelado) y ENCOGE solo los spans inflados (palabra + pausa absorbida: asegurate 2.6s->0.47s, justo 2.06s->0.25s) a su voz dominante. Seguro POR CONSTRUCCION (shrink-only in-place): no mueve/inserta/borra/reordena/re-textea -> no puede perder palabra, resucitar tartamudeo, ni cortar voz floja. 5 gates: candidato por dur/token, umbral RELATIVO al suelo local del span (voz floja a -25dB = voz), prueba de inflacion (hueco mudo contiguo >=0.35s), anchor al onset Whisper (encoge la cola), gate5 anti-claro-ya (la cola liberada debe ser silencio contiguo, no silaba floja). Libera el dead-air para que el inter-word-gap lo corte. proteina/naranja (mis-placed/merged) siguen con island-rescue (mas efectivo sobre las islas liberadas). Kill-switch realign_inflated_spans. Reemplaza el whack-a-mole de parches downstream (todos revertidos).
* [2026-06-11] CTA de cierre partido (clado): pass2 marca el CTA final (te lo dejo aqui anclado, mal-transcrito como te lo dejo aqui en claro) como abandoned_phrase y lo corta a mitad de palabra anclado [44.52-44.92] en 44.69 -> deja clado. La protect_closing solo cuidaba la ultima palabra. Fix: protect_closing_phrase protege el ultimo BLOQUE CONTIGUO de habla (gaps<0.6s) = la CTA+despedida; cualquier corte que empiece antes y acabe DENTRO se recorta a run_start -> la frase de cierre queda entera. + fade_tail 0.15->0.25 para enmascarar el ruido de render del ultimo frame.
* [2026-06-11] ROOT CAUSE del ruido raro del ultimo frame: ORDEN de filtros. El fade-out de cierre se aplicaba a los SEGMENTOS (antes del concat) y loudnorm corria DESPUES. Como el ultimo segmento se desvanecia, loudnorm SUBIA la ganancia para mantener -16 LUFS sobre lo que se volvia silencio -> amplificaba el residuo del final = un burst (-5dB) a ~0.1s del final, despues de la voz. Fix: micro-fade 20ms por segmento (anti-clic concat) + fade de cierre 0.25s SIEMPRE DESPUES de loudnorm (loudnorm ve nivel pleno, no sube ganancia; el fade cierra limpio). Verificado: pico ultimo 0.05s baja a ~-28dB sin burst. Robusto y general (cualquier video).
* [2026-06-11] El burst del ultimo frame tambien lo metia subs_auto (subtitles_only/moviepy): CompositeVideoClip extendia ~0.14s el composite (un sub-clip acababa un pelo mas alla por redondeo) y moviepy leia 1 frame de audio de mas al final = blip aislado tras el silencio. Fix: final.set_duration(video.duration) + audio_fadeout(0.2). Dos capas del ruido final arregladas de raiz: (1) silence_cutter fade tras loudnorm, (2) subs_auto duracion exacta + fadeout.
* [2026-06-11] Cierre de video FIX DEFINITIVO (2 capas de raiz): (1) subs_auto (moviepy) metia el blip del ultimo frame al re-encodear AAC -> ahora moviepy escribe SOLO video y el audio se MUXEA con ffmpeg desde el input sin tocarlo (blip imposible); (2) el fade de cierre fijo de 0.25s del silence_cutter clipaba la ultima palabra cuando esta llena el keep (cuentas->cuento) porque Whisper sub-reporta la fricativa final -> ahora el fade se ancla a _content_end_output_s (fin del audio REAL por energia): si la voz llena el keep el fade es 0, si hay cola muda la desvanece. Nunca clipa la palabra.
* [2026-06-11] cuentas->cuento: el detector de tail_silence + word_guard cortaban el ultimo keep en el fin de palabra de Whisper (sub-reporta la fricativa final). _extend_keeps_to_voiced_edges usa umbral RELATIVO al borde que cae demasiado rapido y no la recupera. Fix: _extend_last_keep_to_word_tail con umbral de SUELO ABSOLUTO (-49dBFS) extiende el ultimo keep hasta el silencio post-voz real -> captura la -s entera.
* [2026-06-12] La extension de la cola de la ultima palabra (_extend_last_keep_to_word_tail) no salia en el output porque el SELF-HEAL acepta un candidato y su render (1541) PISA al principal (1292); solo habia anadido la extension al principal, y _snap_keeps_to_words del self-heal recorta al fin de palabra de Whisper. Fix: aplicar la extension tambien en new_keeps del self-heal tras el snap. Leccion: cualquier ajuste de keeps debe ir en TODOS los render sites (principal + self-heal).
* [2026-06-12] RESUELTO cuentas->cuento del todo: (1) el render del self-heal pisa al principal y necesitaba la extension de cola tambien (61c239b); (2) EL BUG REAL era el deploy: git reset --hard no avanzo en el server (se quedo en commit viejo) y el build uso codigo viejo -> output byte-identico pese a los cambios. Verificado editado: dur 26.30 (+0.1s), Whisper oye cuentas, cierre en silencio total sin blip. SIEMPRE verificar tras deploy: git log -1 == SHA + docker exec grep marcador en /app/src.
* [2026-06-12] Feature revision manual Editor Auto (2df266e): 5a carpeta revision/ (interna, no compartida) + toggle EditorUser.manual_review. Si ON, run.py redirige out_folder a revision/; el admin aprueba moviendo a salida. Clave: move_file ahora maneja el prefijo YYYY-MM-DD/ (los videos de salida lo llevan) preservandolo en destino = espejo entre carpetas. USER_FOLDERS paso de 4 a 5; ensure_user_folders mantiene tupla de 5 (no romper unpack de run.py) y crea revision aparte. Frontend: FolderName/FolderCounts/EditorUser types + UserFoldersPanel (pestana + toggle + botones Aprobar/A revision).
* [2026-06-12] clienta_2 score 32: el modo CONVERSACION auto-detectado (mean_seg<4 + turn_gap_ratio>0.05) subia inter_word_gap_threshold a 1.2s y keep a 350ms -> conservaba TODAS las pausas 0.5-1.2s. En un vídeo de creador con muchas pausas medias = picado + el audit las penaliza = 32. (buga_1 mismo modo pero pocas pausas medias = 100.) Fix: conversation threshold 1.2->0.8s, keep 350->250ms. Son ads TikTok (snappy), no podcasts. Corta las pausas largas dejandolas bajo el umbral del audit.
* [2026-06-12] FIABILIDAD audit (fase 1): el audit profundo marcaba falsos fallos de palabra (euros buga_2, cuentas) porque re-transcribe el output con Whisper y si el modelo no oye una palabra esperada la cuenta missing -aunque su audio este presente. Fix probadamente seguro en _deep_audit_compare: una missing cuyo span cae ENTERO dentro de un keep (margen 0.12s) tiene audio garantizado por el render = recall de Whisper, no perdida -> se descarta. No oculta sobre-cortes (palabra cortada no esta en keeps) ni clips de borde (tocan el borde).
* [2026-06-12] A/B ASR VALIDADO: Deepgram nova-2 (es) vs Whisper large-v3. En buga_2 (falso fallo cronico euros): Whisper 88 -> Deepgram 100/limpio. Whisper transcribe 150 palabras (inventa/parte en audio ruidoso) con spans inflados; Deepgram 112 con timing ajustado -> coloca euros bien -> el juez de coherencia deja de marcar falso. Adoptado: EDITOR_ASR_PROVIDER=deepgram en .env del server + DEEPGRAM_API_KEY. Coste ~0.02$/video (crédito gratis 200$ ~ 10k videos). Adaptador _transcribe_deepgram + fallback a Whisper si falla. Mata la clase de falsos fallos de alineacion que las heuristicas no podian.
* [2026-06-12] CORRECCION del A/B ASR: tras buga_1 el resultado es MIXTO, no triunfo limpio. Deepgram: buga_2 88->100 (arregla falso) PERO buga_1 100->88 (deja 1 palabra suelta: el limpiador de loose-words esta afinado para timestamps de Whisper). Revertido a Whisper (EDITOR_ASR_PROVIDER=whisper) por seguridad en lanzamiento. Deepgram queda como opcion prometedora a validar en TODO el set + re-tunear antes de adoptar. Leccion: no declarar adoptado con 1 video; medir el set entero.
* [2026-06-13] FIABILIDAD audit (fase 2 — la grande): la varianza de score del MISMO vídeo (clienta_2 22/25/28/31) NO es de Whisper (verificado determinista: 367 palabras, hash idéntico a 1 y 4 hilos) sino de las pasadas de corte por IA (ai_holistic/ai/ai_pass2: LLMs que oscilan algo entre runs aun con temp=0). El self_heal era TODO-O-NADA: el lote de cortes de limpieza subía clienta_2 25->77 PERO un corte/higiene clipaba 1 palabra (word_fallo 0->1) y se rechazaba el lote ENTERO por la clave _qkey -> quedaban 18 silencios (hasta 4s de aire muerto) en el output. Fix SALVAGE en silence_cutter.py (todo monótono: solo reemplaza si _qkey queda estrictamente mejor -> NINGÚN vídeo puede empeorar): (1) si un candidato sube >=10pts pero añade fallo de palabra/coherencia, RESTAURA los spans de lo perdido (deep.missing_blocks input_start/end + coherence_issues.restore_span) y CORTA el residuo (deep.inserted_blocks mapeado output->input), conservando la limpieza de silencios; re-render + re-audit. (2) tras una pasada ACEPTADA, re-habilita 'cortes' (tried_kinds.discard) para limpiar los silencios que queden. (3) self_heal_max_attempts default 2->3 (solo gasta en vídeos que lo necesitan; los limpios no iteran). Verificado limpio: buga_1 100->100, buga_2 88->100 (restaura euros), clienta_2 25->88 (silencios 18->4). Constante _SALVAGE_MIN_GAIN=10. Red de seguridad intacta: <90 marca needs_requeue -> con manual_review va a revision/, nunca se publica picado. GOTCHA del harness de test: scripts que se corren con `python x.py` y llaman a Whisper (usa mp spawn) NECESITAN `if __name__=="__main__"` o el subproceso spawn re-ejecuta el script entero (fork-bomb de runs solapados); el salvage lo amplificó por hacer más llamadas a Whisper.
* [2026-06-14] Claude en el VPS (móvil/PC, PC apagado): (1) Remote Control oficial como servicios systemd `claude-remote@<repo>` (uno por repo, en ~/proyectos); login OAuth pilotando `claude` por tmux + capture-pane (los flujos OAuth necesitan TTY); trust/onboarding/remoteDialogSeen pre-seteados en ~/.claude.json o el servicio headless cicla. (2) Chat web propio (menú Claude) con Agent SDK que usa las creds de la SUSCRIPCIÓN Max del host (sin API key ni coste) — backend FastAPI host atado a 172.18.0.1:8765, ufw allow solo 172.18.0.0/16, ruta Caddy. GOTCHA DEPLOY: el Caddyfile es bind-mount de fichero único; `git reset` cambia el inodo y el contenedor caddy ve el VIEJO → rutas nuevas dan 404 aunque `caddy reload`; FIX = `docker compose up -d --force-recreate caddy`. Caddy: para sub-ruta a backend host usar `handle @m { uri strip_prefix /x; reverse_proxy IP:port }` (NO `handle_path` con matcher nombrado, no strippea). Backup chats: cron tar de ~/.claude/projects a Drive (los chats de Claude Code viven SOLO en la máquina, no en la cuenta).
* [2026-06-15] Quitar Copy fallaba en prod `ModuleNotFoundError: No module named 'easyocr'`: el dir DESPLEGADO (/home/nebulabsai/TikTok_Automation_Python, distinto del checkout dev ~/proyectos) tenía `requirements.txt` SIN easyocr aunque `video_remover.py` lo importa (línea 11). La imagen `tiktok-api:latest` se había construido antes de añadir la dep. Fix: añadir `easyocr>=1.7` al requirements.txt del dir desplegado + rebuild de `api` + `up -d api`. GOTCHA build: easyocr arrastra torch → build pesado (~min, imagen 11.5GB) y un `docker compose build` lanzado en background MUERE al desconectar/reconectar la sesión web (hay que relanzarlo). OJO: hay DOS checkouts del repo en el VPS; la sesión Claude corre con workdir en ~/proyectos pero el stack docker corre desde ~/TikTok_Automation_Python — operar compose con `--project-directory`/`-f` apuntando al dir real, no al de la sesión.
* [2026-06-15] Quitar Copy multi-vídeo: VideoUploader pasó de API single (`file`/`onChange(File|null)`) a lista (`files: File[]`/`onChange(File[])`) + prop `multiple` (default true). El componente es COMPARTIDO por copyright, construccion-pov y subs-auto → al cambiar la API hay que adaptar los 3 usos o el build de Next falla en typecheck (construccion-pov/subs-auto usan `multiple={false}` y adaptan single<->array con `files={file?[file]:[]}` y `onChange={fs=>set(fs[0]??null)}`). GOTCHA tsconfig: `noUncheckedIndexedAccess` activo → `valid[0]` es `File|undefined` aunque haya un guard de length antes; usar `valid[0]!`. La página copyright encola en bucle secuencial (un job/vídeo, mismo config) y conserva solo los fallidos para reintentar. Deploy: editar en ~/proyectos NO afecta a producción; hay que copiar a ~/TikTok_Automation_Python + `docker compose build web` + `up -d web`.
* [2026-06-15] Radar de Productos (TikTok Shop): nuevo tab que automatiza la estrategia del operador 100€/día (capturas TG): «productos con inyección de ADS + pocos creadores». INVESTIGADO: Kalodata NO tiene API barata (solo Enterprise custom; Starter $38/mo y Pro $83/mo NO traen API; el "truco 8€" es group-buy del PANEL web → habría que scrapear, frágil + viola ToS). SOLUCIÓN sin Kalodata: EchoTik (ya integrado, API limpia) da influencer_count/gmv/ranklist/vídeos-por-producto. Proxy de "inyección de ADS" = vídeos del producto con views altas + engagement bajo (<1%) que ADEMÁS venden (units_sold>0) → GMV Max inyectando y convirtiendo (mismo proxy que research_service._split_paid_organic pero a nivel producto). Piezas: models/discovery.py (DiscoveredProduct/WinnerScore/AdsSignal), services/ads_signal.py (score_product ponderado ads.30/comp.25/dem.20/mom.15/com.10 + ads_injection_signal por vídeos + DiscoveryFilters), services/discovery_service.py (scan ranklist+keywords → score → deep-ads top N → filter → persist; import_candidate crea Product+carpeta+cover), repos/discovery_repo.py (keyed por product_id, upsert_scored preserva imported en re-scan). Carruseles: pipeline/carousel_director.py prompt-only (como veo3/nano_banana) → JSON slides{on_screen_text+image_prompt EN}+caption, reusa research_context. Smoke test (venv en ~/TikTok_Automation_Python/venv, NO hay venv en ~/proyectos): ganador 97.9, saturado 9.6, ADS "fuerte". GOTCHA: python del sistema sin pydantic; usar el python del venv del dir desplegado para tests.
* [2026-06-15] Radar — automatización end-to-end (creation_pack.py): build_pack(product) orquesta TODAS las piezas existentes para dejar un producto listo sin buscar nada: image_search.search_product_images (DDG, gratis) + photo_downloader → fotos a photos_source/; re_analyze_with_gemini → audiencias/selling_points; research_service.deep_research_product → research_context (analiza VÍDEOS GANADORES del producto, pains/hooks/sonidos); preset_generator.generate_presets(kind=both) → estilos de vídeo (ya tiran del research_context vía _research_block); carousel_director × N; nano_banana_prompt. Escribe archivos REALES en la carpeta Drive (_products/<slug>/PLAN.md + prompt_templates/carousels/*.txt + nano_banana_prompt.txt + video_presets.txt) — el operador abre la carpeta y crea. plan_week(n_products,days) coge top-N candidatos no importados del Radar, importa+build_pack cada uno, y escribe _plans/week_<fecha>.md repartiendo por día. Todo best-effort (un paso falla → warning + sigue). Coste ~$0.17/producto si research ON (necesita Apify para análisis de vídeo; sin él cae a solo reviews). UI: sub-tab "🗓️ Plan 7 días" + botón "📦 Pack completo" por tarjeta importada. CLAVE de diseño: NADA nuevo de generación — solo glue de lo ya construido (research/presets/carousel) + escritura a disco; el vídeo/imagen final lo genera el operador aparte (prompt-only).
* [2026-06-15] Radar — calendario + separación de origen + EchoTik quota. (1) EchoTik es PAY-PER-USE con free trial de 100 requests TOTALES (no diario): el error code 500 "Usage Limit Exceeded, Please Contact Administrator to Increase Quota" = trial agotado (no resetea; confirmado probando con la URL oficial open.echotik.live/api/v2 y creds válidas). MISMA clave/cuota la comparten el buscador de productos existente (src/api/routers/tiktok_shop/discovery.py: /discovery/products + /top-sellers) Y el Radar nuevo → cualquier búsqueda gasta de la misma bolsa. (2) Product.origin Literal["manual","radar"]=default "manual" (todos los existentes quedan manuales por el default al model_validate); import_candidate setea "radar"; tab_products añade filtro radio (Todos/Manuales/Radar) + badge en card. (3) Calendario: models/week_plan.py (WeekPlan+PlanEntry con day/tested/score) + repos/plan_repo.py (key plan:<id>, puntero plan:current, índice plan:index); creation_pack.plan_week ahora _persist_week_plan(day_map) además del _plans/week_*.md; ui sub-tab "📅 Calendario" agrupa por día con checkbox "probado" que persiste (save make_current=False + rerun). No-repeat entre días ya garantizado por plan_week(only_non_imported=True) + flag imported persistido. GOTCHA test: usar el venv de ~/TikTok_Automation_Python (el del sistema no tiene pydantic/dotenv) y load_dotenv del .env del dir desplegado.
* [2026-06-15] Radar — deducción "impulsado con GMV Max" (ads_signal.ads_injection_signal reescrito). Combina señales de los vídeos del producto en gmv_max_likelihood (0-100) + probable_boosted + verdict + reasons: (A) etiqueta AD REAL de TikTok (branded content) leída vía Apify — apify_cloud.extract_ad_flag(item) chequea varios campos (isAd/isSponsored/isPaidPartnership/brandedContent/adAuthorization/isCommerce/brandOrganicType, top-level y anidados) devolviendo bool|None; apify_cloud.search_product_ad_videos(name) busca por nombre y normaliza {views,likes,comments,shares,ad_flag}; (B) vídeos ad-like (views↑/eng<1%) que VENDEN (EchoTik units_sold); (C) % de views pagadas; (D) engagement bajo en top-views; (E) concentración de views. Blend ponderado con renormalización sobre las señales presentes (label 0.40, selling 0.30, paid_view_share 0.25, low_eng 0.15, conc 0.10). discovery_service.discover(ads_provider='echotik'|'apify') vía _fetch_ads_videos: echotik=get_product_videos (ventas, barato, sin label), apify=search_product_ad_videos (etiqueta AD real, sin ventas). EchoTik da ventas pero no label; Apify da label pero no ventas (TikTok no expone ventas públicas) — son samples distintos del mismo producto, el usuario elige fuente. Verificado sintético: producto impulsado 71/100 fuerte (por ventas O por 3/5 etiquetas AD), orgánico (eng alto, sin AD) 4/100 baja=no boosted. CLAVE: el "truco AD lila de Kalodata" NO es métrica de Kalodata sino el badge nativo de TikTok — automatizable vía Apify, no hace falta Kalodata.
* [2026-06-16] Radar EchoTik — validado con creds API reales (trial 100 calls funciona, Basic Auth user=ID password=apikey). HALLAZGOS al probar: (1) /product/ranklist devuelve los productos SIN métricas (total_sale_cnt/gmv/ifl_cnt todo a 0) → inservible para scoring; la fuente fiable es /product/list (search por keyword) que SÍ trae total_sale_cnt, total_sale_gmv_amt, total_ifl_cnt, total_video_cnt, total_views_cnt + breakdowns 7d/30d/60d/90d. OJO: los campos 30d (total_sale_30d_cnt/gmv) suelen venir a 0; el dato vivo está en total_sale_cnt (all-time) y 60d/90d → el score cae a gmv_total (ya tenía ese fallback). (2) /product/list NO soporta sort (sort_field da 500; order_field/sort/sort_by se ignoran) → fetch + sort local por units_sold. (3) España es mercado PEQUEÑO: ganadores con cifras modestas (crocs ballet 872uds/45k€/5 creadores; creatina 224uds/9k€/9 creadores; ventilador techo 8uds/1 creador). Recalibrados defaults: ScoreParams.demand_full_gmv_eur 30000→10000, comp_zero_above 300→200; DiscoveryFilters max_influencers 250→200, min_gmv_eur 1000→300, min_score 40→25; discover(use_ranklist) default True→False. UI: keywords pre-rellenadas, ranklist off por defecto, sliders ES. Resultado real: 50 productos→13 pasan filtros, top crocs/creatina marcados 🚀 probable_boosted (GMV Max 56-57/100 con deep-ads EchoTik por vídeo). Botón "📦 Pack completo" añadido también a tab_products (flujo descubrimiento manual con panel web). Plan API EchoTik = Custom 100k req/año @ ~$1/1k (~$100/año), suelo holgado (uso ~11k/año); el Basic web $13.9/mes NO incluye API.
* [2026-06-16] Radar — filtros alineados con Kalodata del operador + orden por comisión + multi-país. Captura del operador: filtra creadores<250, ¿producto de anuncios?=Sí, artículos vendidos>5, comisión>19% (y la BAJA para ver más), tasa crecimiento>50%, fuente ingresos=Video. Mapeo a EchoTik: min_units_sold (total_sale_cnt), min_commission_pct (expuesto en UI; en ES comisiones 8-12% → default 0), require_video_driven via video_sales_ratio=1-live_sale_count/units (total_live_sale_cnt), min_growth_pct via _growth_pct(units_30d vs units_60d-units_30d). Selector "Ordenar por" (comisión/score/GMV/GMV Max/crecimiento) en el listado — el usuario prioriza mejor comisión aunque sea de otro país (vende desde España igual). Multi-país: EchoTik solo soporta US|ID|TH|PH|MY|VN|GB|MX|SG|SA|BR|ES|DE|FR|JP|IT (de su error 500); de TikTok Shop EU cubre ES/DE/FR/IT, NO los otros 8. LIMITACIÓN clave: muchos productos ES tienen last_crawl viejo (ej. crocs crawl marzo, hoy junio) → total_sale_7d/30d=0 → growth_pct sale -100% para casi todos → el filtro de crecimiento (default OFF) los dejaría sin candidatos; con plan EchoTik de pago el crawl es más fresco. video_sales_ratio en ES ~100% (apenas hay livestream). Deploy: push a main → webhook deploy_safe.sh (git pull --ff-only + systemctl restart tiktok-factory, el servicio Streamlit en /etc/systemd; el build docker api/web es aparte y no afecta al Radar). .env de prod actualizado con las creds API EchoTik del trial.
* [2026-06-16] Radar — aviso cuota EchoTik + carruseles mejorados (investigados) + plan N/día. (1) echotik_cloud rastrea cuota agotada: _mark_quota_error detecta "usage limit/increase quota" en respuestas 500/code!=0 y setea _LAST_QUOTA_ERROR (timestamp,msg); se limpia en la siguiente llamada con éxito; quota_exhausted()/last_quota_error_msg() los lee el banner del Radar (st.error "EchoTik sin cuota"). (2) carousel_director.md reescrito con best practices reales de TikTok Photo Mode 2026 (investigado): formato elegible (listicle=default/before_after/comparison/pov/problem_solution), slide1=hook scroll-stop<1s + swipe-bait, una idea/slide + swipe_cue direccional, estética NATIVA/UGC (no ad pulido), 9:16, consistencia entre slides, CTA cesto último; output JSON ahora con format+suggested_sound+swipe_cue por slide. Verificado real (Gemini): genera listicle "3 razones..." con ganchos+flechas correctos. (3) plan_week(per_day) → asignación secuencial día=idx//per_day+1 (2/día como el chaval); UI piensa en "productos por día × días"; effective_days=max(día) para WeekPlan/.md. GOTCHA cover: algunas echosell-images.tos-*.volces.com dan 403 (hotlink) → fallback DDG en build_pack ya lo cubre.
* [2026-06-16] Radar — visor in-app de prompts (no abrir Drive). Product.carousels: list[dict] nuevo campo (persiste los carruseles del carousel_director en Redis; antes solo iban a disco). creation_pack.build_pack ahora setea product.carousels=carousels antes de save (solo si len>0, no pisa). tab_radar._render_product_prompts(product): st.tabs [🎥 Vídeos (presets: voice_script/hooks/seedance_prompt/veo3_prompt/cta en st.code copiables) | 🎠 Carruseles (caption + slides con on_screen_text+swipe_cue+image_prompt)]. En el Calendario, cada producto del día tiene botón "🎬 Ver vídeos + carruseles" (toggle por session_state cal_show_<plan>_<slug>_<day>, carga el Product on-demand). Backfill: los productos creados antes del campo se rellenan leyendo _products/<slug>/prompt_templates/carousels/carousel_*.json → product.carousels. Así el flujo es 100% in-app: Calendario → día → producto → ves opciones de vídeo+carrusel con prompts copiables, estilo tab_generator.
* [2026-06-16] Radar PORTADO a la web Next.js (estaba solo en Streamlit, que NO está expuesto: 8501 localhost, Caddy enruta funnel→web:3000 Next.js). El motor (discovery_service/ads_signal/creation_pack) es Python compartido y se reusa tal cual. FASE 1 (descubrir): backend src/api/routers/tiktok_shop/radar.py (POST /scan multi-región+filtros+persist, GET /candidates?sort, POST /import, POST /clear, GET /regions) registrado en routers/{tiktok_shop/__init__,__init__}.py + main.py (import+include_router, 3 sitios). Response model = reusa src.tiktok_shop.models.discovery.DiscoveredProduct (pydantic) directamente. Auth Depends(get_current_user) X-API-Key. Frontend: lib/queries/radar.ts (react-query useRadarScan mutation/useRadarCandidates query/useRadarImport/useRadarClear/useRadarRegions) + app/tiktok-shop/radar/page.tsx (chips países, keywords textarea, filtros, scan, cards con comisión prominente+GMV Max badge+import, sort) + nav en components/layout/Sidebar.tsx (icono Radar de lucide). Verificado backend TestClient: /regions 8 países, /candidates 18 ordenados por comisión. GOTCHA frontend (sin node_modules local, no se puede typecheck): React.ReactNode necesita `import {type ReactNode}`, quitar imports sin usar (next build corre eslint y FALLA en errores), <img> necesita `// eslint-disable-next-line @next/next/no-img-element`. PENDIENTE Fase 2: calendario + pack/plan + visor de prompts en web (las generaciones largas necesitan ir por la cola de jobs, no sync HTTP).
* [2026-06-16] Radar Fase 2 en la web: separación origen + calendario + packs por cola de jobs. (1) Product.origin ("manual"/"radar") NO estaba en la API: añadido a src/api/schemas/product.py:ProductResponse (+ carousels) — _to_response usa model_validate(model_dump()) así que basta declarar el campo; frontend lib/types/product.ts +origin/carousels; products/page.tsx filtro chips Todos/Manuales/Radar (client-side por p.origin). (2) Cola: nuevos JobMode TIKTOK_SHOP_PACK/TIKTOK_SHOP_PLAN + runners run_tiktok_shop_pack (build_pack 1 producto, mapea logs→progreso por keywords) y run_tiktok_shop_plan (plan_week N/día) registrados en _RUNNERS+_MODE_TO_PROGRAM (src/queue/runners.py ~1716). build_pack/plan_week ya aceptan log_callback → integración directa. (3) radar.py endpoints Fase 2: POST /import-to-calendar (import_candidate + añade PlanEntry al WeekPlan actual del día + encola pack), POST /plan/generate (encola plan N/día), GET /plan (enriquece cada entry con presets_count/carousels_count/pack_ready LIVE del Product en Redis), POST /plan/tested, DELETE /plan. Usa Depends(get_queue) + queue.enqueue(mode,title,params,enqueued_by). (4) Frontend: lib/queries/radar.ts +useRadarPlan (refetchInterval 5s mientras !pack_ready)/useImportToCalendar/usePlanGenerate/useMarkTested/useDeletePlan; app/tiktok-shop/calendar/page.tsx (días, entries, checkbox probado, visor prompts con useProduct → presets+carruseles copiables); botón "Al calendario"+select día en RadarCard; nav Calendario. GOTCHA test: el router tiene Depends(get_current_user) con API_KEY seteada → TestClient necesita header X-API-Key (la web manda NEXT_PUBLIC_API_KEY). NO llamar /import-to-calendar en tests: encola trabajo REAL (Apify+Gemini).
* [2026-06-16] REGRESIÓN "no está fino" (buga_1 score 100 con fallos visibles): el editor INFRA-cortaba — dejaba dentro tos, falsos arranques ("y esto"/"así que aprovecha") y dead-air de cabecera/cola que antes quitaba. Root cause en `_preserve_speech_islands_in_cuts` (rescate de islas de voz): escaneaba TODOS los cortes por energía y rescataba cualquier isla ≥0.22s sobre el suelo de ruido, SIN saber la fuente del corte → resucitaba tos/arranques que la IA/auto_trim habían mandado cortar (en buga_1: 9 islas, la salida arrancaba en una islita dentro de la cabecera 0→22.7s y acababa en otra de la cola). El JUEZ daba 100 porque solo mide palabras-perdidas/coherencia/silencios, NO "basura que debió cortarse". Fix quirúrgico: pasar `protected_cuts` (= `_CONTENT_CUT_SOURCES` ∪ `auto_trim`) y NO rescatar islas que solapen un corte autoritativo; el rescate solo aplica dentro de cortes ACÚSTICOS (palabra real mal-alineada por Whisper, caso 'proteína'). +auto_trim al guard de `_extend_keeps_to_voiced_edges`. Verificado: test unitario determinista + el bug solo dispara con cabecera larga con ruido; prueba deductiva sobre el diag real (9 islas = los 6 fallos timestamped, todas dentro de cortes auto_trim/IA).
* [2026-06-16] Juez endurecido (fiabilidad del score): nuevo `_count_residue_islands` detecta "residuo sin voz" = tramos conservados ≥0.30s cuyo span NO solapa ninguna palabra Whisper voiced (dead-air/tos/chasquido que sobrevivió). El juez antes solo medía palabras-perdidas/coherencia/silencios → daba 100 a vídeos con basura visible. Ahora baja el score (5/isla + 5 si en cabecera/cola, que es lo más visible) y lo surfacea en verdict_detail (dim Limpieza "N hueco(s) sin voz" + overall "revisar"). Cableado en `_full_audit` tras `_post_render_audit` (antes del deep, así stackea). Alta precisión: un tramo con voz (aunque relleno) NO cuenta (eso lo cubren loose_words/coherencia). Verificado: test unitario determinista (borde/interior/con-voz/<0.30 ignorada) + 0 falsos positivos en vídeo limpio del corpus (n_residue=0; el 88 de ese run fue coherencia IA oscilando, no el residuo). Complementa el fix del rescate de islas: el cutter ya no las mete, y si alguna se cuela el score lo refleja.
* [2026-06-16] Radar web — ajustes UX tras feedback. (1) EchoTik NO tiene feed "top de todos": verificado que product/category, category/list, product/rank devuelven vacío y ranklist rank_field=2/rank_type=2 también data=0 → el descubrimiento es SOLO por keyword/categoría (/product/list). Mismo método que el operador (escanea nichos). Añadido botón "🌐 Nichos amplios" (BROAD_NICHES ~17 nichos de las categorías principales) para barrer mucho en un scan. (2) Filtros POST-scan client-side en el listado del Radar (viewOnlyBoosted = solo probable_boosted, viewMinComm = comisión≥%) sobre allCands cargados, sin re-buscar. (3) Covers rotas: muchas echosell-images.tos-*.volces.com dan 403 hotlink → componente Cover con onError→placeholder Package + referrerPolicy="no-referrer" (ayuda a pasar el check de referer). COSTE: un scan de nichos amplios = ~17 keywords×región + deep_ads → casi agota el trial de 100; avisado en la UI.
* [2026-06-16] Subtítulos ESCALONADOS (palabras de la misma línea a distinta altura, "escalera"): root cause en `src/subtitles.py:render_chunk_image` — cada palabra se dibujaba con `anchor="lt"` (left-TOP) en una `y` común → PIL alinea el TOPE del glifo, así palabras con alturas distintas (minúsculas vs mayúsculas/ascendentes) caen a líneas base distintas. Invisible en presets MAYÚSCULAS (Impact, glifos uniformes — Presidentes/Pronósticos) pero MUY visible en minúsculas/mixto (Editor Auto, Montserrat). Fix: anclar a la LÍNEA BASE con `anchor="ls"` a una base común `y_base = y_top_línea + font.getmetrics()[0]` (ascent del FONT, constante e independiente de las letras) — texto principal, sombra, glow y el bbox `actual` (píldora/box/underline). Verificado render: "invertir en uno de" y "estos Esto te va" quedan en una sola base. Renderer COMPARTIDO por los 3 programas → el fix es typográficamente correcto y no cambia los presets MAYÚSCULAS.
* [2026-06-16] Falso arranque NO cortado (seg 23 buga_1: "...android y esto empezó esto costaba 12 euros" — el cliente lo notó): no es n-grama (solo repite "esto", 1 palabra → `_detect_repeated_ngrams` min_n=2 no lo coge) sino un ARRANQUE ABANDONADO + reinicio sobre palabra pivote → trabajo de la pasada IA de falsos arranques (`silence_cutter_false_starts.md`, temp 0 + seed = DETERMINISTA). El prompt pedía 90% de certeza y lo saltaba por borderline. Fix: añadido Pattern 2b al prompt ("abandoned clause restarted on a repeated PIVOT word": conector+≤4 palabras colgadas seguidas de reinicio que reusa una palabra y dice algo nuevo → cortar el arranque) + lección en editor_lessons.md (Rectificaciones) con el caso real. Verificado aislado: Gemini Y OpenAI ahora devuelven `y esto empezó`=>`esto costaba 12 euros` (n_cuts=1, sin sobre-cortar). Consenso pass2 = UNIÓN (cualquier modelo que lo detecte, se corta). Palanca general para futuros falsos arranques: prompt + lección, no heurístico determinista (1-palabra-pivote es ambiguo y arriesga over-cut).
* [2026-06-16] Carruseles v2 (feedback operador, imágenes salían muy bien): carousel_director.md reescrito — (a) hook slide 1 = el más específico (es la thumbnail), (b) image_prompt SELF-CONTAINED que INSTRUYE renderizar el on_screen_text BAKED en la imagen (text_style consistente en todas: font+color+posición), el operador ya no pone texto a mano, (c) NADA de hashtags/logos/watermarks en las imágenes — hashtags SOLO en hook_caption, (d) campo text_style global + language. generate_carousel(language="es"|"en") + _norm_lang (es_*/en_*→es/en); _build_user_prompt fuerza OUTPUT LANGUAGE. Backend POST /radar/carousels/regenerate (product_id,language,n) síncrono (solo Gemini ~10-15s/carrusel) → regenera y guarda product.carousels. Frontend: selector idioma 🇪🇸/🇬🇧 + botón "Regenerar carruseles" en la pestaña Carruseles del visor del calendario (invalida productKeys.detail al terminar). Verificado EN: render-text-in-img=True + hashtag-in-prompt=False en todas las slides.
* [2026-06-16] AUTO-REVISOR de falsos arranques (editor que revisa su propio corte) — RESUELTO el "y esto empezó". Insight clave: la pasada 2 corre sobre el transcript BRUTO del input y se le escapan restarts borderline; pero la auditoría profunda YA re-transcribe el OUTPUT (out_words) y SOBRE ESE texto el detector SÍ los caza (gpt-4o y gpt-5.4, temp 0). Solución: barrido final tras el self-heal (`false_start_output_sweep`) que corre `_ai_false_starts_openai` sobre out_words, mapea el span a input con `_map_output_to_input`, re-corta y re-audita. GUARD correcto (el que costó): cortar un restart rephrase deja sus palabras como "missing" en el audit profundo (no recurren) → NO se puede exigir word_fallos igual; se acepta si (1) coherencia no empeora y (2) TODA palabra que ahora falta cae DENTRO del span barrido (intencionado); si falta algo FUERA = pérdida real → rechaza; al aceptar se limpia el needs_requeue/score espurio. Dead-ends descartados (no repetir): Gemini en pass2 (no aportó + rompe determinismo), y pasar el transcript "limpio" (_p2_words) a pass2 (tampoco lo cazó, el input que funciona es out_words del OUTPUT renderizado, no disponible en pass2). Verificado: buga_1 v7 cortó "y esto empezó", score 95, coherencia 100, needs_requeue False, sin perder contenido. GOTCHA test: encolar por `docker exec` (proceso aparte) NO lo ve el worker vivo (cola en memoria) → queda PENDING; hace falta restart del api para que recargue del disco. El flujo web normal (get_queue singleton) no tiene este problema.
* [2026-06-16] Pantalla blanca ("Application error: a client-side exception") al activar el check "variante números" en Presidentes (móvil y desktop): `useLocalStorageState` hacía `JSON.parse(raw)` y usaba el valor TAL CUAL, sin fusionar con el default. Un localStorage guardado por una versión anterior del config `numbers` (PresidentsNumbersConfig) no tenía los campos añadidos después (`list_x_position`, etc.) → quedaban `undefined` → `numbers.list_x_position.toFixed(2)` en PresidentsPreview/page reventaba el render. Fix general en useLocalStorageState: si parsed y default son objetos planos, `setValue({ ...default, ...parsed })` (rellena campos nuevos). Cubre TODOS los configs persistidos flat (subs, hook, numbers). Lección: cualquier estado persistido en localStorage/Redis con un schema que crece DEBE fusionarse sobre defaults al leer, o los clientes viejos crashean al añadir campos.
* [2026-06-17] Editor que REVISA y CORRIGE de verdad — fix de raíz de la INESTABILIDAD entre runs (mismo vídeo: 100 un run, 88 otro). El pipeline NO es determinista (Whisper re-transcribe cada run → la IA toma decisiones de corte distintas → a veces sobre-corta una frase: "...la cesta amarilla y date prisa"→"la cesta prisa"; "20 euros"→"20"). El juez de coherencia SÍ detectaba el over-cut (con `restore_span` localizado), PERO `_derive_self_heal_actions` solo generaba la restauración para `type=="promised_item_cut"` e IGNORABA `dangling_ref`/`number_altered` → el self-heal no tenía acción → break → se entregaba el over-cut (coh 88). Fix: restaurar CUALQUIER defecto `fixable` con `restore_span` (el defecto ya pasó el filtro estricto del juez: cita real + palabra de contenido concreta ausente del render + span localizado → es corte REAL; la puerta MONÓTONA del self-heal descarta una restauración que no baje coherence_fallos/suba score). PRUEBA EN VIVO: buga_2 este run sobre-cortó (88), el self-heal restauró 88→100 ACEPTADO (restores=1). buga_1=100, buga_2=100 sin regresión. El fix sube el SUELO de calidad aunque la IA varíe — la clave para fiarse a escala. Aparte: limpieza de palabras sueltas (`_detect_loose_words` ya las marcaba para el score → ahora también se DESCARTAN del keep tras voiced_edge_rescue; no-op si no hay sueltas) que arregló la regresión clienta_2 88→64 (2 sueltas "os"/"la"=-24). Y revertida la lección "cierre seco": transcribí el input de clienta_2 (235-263s) y dijo LITERAL "...hasta el día 7 y píllate la tuya" — NO hay frase puente, el editor fue fiel (perseguíamos una frase inexistente). GOTCHA deploy/cola: exec-enqueue + `docker restart` PIERDE los jobs nuevos (el shutdown graceful DUMPEA la cola en memoria del api SOBRE el queue_state.json que escribió el exec). Patrón fiable: exec-enqueue + `docker kill` (SIGKILL, sin dump) + `docker start` (carga del disco). El progreso de la cola es de grano grueso (0.01→salta al final): no confundir con cuelgue. El audit se escribe ANTES del render → no fiarse de `test -f diagnostic` para "terminado", mirar status=completed en la cola.
* [2026-06-17] buga_3 "proteína" se comía dejando 1s de silencio (cliente lo nota, recurrente). ROOT CAUSE (workflow multi-agente, 3 lentes + adversarial): Whisper INFLA el span de "proteína" a [8.28-11.12]=2.84s absorbiendo la pausa de ~2s que viene DESPUÉS; la IA marca [8.3-12.16] como `ai_noise_gap` (pausa adivinada) pero silero VAD ve voz real solo hasta 8.7 → el límite del noise_gap partió la cola hablada de proteína, dejando 0.12s en el keep (inaudible). Ninguna red la salvaba: `_protect_words_from_acoustic_cuts` NI LA ITERA (`_drop_words_inside_silences` la tira de voiced_words: su span inflado es casi todo silencio = "fantasma de amplitud"), y `_extend_keeps_to_voiced_edges` está BLOQUEADO por `_in_content_cut` (el commit 4f9af3a metió `ai_noise_gap` en sus content_cuts). Y los intervalos de VOZ de silero (`speech_intervals`, L490) se calculaban y SE TIRABAN (solo se invertían a silencios + diag). Fix: `_snap_keep_edges_to_silero_voice` — si un borde de keep cae DENTRO de un intervalo de voz silero Y está pegado a un `ai_noise_gap`, estira el borde hasta el FIN de esa voz silero. DISPARO ESTRECHO = regresión cero: SOLO junto a ai_noise_gap (la IA admite pausa adivinada, no borrado); vídeo sin noise_gaps → no-op. Salvaguardas: para ante `_CONTENT_CUT_SOURCES`, cap 0.6s, no pasa de media al keep vecino (anti merge-collapse), cap al fin de voz silero (jamás re-mete la pausa real, que cae FUERA de todo voiced span). Verificado: buga_3 keep2 6.28→8.7, score 92→97, proteína capturada (0.42s completos). DEAD-END descartado: un "word-guard" (exigir palabra Whisper en la extensión) ROMPE el fix — depende de los timings de Whisper que son justo lo no-fiable; cuando Whisper trunca proteína en el borde 8.40 no hay palabra "después" → no extiende (buga_3 flip-flop 97/92 entre runs). Y el residuo de 0.39s de buga_1 (que motivó el word-guard) resultó ser del PIPELINE BASE, no del snap (aparece con snap=null igual). Lección: para proteger voz, fiarse de silero VAD (entrenado para voz), NO de los spans de palabra de Whisper. Existe `_shrink_inflated_word_spans` (L4988) que debería encoger estos spans inflados pero no cazó proteína — posible mejora futura.
* [2026-06-17] buga_3 proteína — VERDADERO root cause (tras varias hipótesis falsas): la clienta dice "Proteína." CLARA y FUERTE (-22dB) en una ISLA de voz aislada [input 10.4-11.6], precedida y seguida de pausas largas. La IA marcó TODO [8.3-12.16] como UNA pausa (`ai_noise_gap`) y se tragó la isla → hueco en el output (seg 6: "dos ingredientes [silencio] y crema de arroz"). El rescate de islas normal (`_preserve_speech_islands_in_cuts`) SE SALTA los ai_noise_gap (los metió `protected_cuts` en 4f9af3a, para no resucitar tos/falsos-inicios) → la isla de proteína no se rescataba. Fix: `_rescue_silero_islands_in_noise_gaps` — devuelve al keep las islas de voz silero que (1) silero marca voz, (2) NO están ya en keep, (3) caen dentro de un ai_noise_gap, (4) NO tocan content cut, (5) ≥0.30s, (6) energía ≥16dB sobre el suelo de silencio (descarta tos/blips; proteína está +33 sobre suelo). Verificado: salida pasa de "ingredientes [hueco] y crema" → "ingredientes PROTEÍNA y crema de arroz". LECCIONES META: (a) la ASR es CAÓTICA en este audio — large-v3 colocó "proteína" en 8.28 (inflada), 11.62 (¡en silencio!) y small en 10.74, en runs distintos; NO fiarse de los timings de Whisper para localizar contenido, usar silero (VAD) + energía. (b) Persegui DOS sitios equivocados antes (un mumble flojo @8.1 que NO era la proteína buena, sino un falso-inicio quizá) — el cliente lo clavó: "coge los segundos equivocados". (c) El boost de palabras mumbleadas (ASR-independiente, energía+silero) y el silero edge-snap son features generales útiles pero NO eran el fix de buga_3; el fix fue el rescate de la ISLA clara. Pendiente menor: el falso-inicio flojo @8.1 sigue conservado+boosteado justo antes de la proteína buena (posible leve "stutter"); evaluar si suprimirlo según feedback.
* [2026-06-17] Radar/carruseles — tips del admin incorporados. (1) NO personas/caras IA (salen falsas, rinden peor); sí POV/manos/pies/explicativo. carousel_director.md: regla "no human faces, no full person; hands/feet/POV only" + human_presence_note. preset_generator.py: _NO_ONSCREEN_TEXT_DIRECTIVE ahora incluye "no AI people/faces, hands/feet POV only" → afecta seedance_prompt/veo3_prompt de TODOS los presets. (2) Caros / sin muestra gratis = menos competencia: no hay campo free-sample en EchoTik → uso PRECIO como proxy. radar.py _SORT_KEYS["price"]=max_price||min_price; RadarCandidate +min_price/max_price; card muestra 💸€precio; sort "💸 Precio alto" + tip en la intro. (3) Hasta 20 productos/día sin repetir: calendar perDay max 5→20 (no-repeat ya garantizado por plan_week only_non_imported + imported flag; plan_week lo cap por tamaño del pool de candidatos). BLOQUEO vigente: Gemini en tope de gasto mensual (key PAID "exceeded its monthly spending cap" + FREE diaria agotada) → regenerar carruseles/packs falla hasta reset (free diario / paid mensual) o subir el cap en ai.studio/spend. Código desplegado y listo para cuando haya cuota.
* [2026-06-17] Plantillas de vídeo reutilizables (volumen, sin coste IA) — idea del admin: "2 prompts para casi cualquier producto + algunos por tipo". src/tiktok_shop/video_templates.py: VIDEO_TEMPLATES (2 universales POV/demo + 6 por nicho: skincare/fitness/hogar/moda/tech/comida), cada uno prompt Veo3 8s 9:16 con {product} + _RULES comunes (foto adjunta como ref exacta, SIN caras/personas IA solo POV/manos, UGC nativo, sin texto). render_template({product}→nombre) + templates_for_niche (universales + match por tags). NO usa Gemini (texto estático) → gratis, para testear 20/día. Endpoint GET /radar/video-templates?product_id= (rellena nombre + filtra por categoría/subcategoría/nombre del producto). Frontend: 3ª pestaña "⚡ Plantillas" en el visor del calendario (useVideoTemplates) con prompts copiables. Estrategia híbrida: plantillas rápidas para volumen + pack IA (research+presets) solo para ganadores que demuestren ventas.
* [2026-06-17] Fallback OpenAI cuando Gemini sin cuota (tiktok_shop/api/gemini.py). generate_text ahora envuelve _generate_text_gemini (la lógica anterior renombrada) y, si Gemini falla por cuota (_is_quota_error) o no hay keys (EnvironmentError), cae a _call_openai_fallback (OpenAI Chat, gpt-4o-mini default, override TIKTOK_SHOP_OPENAI_FALLBACK_MODEL). Soporta texto + imágenes (data URI base64 en content image_url); NO vídeo ni web_search (research sigue Gemini-only → propaga). expect_json → response_format json_object + _strip_json_fences. Cost tracking via record_openai_chat. Afecta carruseles/presets/análisis. OJO: el gate is_configured() (solo mira keys Gemini) en carousel_director/analyzer devuelve mock si NO hay keys Gemini → el fallback OpenAI solo entra cuando HAY keys Gemini pero están agotadas (que es el caso real: free diaria + paid cap). Las ⚡ Plantillas NO usan IA (texto estático) → siempre gratis. Verificado: _call_openai_fallback devuelve JSON válido con OPENAI_API_KEY.
* [2026-06-17] Hooks BOFU (consejo admin: contenido parte baja del embudo, texto subliminal = nombre del producto + empuje a la venta, sin complicarse). prompts/hooks_bofu.md (textos ≤6 palabras, mezcla de tipos nombre/beneficio/urgencia/cta/prueba_social, ≥2 que son solo el nombre). hooks_generator.generate_bofu_hooks(product, n, language) usa gemini.generate_text (→ fallback OpenAI si Gemini sin cuota). Endpoint POST /radar/hooks/bofu (product_id, language, n). Frontend: pestaña "🎣 Hooks BOFU" en el visor del calendario, botón "Generar hooks BOFU" + lista con tipo + copiar. Ya existía infra de hooks (generate_hook_variants/generate_themed_hooks + favorite_hooks + router hooks.py) pero faltaba el ángulo BOFU simple. Verificado: genera "Crocs Classic Ballet 🤍", "Añade al carrito ya", "+vendidas", etc.
* [2026-06-17] Hooks BOFU persistidos en el producto (que estén "a mano" en el calendario sin pulsar generar). Product.bofu_hooks: list[dict] {text,type}; ProductResponse + frontend Product type lo exponen. build_pack genera+guarda bofu_hooks (paso 5b, +1 call Gemini/OpenAI). Endpoint /radar/hooks/bofu ahora también SAVE a product.bofu_hooks. Calendar viewer pestaña 🎣 Hooks: muestra product.bofu_hooks por defecto (IIFE: persisted = product.bofu_hooks, hooksToShow = local||persisted), botón "Regenerar" invalida productKeys.detail. Backfill de los 4 productos del calendario (10 hooks c/u). Así todo el kit del producto (vídeos IA / carruseles / plantillas / hooks BOFU / fotos / ficha) está en una sola pantalla del calendario.
* [2026-06-20] Flujo ligero del calendario (decisión del operador: solo lo rápido por defecto). Al añadir producto manual de Kalodata: SOLO plantillas universales (estáticas, gratis) + hooks BOFU; NO vídeos IA ni carruseles hasta que venda. PackOptions(generate_video_presets=False, n_carousels=0, research=False) = fotos+análisis+bofu_hooks (2 calls LLM). radar.py /plan: PlanEntryOut +hooks_count +ai_ready; pack_ready ahora = presets||carousels||hooks>0 (hooks ya cuenta → no spinner falso ni polling infinito); ai_ready = presets||carousels>0. Calendar DayEntry: expander SIEMPRE visible ("Ver prompts", pestaña default ⚡ Plantillas), status muestra ⚡plantillas·🎣hooks (+🎥/🎠 solo si ai_ready), botón "✨ Generar vídeos IA + carruseles" (usePlanPack→/plan/pack full) visible cuando !ai_ready, con polling local (invalidate radar-plan c/6s) hasta ai_ready. Nuevo endpoint POST /radar/plan/pack (re-empaqueta producto YA en plan, sin candidato DiscoveryRepo) + hook usePlanPack. Estrategia: volumen barato primero, IA solo para ganadores.
* [2026-06-23] Añadir producto a la cola del calendario por URL de TikTok Shop (flujo Kalodata manual autónomo). Reutiliza utils/url_scraper.scrape_product_url (ya existía para crear-producto): saca name+image_url+price de la SHARE-URL (vm.tiktok.com / la que lleva og_info en query). OJO: la URL canónica www.tiktok.com/view/product/<id> está BLOQUEADA (TikTok responde "Security Check") tanto con urllib como con el scraper → hay que usar la share-URL del botón Compartir de la app. Endpoint POST /radar/plan/add-url (radar.py): scrape (use_gemini=False, rápido) → crea Product (origin manual, tiktok_shop.product_url=url, descarga foto vía photo_downloader.download_image_to_product) → coloca en cola (append día=10000 + _reflow_plan) → encola pack LIGERO (_light_pack_options: analyze+hooks BOFU, SIN presets/carruseles). Cola/reflow: _reflow_plan(plan, per_day=10) ordena entries por día (stable) y reparte en bloques de per_day → al borrar productos de un día los posteriores SUBEN (reflow también en /plan/remove). Kling i2v (Magnific Premium+): video_templates.py +first_frame (Nano Banana, foto del 1er frame con foto producto adjunta) + motion → render_first_frame/render_kling; endpoint /video-templates devuelve first_frame_prompt+kling_prompt; pestaña ⚡Plantillas del calendario muestra 🍌1er frame + 🎬Kling + 🟣Veo3. Frontend: tarjeta "➕ Añadir producto por URL" arriba del calendario (useAddUrl). Si scrape no saca nombre (Security Check) el endpoint devuelve ok=false pidiendo share-URL o nombre manual.
* [2026-06-24] BUG (pantalla blanca "Application error: client-side exception" al abrir la Cola o encolar CUALQUIER cosa). Causa: el backend tenía JobMode.TIKTOK_SHOP_PACK/TIKTOK_SHOP_PLAN pero el frontend NO los conocía. Cuando un job de esos entra en la cola, JobCard hace `PROGRAM_ICON[MODE_TO_PROGRAM[mode]]` y `MODE_ICON[mode]` → undefined → `<undefined/>` → React revienta TODO el drawer (un job malo tumba la lista entera → error boundary raíz → pantalla blanca), por eso fallaba hasta encolando Creator Reward. Síntoma en logs web: "Failed to find Server Action x" (despista, es ruido de bundle, NO la causa). Fix: añadir los modes nuevos a frontend/lib/types/queue.ts (JobMode) + los 3 Record<JobMode> de queue-meta.ts (MODE_TO_PROGRAM, SUBMODULE_LABEL, MODE_ICON) + opcional describeJobParams. Y fallback defensivo en JobCard.tsx (`MODE_TO_PROGRAM[mode] ?? "tiktok_shop"`, `MODE_ICON[mode] ?? ShoppingBag`) para que un mode futuro nunca vuelva a tumbar la UI. REGLA: al añadir un JobMode en src/queue/models.py, añadirlo SIEMPRE a queue-meta.ts + types/queue.ts (Record<JobMode> exige todas las claves en TS pero en runtime un job con mode no mapeado da icono undefined = crash).
* [2026-07-01] Remote Control del chat web ("A la app" en el menú Claude) tiene 1 SOLO slot: inyecta Remote Control a UNA sesión del Agent SDK (uvicorn app:app en 172.18.0.1:8765, dir ~/.claude-sdk-test, FUERA de Docker, creds Max del host). Si una sesión queda colgada ocupando el slot, el SDK entra en crash-loop resumiéndola (--resume <id> respawn c/~25s) y "A la app" ya no activa remoto en otro chat (síntoma: "le doy a remoto y no veo nada"). El estado remoto es EN MEMORIA del uvicorn → un restart/deploy lo pierde. FIX: (1) frontend/app/claude/page.tsx — botón SmartphoneOff (📱✕) por chat con remote=true que llama stopRemote, + doRemote auto-libera (si startRemote devuelve remote:false hace stopRemote de las otras remote=true y reintenta 1 vez). (2) deploy/fix_remote.sh — rescate: sin args=free graceful (POST /claude-chat/remote/stop), --restart=kill+relaunch|systemctl, --status. (3) Botón en Settings→Deploy: el SDK NO es container docker → /admin/docker/* no lo toca; añadido /admin/claude-sdk/{status,free,restart} a webhook_listener.py (corre como nebulabsai, mismo user del uvicorn → kill sin sudo) → proxy /api/v1/deploy/claude-sdk/* en src/api/routers/deploy.py → hooks useClaudeSdkFree/Restart en deploy.ts → AdvancedAction en DeployPanel. GOTCHA sesión headless: cuando operas DESDE el propio chat web corres dentro del Agent SDK con permisos solo-edición (Read/Edit/Write + bash de lectura OK, pero kill/curl/systemctl/git commit/push dan "requires approval" sin diálogo) → no puedes arreglarlo desde ahí; hace falta 1 acción host (consola Hetzner/SSH) o 1 push a origin (deploy vía webhook).
* [2026-06-24] Vídeos que ATACAN EL PROBLEMA (MOFU/TOFU) — subir nivel vs hooks BOFU que no tiran. prompts/problem_video_director.md (análisis cliente ideal: quién/dolor/deseo profundo/frustración/vergüenza + venta: emoción/promesa/argumentos/objeciones → 2-3 conceptos de vídeo, cada uno veo3_prompt 10s EN + on_screen_text[] en idioma salida + angle/emotion + caption sin hashtags). services/problem_video_generator.generate_problem_videos(product,n,language) (gemini.generate_text→fallback OpenAI, reusa hooks_generator._product_context_block). Product +problem_videos:list[dict] +problem_analysis:dict; ProductResponse los expone (model_validate del dump). Endpoint POST /radar/videos/problem (on-demand, persiste). build_pack: PackOptions +generate_bofu_hooks(default True, ahora gateado) +generate_problem_videos(default False)+n_problem_videos; pasos 5b/5c. add-url: nuevo param gens:list ["problem_videos"|"bofu_hooks"|"styles"] (default solo problem_videos) → _gen_options(gens) construye PackOptions; add-url ya NO usa _light_pack_options. NO se puede scrapear la página TikTok (Security Check) → el análisis sale del NOMBRE + analyze IA (nombres Kalodata muy descriptivos = suficiente). Frontend calendario: selector de tipos de generación (chips) en "Añadir por URL" + pestaña "🎯 Vídeos problema" (default) con Veo3 prompt + textos en pantalla + caption + botón generar/regenerar; PlanEntryOut +problem_videos_count (cuenta para pack_ready). REGLA operador: activar SOLO vídeos-problema ahora para testear.
* [2026-06-24] Añadir productos en LOTE + desacoplar generación del alta (petición operador). radar.py: extraído _scrape_create_product(url,name,cat,lang)→(product,err) + _queue_product(product,per_day)→día + _maybe_enqueue_gens(queue,product,gens,op). add-url ahora gens default VACÍO = NO genera al añadir (el operador elige después por producto). Nuevo POST /plan/add-batch (raw multilínea, un "nombre — url" por línea; _parse_batch_line regex http\S+ = url, resto = nombre) → crea+encola cada uno en orden, reflow, devuelve results[] con ok/día/error por línea. Optimización: si name dado y url canónica (tiktok.com/view/product, bloqueada), se SALTA el scrape HTTP (no da foto igual) → add casi instantáneo. Frontend calendario: la tarjeta pasa de input url+nombre a TEXTAREA batch (useAddBatch); chips "Generar ahora" default NINGUNO; onSuccess deja en el textarea solo las líneas que fallaron con su motivo. Generación se dispara luego desde cada producto (pestaña 🎯 Vídeos problema botón / hooks / estilos).
* [2026-07-09] Vídeo LISTO para subir (quita-marca + textos quemados) sin editar a mano. El operador genera en Flow/Kling y sube el mp4 por CONCEPTO. pipeline/ready_video.py:process_ready_video(input,output,hook_text,cta_text,zoom): ZOOM (cover-fit 1080x1920 → resize(zoom) → crop centrado) saca la marca de agua del borde fuera de cuadro (lo que hacía a mano; mejor que el inpaint viejo que iba mal) + quema gancho (arriba y=16%) + CTA (abajo y=82%) con PIL (Montserrat-ExtraBold blanco borde negro grueso) sobre MoviePy CompositeVideoClip. Emojis se quitan del texto quemado (no rasterizan). Fuentes en assets/fonts (font_resolver._bundled_fonts_dir). Endpoints radar.py: POST /videos/problem/upload (multipart file+product_id+concept_index+zoom, SYNC def→threadpool, procesa y guarda en _products/<slug>/videos_ready/concept_<i>.mp4, marca concept["ready_video"]) + GET /videos/problem/ready (FileResponse). Emparejamiento vídeo↔gancho = subes al hueco de ESE concepto → coge su hook_text/cta_text guardado. Frontend calendario pestaña 🎯: por concepto botón 📤 subir → procesa → ⬇️ descargar listo; slider de zoom (1.0-1.4, default 1.12) para calibrar si la marca aún asoma. Sin coste (ffmpeg/MoviePy local). Falta calibrar zoom con vídeo real de Flow.
* [2026-07-09] Subir vídeo → procesado va a la COLA (async) en vez de síncrono. Antes el endpoint /videos/problem/upload procesaba con MoviePy dentro del request (bloqueante + si el vídeo del operador fallaba, "Error procesando" genérico sin traza). Ahora: guarda el original y ENCOLA JobMode.TIKTOK_SHOP_READY_VIDEO (runners.run_tiktok_shop_ready_video → process_ready_video, guarda videos_ready/concept_<i>.mp4, marca concept["ready_video"], borra el raw SOLO si va bien → si falla el raw queda para depurar + el error se ve en la Cola). Nuevo JobMode requiere: models.py (enum + MODE_LABELS) + runners.py (_RUNNERS + _MODE_TO_PROGRAM) + frontend types/queue.ts (JobMode) + queue-meta.ts (MODE_TO_PROGRAM/SUBMODULE_LABEL/MODE_ICON, icono Film) — si no, la Cola crashea (ver bug [2026-06-24]). Frontend: uploadVideo ya no espera el resultado; on ok setProcessingIdx(i) + useEffect que invalida productKeys.detail cada 5s hasta que aparezca ready_video. IMPORTANTE (infra): el deploy del web falla y NO actualiza si hay UN error TS (next build) — el webhook además NO encola (si hay deploy en curso, sale). Typecheck local antes de push: docker run node:20-alpine node_modules/.bin/tsc --noEmit. Y vigilar disco (builds petan a <2GB → docker builder prune -f).
* [2026-07-10] ready_video: 2 fixes. (1) La flecha .mov del Editor (leída con MoviePy has_mask desde el drive de red gdrive) fallaba INTERMITENTEMENTE ("failed to read the first frame") y tumbaba todo el render → ahora la flecha se DIBUJA con PIL (_draw_arrow_png, flecha abajo gruesa, color por luminancia del fondo blanco/negro) — sin I/O del drive, siempre funciona. + fallback: si el render con overlays peta, reintenta solo core+textos. (2) UX subida: uploadVideo pasa de fetch a XHR con xhr.upload.onprogress → muestra "Subiendo X%…" (el operador no sabía cuánto quedaba). Nota: no se puede "encolar antes de subir" (el server necesita el fichero); el % es la solución. Recordatorio auth: /videos/problem/upload usa header X-API-Key (get_current_user); la descarga /ready acepta ?api_key= query (va en <a href>). Verificado con el vídeo real del operador: 1080x1920 10s OK.
* [2026-07-10] ready_video feedback del operador (3 fixes). (1) Marca de agua NO se quitaba: Flow/Veo pone su ✦ abajo-DERECHA y la detección no lo pilla (destello sutil, bajo edge-energy) → default_corner="br" cuando detect_watermark_corner devuelve None → recorte dirigido (zoom~1.22) que saca la esquina br. Verificado: bloque brillante 244→58. (2) Flecha estática: la PIL era ImageClip fijo → ahora bob animado con set_position(lambda t: (x0, y0+amp*sin(t*2π*1.2))). (3) CTA "Míralo aquí" separado de la flecha → van JUNTOS abajo-izq (texto _CTA_CX/_CY=0.26/0.72 encima, flecha _ARROW_CX/_CY=0.26/0.82 debajo); _text_clip +x_center_pct para posicionar a la izq (no siempre center). OJO: el runner borra el raw al terminar bien → para depurar hay que re-subir. La detección de marca sigue floja (nunca detecta) pero el fallback br cubre Flow.
* [2026-07-11] ready_video: variedad de estilo entre las 3 versiones (operador: "el texto es siempre el mismo, algo de variedad"). _TEXT_STYLES = lista de {font, fill, stroke} (Montserrat-ExtraBold blanco/negro · Anton amarillo/negro · LuckiestGuy blanco/azul · Rubik-Bold blanco/rosa); _style(idx) rota por índice. process_ready_video +param style:int (=concept_index desde el runner); _render_text_png/_text_clip parametrizados con style. Emojis en color: NO se pueden aún (no hay fuente Noto Color Emoji en el server + PIL no hace fallback per-glyph con color fonts) → pendiente bundlear NotoColorEmoji y render especial. Fuentes disponibles en assets/fonts: Montserrat/Anton/Rubik/Fredoka/LuckiestGuy/Bangers/PermanentMarker/Caveat.
* [2026-07-14] ready_video: estilo "edición simple que vende" (ref: vídeos que vendieron — 1 texto + 1 flecha roja sola en zona media apuntando al carrito). Cambios: UN solo texto arriba (y=0.26, ya no hook+CTA), flecha SOLA (quitado el CTA burn) ROJA (228,30,30) con borde blanco en zona media-izq (_ARROW_CX/CY=0.22/0.60) apuntando al carrito naranja, animada (bob). 3 tipos de flecha (_ARROW_KINDS=[down,double,fat]) que rotan por índice de concepto (_arrow_kind). Combinado con _TEXT_STYLES (fuente+color por versión) → cada una de las 3 versiones = fuente+color+flecha distintos. PENDIENTE: emojis en color (falta bundlear Noto Color Emoji + render per-glyph). Verificado con 3 frames render.
* [2026-07-14] ready_video: modo de texto por versión (single vs sequence). Ref perro = SECUENCIA: gancho SIN flecha (fase1, 0-45%) → cambia a CTA + aparece flecha (fase2, 45-100%). Ref chica = SINGLE: 1 texto fijo + flecha. _TEXT_MODES rota por índice ["single","sequence",...]; _text_mode(idx). En process_ready_video: sequence → hook _text_clip(duration=split) + cta.set_start(split) + arrow.set_start(split); single → hook+arrow duration completa. split=dur*0.45. Verificado: en sequence a t<split hay 0 px rojos (sin flecha), a t>split aparece. Guía: sequence para productos historia/POV, single para valor directo/precio. Combinado con font+color+arrow-kind por versión → 3 versiones bien distintas para testear.
* [2026-07-14] ready_video: EMOJIS en color en el texto quemado. Bundleado assets/fonts/NotoColorEmoji.ttf (10.7MB, CBDT bitmap). Render: _emoji_font() lo carga a 109px (única strike CBDT) con lazy+guard (si falta → None → se quitan emojis, no peta). _split_runs parte cada línea en tramos text/emoji; _emoji_run_img renderiza el tramo con embedded_color=True → crop bbox → resize a ~0.92*font_size; _render_text_png compone texto (fuente estilo) + emojis inline con wrap que mide ambos. use_emoji por versión: la v0 va LIMPIA sin emoji, el resto con emoji (variedad). Prompt problem_video_director.md: hook puede terminar con 1 emoji que encaje. Los cta_text ya traían 👇🛒. Verificado visualmente: "No puedo creer este precio 🥰💕" con emojis a color alineados. OJO deploy: hoy un deploy se quedó a medias por colisión de flock (2 pushes seguidos) → api 3 días sin reiniciar; re-disparé con commit vacío. Evitar 2 pushes muy seguidos.
* [2026-07-14] problem_video_director.md: reglas de REALISMO para evitar el look IA (operador: fondo desenfocado delata IA). Añadido al veo3_prompt: fondo ENFOCADO/nítido (deep focus, no bokeh, no shallow DoF — un móvil real tiene todo en foco), entorno real con vida, piel con textura/poros (no cara plastificada), luz natural, encuadre móvil imperfecto; SIEMPRE incluir "sharp deep focus with the entire background in focus, no bokeh, no shallow depth of field, realistic natural skin texture with pores, authentic handheld phone-camera look". Regla extra para ROPA/moda: prenda bien visible, proporciones naturales, sin fondo borroso de estudio. Verificado: los veo3_prompt generados incluyen deep focus + no bokeh + skin.
* [2026-07-14] ready_video feedback: (1) flecha demasiado arriba → _ARROW_CY 0.60→0.72 (ocupa Y 68-76%, más cerca del carrito bottom). (2) nombres de salida concept_N.mp4 generaban duplicados → runner nombra {nombre_producto_corto}_v{N}_{YYYYMMDD_HHMM}.mp4, guarda concept["ready_video"]+["ready_at"]; el endpoint /videos/problem/ready y su FileResponse filename usan concept["ready_video"] (fallback concept_N.mp4 para compat). El JobVideoDialog de la Cola muestra basename(out_path) → ahora nombre único.
* [2026-07-14] ready_video quita-marca cambiado al método del operador: antes recorte dirigido a la esquina "br" (cortaba abajo + DERECHA → se comía al sujeto/móvil de la derecha, ej. v2). Ahora: zoom mínimo (slider) + recorte PEGADO ARRIBA (y_center=TARGET_H/2 → todo el sobrante zh-1920 se corta ABAJO, donde Flow/Veo pone la marca) + x centrado (laterales simétricos, mínima pérdida). Verificado: marca abajo eliminada (0), contenido derecha conservado. detect_watermark_corner/_crop_params quedan sin usar (dead code inofensivo).
* [2026-07-14] Formato "Persona luciendo el producto" (técnica i2v del operador, para ropa/moda/accesorios). Flujo 2 pasos: Paso 1 imagen (Gemini/Nano Banana + foto producto → genera persona GENERADA que encaja con el producto, luciéndolo, mirror-selfie, sharp focus/no bokeh, sin marca) → Paso 2 animar el still (Veo Frames/Kling i2v, SILENCIOSO: no talking/lip movement). Prompt problem_video_director.md: +formato 6, +campos image_prompt/animate_prompt (vacíos en otros formatos). Generador: parsea image_prompt/animate_prompt; el filtro ya no descarta concepto sin veo3_prompt si tiene image_prompt. Frontend calendario: si hay image_prompt muestra "🖼️ Paso 1 imagen" + "🎬 Paso 2 animar" en vez del Veo3. ProblemVideo type +image_prompt/animate_prompt. Verificado: para camisa V1 sale formato persona 2-pasos, V2/V3 otros. TAMBIÉN: marca de agua de Flow está a ~82% ancho, ~85% alto (no pegada al borde) → zoom por defecto 1.12→1.18 (frontend slider + backend upload/runner) para que el corte inferior llegue a ella.
* [2026-07-14] Calendario: (1) lentitud al cargar/borrar = get_plan hacía N gets a Redis (uno por producto). Añadido ProductRepo.get_many(ids) (mget_json, 1 roundtrip); get_plan lo usa → carga rápida. El borrado se sentía lento por el refetch de /plan (ahora rápido). (2) Borrado en lote: endpoint POST /plan/remove-batch (product_ids y/o day → quita todos + 1 reflow + 1 save). Frontend: botón "Vaciar día" en la cabecera del día + checkbox por producto + barra "Borrar N seleccionados" (useRemoveBatch). DayEntry recibe selected/onToggle.
* [2026-07-14] Radar — la señal de GMV Max se puede DEJAR DE INFERIR: EchoTik v3 expone la etiqueta AD real. (1) HALLAZGO: `opendocs.echotik.live` (con "s"; `opendoc.` sin "s" está muerto, da 402) documenta v3 (`open.echotik.live/api/v3`, mismo Basic Auth, paths con prefijo `echotik/`) y `echotik/video/list` acepta `is_ad` COMO FILTRO Y COMO CAMPO: «是否投流视频，1=投流视频，0=非投流视频» — 投流 = "inyección de tráfico pagado" = literalmente la inyección de ADS que persigue la estrategia. Acepta también `product_id` → 1 request por producto da etiqueta AD + ventas por vídeo. (2) POR QUÉ ES FIABLE EN ES: ads.tiktok.com/help/article/about-product-gmv-max dice literal «Affiliate videos that feature products included in your GMV Max campaign will receive a commercial content label» y «**In the EU and UK**, if organic content is part of a GMV Max campaign, a commercial content label will be applied **automatically**» → España=UE → ratio de vídeos is_ad=1 sobre el total = huella pública de GMV Max = la misma cuenta que el operador hace a mano con las etiquetas lilas de Kalodata. Mata la dependencia de Apify (su `isAd` de clockworks/tiktok-scraper es ~siempre False por construcción: al scrapear por hashtag/perfil recibes el ítem ORGÁNICO, no la impresión servida como anuncio). (3) BUG GORDO descubierto auditando: `ads_signal.ads_injection_signal` renormaliza el blend sobre las señales presentes (`wsum`), y como `total_video_sale_cnt`=0 siempre (has_sales=False) y sin Apify no hay labels (has_labels=False), el likelihood sale SOLO de la base débil (paid_view_share .25 / low_eng .15 / conc .10) → escupe un número igualmente y marca probable_boosted por encima de 50 SIN evidencia de ADS. Los "GMV Max 56-57/100" de crocs/creatina (learnings:192) NO medían inyección. (4) Añadido `echotik_cloud.get_product_ad_videos()` (v3, sin filtro is_ad a propósito: necesitamos numerador Y denominador en 1 request) + `_to_ad_flag` (None="no sé" ≠ False) + `_get(base=...)`; `scripts/verify_echotik_is_ad.py` zanja en 1 comando: ¿cuota? ¿v3 en ES? ¿is_ad poblado? ¿ventas por vídeo? SIN VERIFICAR: trial de 100 req agotado (probado hoy: v2 Y v3 dan `{"code":500,"message":"Usage Limit Exceeded, Please Contact Administrator to Increase Quota"}`; el auth SÍ es válido → v3 nos acepta). (5) PRECIO: los planes web (Basic $9.9/mo anual … Enterprise $29.1) NO incluyen API; la API es presupuesto a medida (massif@echotik.live) — el que ya nos dieron: ~100$/año/100k req ≈ 8€/mes, y un scan diario gasta ~60 req/día (~22k/año) → sobra. (6) DESCARTADO tras investigar: Commercial Content API de TikTok (gratis, DSA, cubre ES, pero NUNCA nombra el producto, sin spend/impresiones, y su licencia acota el uso a investigación de riesgos sistémicos); API oficial de afiliados (`EU5 | Not open`, ES incluida, + gating de empresa con GMV>$12.5k/mes); Creative Center (endpoint `creative_radar_api` firmado → 40101, y no baja a nivel producto); FastMoss/Shoplus/Pipiads/Tabcut (API solo Enterprise). RIESGO VIVO: frescura ES — EchoTik es T+1 y admite que productos de poca actividad se crawlean menos (justo el long-tail de "pocos creadores"); ya mordió con growth_pct=-100% (learnings:193). Por eso: créditos de prueba ANTES de pagar el año.
* [2026-07-15] Radar — VERIFICADO EN VIVO con creds nuevas (100 req trial, gastadas ~18). Veredicto: EchoTik v3 SIRVE, pagar el plan. (1) `is_ad` VIENE POBLADO EN ES: 10/10 vídeos con el campo, 3 marcados (30%) en un producto de creatina; discrimina de verdad (orgánicos→0, inyectados→1). (2) FRESCURA OK — el "crawl viejo" de learnings:193 era un ARTEFACTO DEL ORDEN POR DEFECTO, no del dato: `echotik/video/list` sin sort devuelve vídeos de 406-1254 días, pero con `video_sort_field=2` (create_time) + `sort_type=1` (desc) el más nuevo es de **1.1 días**. Valores válidos (doc): video_sort_field 1=total_digg_cnt 2=create_time 3=total_views_cnt; sort_type 0=asc 1=desc (sort_type=2 → 500 "must be less than or equal to 1"). `sales_flag=1` = vídeo que vende (filtra ruido); `created_by_ai` true/false. (3) DESCUBRIMIENTO INVERTIDO (mejor que el flujo Kalodata): `echotik/video/list?region=ES&is_ad=1&sales_flag=1&video_sort_field=2&sort_type=1` → vídeos con inyección FRESCA; cada uno trae `video_products` con su product_id → de la inyección al producto, sin depender de los contadores 7d/30d. (4) `echotik/product/detail?product_ids=a,b,...` acepta **10 IDs en 1 request** (sin param region) y da los 4 ejes de la estrategia POBLADOS en ES (6/6): total_ifl_cnt (max 488) + **total_ifl_video_7d_cnt** (creadores activos 7d, max 27) + total_video_cnt/total_video_7d_cnt + total_sale_cnt + **total_views_7d_cnt** (max 132.400) + product_commission_rate + min_price + sales_trend_flag. Hit rate ~60% (6 de 10 IDs). (5) MUERTO: `total_sale_7d_cnt`/`total_sale_30d_cnt` = 0/6 → el crecimiento se mide con **total_views_7d_cnt**, NO con ventas por ventana (mata el `_growth_pct` de units_30d vs 60d). `total_video_sale_cnt`=0 sigue (ventas por vídeo no existen). (6) `is_ad` NO se puede auditar contra TikTok: `realtime/video/detail` SÍ devuelve el aweme real, pero sus únicos campos comerciales son de música (added_sound_music_info.*) y autor, IDÉNTICOS en is_ad=1 y is_ad=0; branded_content_accounts/commerce_config_data=None → TikTok no publica la etiqueta de anuncio en el JSON público. is_ad es medición propia de EchoTik (投流). NO es dealbreaker: la etiqueta lila de Kalodata también es inferida (su FAQ admite variaciones por usar modelos IA) — misma clase de señal, pero automatizable. GOTCHA: realtime/video/detail sobre un vídeo viejo/borrado devuelve cascarón {extra,log_pb,status_code:0,status_msg} sin aweme_detail → tratar como "no sé", no como "no es ad". COSTE del scan diario: ~18 req (10 páginas de vídeos + 8 lotes de detail) = ~6.600/año vs 100k del plan → 15x margen.
* [2026-07-15] Radar v2 — `services/fresh_ads_discovery.py`: DESCUBRIMIENTO INVERTIDO (de la inyección al producto) + 3 fallos de calibración que SOLO se ven con datos reales. Flujo: barrer `get_fresh_ad_videos` (v3 video/list is_ad=1 sales_flag=1 sort create_time desc, 1 req/página, corta al salir de la ventana) → agrupar por `video_products` → `get_products_detail` (10 IDs/req) → score → filtrar → deep AD-ratio de los finalistas (`get_product_ad_videos`, 1 req c/u) → persistir. Real: 4 páginas → 40 vídeos → 40 productos → 11 con métricas (~55% hit) → 8 pasan → top con 95.0. Coste ~18 req/scan. LOS 3 FALLOS (todos cazados corriéndolo, ninguno por revisión): (1) usar `views_7d` TOTALES como "crecimiento" PREMIA LA SATURACIÓN — un producto con 488 creadores tiene más views *porque* tiene 488 creadores; fix = `views_per_video()` = views_7d/video_count_7d ("lo que se llevaría TU vídeo"). Además `total_views_7d_cnt` es INCREMENTO 7d, no acumulado → rango real ES 2-850, no decenas de miles (mi full_at=20.000 dejaba la tracción en ~1 punto de 20 → muerta; ahora 800). (2) `ad_videos_fresh` es CONSTANTE=1 barriendo 60 vídeos → el eje ads (peso .30) no ordenaba nada; fix = deep-check del ratio real de etiquetas AD sobre los finalistas = el "5 de 10 llevan la lila" de Kalodata (POCO 2/5=40%, GEOMAR 3/10=30%). (3) puntuar comisión en % ORDENA AL REVÉS DE COMO SE COBRA: 12% de un pintalabios de 10€ = 1,20€ vs 2% de un móvil de 831€ = 16,62€ vs 10% del GEOMAR de 501€ = 50,14€; fix = `commission_eur()` = precio×%/100 (el % queda solo como suelo en filtros). VETO POR SATURACIÓN: la competencia como componente lineal NO basta — con datos reales el perfume (488 creadores) ganaba 54.0 a 52.5 al GEOMAR porque su tracción/vídeo es MAYOR (8.275 vs 2, es verdad); la regla del operador («menos de 200 creadores») es un VETO, así que va como MULTIPLICADOR del total (gate 1.0 ≤200 → 0.4 a 600+), no como resta; si no, al aflojar filtros el ranking lidera lo que no quieres. Pesos: ads .30 / competencia .35 (interno: total .45 + activos_7d .35 + vídeos_7d .20) / tracción .20 / comisión .15. tests/tiktok_shop/test_fresh_ads_discovery.py: 14 tests con fixtures CALCADOS de productos reales de ES (GEOMAR/PERFUME/ROBOT/POCO/MAQUILLAJE) — primer test del Radar que existe.
* [2026-07-15] Radar v2 — botón "Llenar un día automáticamente" (calendario). JobMode.TIKTOK_SHOP_AUTO_DAY + `runners.run_tiktok_shop_auto_day` (escanea fresh_ads_discovery → filtra por €/venta → importa top N → PlanEntry del día → build_pack por producto, best-effort: si el pack falla el producto YA está en el calendario, mejor sin prompts que perderlo) + `POST /radar/plan/auto-day` (AutoDayRequest: day/top_n/region/days_window/video_pages/max_influencers/min_commission_eur/gens; va por cola porque tarda ~18 req EchoTik + Gemini) + `useAutoDay` + tarjeta morada arriba del calendario con día/productos/máx creadores/mín €-venta. GOTCHA REPETIDO (3ª vez): al añadir un JobMode hay que tocar 5 sitios o la Cola da pantalla blanca — `src/queue/models.py` (enum + label), `runners.py` (_RUNNERS + _MODE_TO_PROGRAM), `frontend/lib/types/queue.ts` (union), `frontend/lib/queue-meta.ts` (MODE_TO_PROGRAM + SUBMODULE_LABEL + MODE_ICON + import del icono lucide + case del resumen de params). El typecheck local caza el union y el Record<JobMode,...> incompleto → `docker run --rm -v "$PWD/frontend":/src -w /src node:20-alpine sh -c "node_modules/.bin/tsc --noEmit -p ."` ANTES de push (el build de web casca con cualquier error TS). `min_commission_eur` se filtra en el RUNNER, no en FreshAdsFilters, porque necesita el precio ya resuelto del product/detail.
* [2026-07-15] Radar v2 — flujo auto-day VERIFICADO end-to-end en producción (día 3 del calendario): escaneo → import_candidate → PlanEntry → build_pack → 2 productos con 3 vídeos-problema cada uno (POCO F8 score 95 ads=fuerte, GEOMAR score 72.5 ads=media). GOTCHAS reales del run: (1) el runner tarda >2 min (Gemini por producto) → lanzarlo con run_in_background o por la cola, NUNCA en un Bash normal (timeout 2 min lo mata a media generación; el plan NO se corrompe porque prepo.save() está al final, pero los Product SÍ quedan creados → import_candidate es idempotente vía candidate.imported, así que re-lanzar no duplica). (2) Si un run se corta, NO hace falta re-escanear: DiscoveryRepo ya tiene los candidatos puntuados y con la etiqueta AD contada (ad_videos_fresh>0 los distingue del scan viejo por keyword) → retomar desde ahí cuesta 0 requests de EchoTik. (3) Las covers de echosell-images dan 403 (hotlink, learnings:194) → los productos entran SIN fotos y build_pack loguea "Re-análisis sin fotos source válidas"; el análisis IA tira solo del NOMBRE (que en EchoTik es largo y descriptivo → suficiente, como en learnings:218). El operador saca la foto de `cover_url` a mano para el escaparate. TODO futuro: `echotik/batch/cover/download` (≤10 URLs, NO consume créditos) resolvería el 403 de raíz.
* [2026-07-15] Calendario v2 — FECHAS reales + resultado por versión + estadísticas que escalan. (1) `models/month_plan.py` (DayEntry con `date` YYYY-MM-DD + uploaded/sold/sold_version/**sold_format**/revenue_eur; MonthPlan.stats()) + `repos/month_plan_repo.py` (1 doc por mes, key `plan:month:2026-07`, índice `plan:months`; add_entry idempotente por (date,product_id); stats(months) por mget). Sustituye a week_plan (día 1..N sin fecha, TODO el histórico en 1 clave). (2) LAS 2 DECISIONES QUE HACEN QUE ESCALE — el `/radar/plan` viejo enriquecía TODAS las entradas con datos vivos del Product (bien con 15, inviable con 200/mes): **la vista de MES no lee productos** (`GET /calendar/month/{m}` devuelve solo lo guardado = 1 lectura Redis) y **la de DÍA sí** (`GET /calendar/day/{date}`, acotado a ~6-20 productos, get_many). Si alguien mete enriquecimiento en /month, vuelve el problema. (3) `sold_format` está DENORMALIZADO en la entrada a propósito: guardar solo sold_version=2 e ir al Product a preguntar el formato obligaría a leer N productos para pintar estadísticas (lento justo cuando hay histórico) Y se rompería al regenerar prompts (los índices cambian → el histórico mentiría). `POST /calendar/outcome` resuelve el formato UNA vez, al marcar, y lo congela. (4) `scripts/migrate_plan_to_months.py` (ensayo por defecto, --apply para escribir): día 1 = HOY por convención — las fechas reales de los días pasados NUNCA se guardaron, así que no se inventa histórico; el WeekPlan viejo NO se borra (respaldo). Migradas 17 entradas → 2026-07 (10/15jul, 5/16jul, 2/17jul). (5) `run_tiktok_shop_auto_day` y AutoDayRequest usan `date` (default hoy) en vez de `day:int`. Router `api/routers/tiktok_shop/calendar.py` registrado en 4 sitios (routers/tiktok_shop/__init__, routers/__init__, main.py import + include_router). tests/tiktok_shop/test_month_plan.py: 6 tests, protegen que stats() NO necesite productos.
* [2026-07-15] Calendario v2 — interfaz. `app/tiktok-shop/calendar/{MonthGrid,OutcomeBar,Stats}.tsx` + `lib/queries/calendar.ts`. Rejilla 7 col L-D (inspiración: FIT9NESS + MasterPicks del propio operador) con color POR RESULTADO (verde vendió / azul subido / ámbar pendiente / gris vacío) y los € dentro de la celda; `StatsRow` (productos·subidos·vendieron·€ conv%) + `FormatRanking` (barras: qué formato vende — la razón de ser de las 3 versiones). `OutcomeBar` por producto: subido/vendió + chips V1/V2/V3 con el formato + € por venta; vender implica subido (si no, conversion_pct = sold/uploaded daría >100%). GOTCHAS: (1) `getUTCDay()` es 0=domingo → rotar `(d+6)%7` para semana europea o el mes sale desplazado un día. (2) `noUncheckedIndexedAccess` está ON → `"2026-07".split("-").map(Number)` da `(number|undefined)[]` y `new Date(Date.UTC(y,m-1,1))` no compila; helper `parseYm()` en vez de esparcir `!`. (3) COHERENCIA: al migrar el calendario a fechas hubo que migrar TAMBIÉN `_queue_product` (antes: append day=10_000 + reflow en bloques de N/día = una cola, no un calendario) y los schemas add-url/add-batch (`per_day`→`date`, BatchItemResult.day→date) + sus tipos TS; si no, lo añadido a mano escribiría en el WeekPlan viejo y NO aparecería en el calendario nuevo. VERIFICADO: `next build` completo en limpio (no solo tsc — el build de web casca el deploy entero, ver learnings del 2026-07). El WeekPlan viejo y `/radar/plan` siguen vivos pero ya NO los usa el frontend.
* [2026-07-15] EchoTik API — TARIFAS REALES (corrige learnings:192, que decía "~$1/1k (~$100/año)" y era FALSO). Los precios SÍ están publicados en `echotik.live/en/api-service` (mirror zh `shaxe.echotik.live/zh/api-service`), pero renderizados por JS → el HTML crudo parece vacío; están en el payload i18n de Next. TABLA: **Start** gratis 1.000 llamadas (10 req/s) · **Pro $199/mes** = ¥1.399/mes por 100.000 llamadas **AL MES** (30 req/s) · **Enterprise** a medida, ilimitado · **ANUAL A MEDIDA**: "Starting from 100K requests/year" + "As low as **¥1 / 1K calls**" + "volume discounts available" → contacto massif@echotik.live. RESUELTA la contradicción ¥0.001 vs ¥0.01: NO es errata — ¥1.399/mes ÷ 100k = **¥0,014/llamada = tarifa MENSUAL** (cuadra con el "低至 ¥0.01" de los docs); **¥0.001 = suelo del pago ANUAL con volumen** (su copy lo acota a "Annual billing"). Exactamente 10x. EL "~100$/año" ERA UNA FUSIÓN: "100K requests/year" (publicado literal) + ¥1/1K × 100K = **¥100/año ≈ 14 $/año ≈ 1,2 €/mes** — son YENES, no dólares; no existe ningún paquete de 100 $. Rango real esperable: **¥100-¥1.000/año (14-140 $/año, 1-12 €/mes)** — la horquilla es porque "as low as" es el suelo con volumen y nosotros pediríamos el mínimo. NUESTRO USO ~8.800 req/año → el paquete mínimo (100k/año) es 11x de sobra; el Pro mensual sería 170x lo necesario a 12x el precio unitario → NO cogerlo. FAQ: **solo se cobran las requests con éxito (HTTP 200 + code=0)**; los fallos NO descuentan cuota → `echotik_cloud._get` sobreestima coste (llama a record_custom también en respuestas con code!=0). Otros: la web dice "free 100 requests" en el hero pero "1.000 API calls" en la tarjeta del plan Start (incoherencia suya; el trial real dio 100). `echotik.live/platform/pricing` es un SPA vacío sin login (3 snapshots en Wayback, todos cascarón). NO existe endpoint público de paquetes (todo 404, sin swagger). NINGÚN tercero (blogs/foros/GitHub) cita precios de la API — solo los planes web ($9.9/$19.1/$29.1, que NO incluyen API).
* [2026-07-15] OpenCode remoto 24/7 — réplica de la infra de Claude Remote Control, montada en el VPS (`opencode-serve.service`, ACTIVO+enabled). CONTEXTO: Claude usa `claude remote-control --name <repo> --spawn same-dir`, UN servicio systemd POR REPO (10 activos), que abre conexión SALIENTE al relay de Anthropic → las apps de Windows/Android entran sin abrir puertos. Al replicarlo con opencode el relay y los clientes los pones TÚ: relay = Tailscale, clientes = opencode desktop + opencode-mobile (dzianisv). DIFERENCIA ARQUITECTÓNICA CLAVE: opencode NO necesita un servicio por repo — **un solo `opencode serve` sirve TODOS los proyectos** porque las sesiones viven en un SQLite global (`~/.local/share/opencode/opencode.db`) y cada sesión lleva su campo `directory`; el cliente filtra con `GET /session?directory=<path>` (verificado: TikTok_Automation_Python → 1 sesión, bet-ai-master → 8). Corolario: el server VE las sesiones que creó el bot de Telegram vía `opencode run` (mismo DB) → las conversaciones de Telegram salen en desktop/móvil. EL BUG DE `{env:...}` YA NO EXISTE: `telegram-agent/bot/src/index.ts:4-7` dice que `opencode serve` no resolvía `{env:OPENCODE_GO_API_KEY}` y por eso el bot spawnea `opencode run` por subproceso (el `opencode-serve.log` del 8-jul lo prueba: `ModelUnavailableError: opencode-go/minimax-m3`); en 1.17.15 la key se resuelve (len=67 en `/config`) y el end-to-end responde — probado creando sesión por API y mandando prompt a minimax-m3. AUTH: `OPENCODE_SERVER_PASSWORD` activa HTTP Basic con usuario `opencode` (override `OPENCODE_SERVER_USERNAME`); **apagada por defecto** — sin ella el server avisa "server is unsecured" y con `--hostname 0.0.0.0` sería una shell remota abierta a internet (el `opencode.jsonc` tiene `edit/bash/webfetch: allow`). Creds en `/etc/opencode-server.env` (root:root 600, password 39 chars + SOLO la key de Go copiada del .env del bot — NO el token de Telegram, que si no el agente lo leería con `env`). EL GOTCHA QUE CASI SE COLA: UFW es `deny (incoming)` por defecto y NO tenía regla para tailscale0 → el móvil habría dado timeout aunque el server estuviese OK; probar desde el propio host NO lo detecta (tráfico local a la IP de tailnet va por loopback y UFW lo deja pasar). Fix: `ufw allow in on tailscale0 to any port 4096 proto tcp`. Bind a `100.122.102.10` (IP tailnet), NO a 0.0.0.0 → inalcanzable desde la IP pública (verificado: conn refused desde 62.238.19.31, 401 sin auth por tailnet, 200 con auth). Funnel NO toca el 4096 (y NO debe: publicaría el agente a internet dejando solo el basic auth). `Requires=tailscaled.service` + `Restart=always` porque si tailscaled no está arriba el bind a esa IP falla. Sesiones persisten a restart (18 antes/18 después). PENDIENTE: opencode no tiene poda de storage (issue #22110, sin `session prune`) — 16M hoy, vigilar con el disco al 78%.
* [2026-07-16] Radar v2 — BUG GRAVE cazado POR EL OPERADOR mirando los productos reales: el ranking salía INVERTIDO respecto a las ventas. El POCO F8 puntuaba **95/100 con 1 (UNA) venta en toda su vida** y era el #1; la recortadora de **3.304 ventas** era el #7. Causa: al escribir `score_fresh_ad_product` quité la DEMANDA de la fórmula (ads .30 / competencia .35 / tracción .20 / comisión .15 — cero peso a ventas) y `FreshAdsFilters` no tenía `min_units_sold`. El error de razonamiento: tratar "pocos creadores" como positivo SIEMPRE, cuando tiene dos causas OPUESTAS — (a) nadie lo ha encontrado = oportunidad, (b) NADIE LO QUIERE = muerto — indistinguibles sin ventas. Corolario que se me escapó: **la etiqueta AD sin ventas NO es "auge de GMV Max", es un vendedor quemando dinero en algo que no convierte**; la inyección solo vale como señal si el producto YA demuestra que vende. Fix: (1) eje `demand` = _ramp_up(units_sold, 0, 500) con peso .20 (pesos → ads .25 / comp .30 / demand .20 / tracción .10 / comisión .15); (2) `FreshAdsFilters.min_units_sold=30` como SUELO DURO (el score por sí solo no basta: el POCO seguía sacando 75); (3) WinnerScore.demand ahora lleva ventas (antes duplicaba tracción); (4) `min_commission_eur` 3€→**1€**: el peine que YA vendió al operador fueron 12,14€ GMV ≈ 1,5€ de comisión → un suelo de 3€ habría descartado su ÚNICO ganador probado (el % engaña pero el suelo alto también). Verificado en vivo: el escaneo nuevo descarta "16 ventas (<30) — nadie lo compra" y ordena por demanda+competencia. `total_sale_cnt` es all-time y ES EL ÚNICO campo de ventas vivo en ES (7d/30d = 0). LECCIÓN: los tests con fixtures inventados pasaban (14/14) porque yo elegía los números; el bug solo se vio con datos REALES. Los fixtures ahora llevan units_sold real (POCO=1 → debe filtrarse; recortadora=3304 → debe pasar). OTRO FALLO propio: le di al operador URLs `tiktok.com/view/product/<id>` que **learnings:215 ya documentaba como BLOQUEADAS** (Security Check) diciéndole "en tu navegador abre bien" — falso, era suposición. Para localizar el producto: buscar el NOMBRE en el Centro de Afiliados (el product_id sí es válido, la URL no).
* [2026-07-16] URL de producto TikTok: **MUERTA en todos los formatos, confirmado**. Probados `www.tiktok.com/view/product/<id>`, `shop.tiktok.com/view/product/<id>` y `www.tiktok.com/shop/pdp/<id>`: los 3 devuelven HTTP 200 pero con página de **Security Check** (5,5KB, 0 menciones del producto, 6 de "verify") incluso con UA de iPhone. Y el operador confirmó con captura que **en la APP nativa da "Hubo un problema · Inténtalo de nuevo"** → no es cosa del scraping, la página no existe para nadie. Las páginas públicas de EchoTik (`echotik.live/product/<id>` y variantes) dan **410 Gone**. CONSECUENCIA: el operador localiza el producto buscando el NOMBRE en el Centro de Afiliados — pero varias tiendas venden lo mismo, así que **hace falta la TIENDA para desambiguar**. FIX: `echotik/product/detail` ya devolvía `seller_id` (no lo guardábamos) y `echotik/seller/detail?seller_ids=a,b,...` (10 por request) da el nombre → `echotik_cloud.get_seller_names()`, `DiscoveredProduct.seller_id/seller_name`, `DayEntry.seller_name`, expuesto en la API y en la tarjeta del calendario (botón "Copiar nombre" + chip 🏪 tienda). Coste: 1 request por cada 10 productos. Verificado real: Flopp Eco / Geomar ES / ARMONIAS CURVE / GLAIRIS Spain. Quitado el `<a href>` a la URL canónica de la tarjeta — era un enlace a una página que no carga.
* [2026-07-16] Radar — EchoTik INFRAVALORA los creadores 2.6x (sistemático) + el Centro de Afiliados es la fuente de verdad. Contrastado con capturas REALES de la ficha del Creator Center (sección "Información de la promoción" → "Tendencias del producto · Últimos 7 días"): GLAIRIS EchoTik 126 → TikTok **330** (2.62x) · ARMONIAS EchoTik 47 → TikTok **122** (2.60x). Las dos razones casi idénticas ⇒ NO es ruido, es diferencia de definición (EchoTik solo cuenta los creadores que tiene rastreados) ⇒ **se corrige con una constante, sin verificar producto por producto** (verificar cada uno mata la automatización, que es el punto). Ventas y comisión de EchoTik SÍ son fiables (±7%: 229 vs 245 uds, 1,21€ vs 1,29€, 2,67€ vs 2,44€). FIX: `ECHOTIK_INFLUENCER_FACTOR=2.6` + `real_influencers(c)`; TODOS los umbrales (score `ifl_ideal_below/zero_above`, filtro `max_influencers`) van ahora contra el REAL estimado — antes iban contra el crudo y `max_influencers=250` dejaba pasar productos con ~650 reales. Para ORDENAR el factor es indiferente (constante ⇒ no cambia el orden); lo que rompía eran los umbrales. n=2, re-medir de vez en cuando abriendo UNA ficha. La entrada del calendario guarda ya el real estimado. LA FICHA DEL CREATOR CENTER da GRATIS lo que EchoTik no puede (sus campos 7d están muertos en ES): **Pedidos 7d ▲▼ · CTR ▲▼ · Nº de creadores ▲▼ · Add-to-cart ▲▼**. Señal de saturación REAL: GLAIRIS +91 creadores en 7d para 18 pedidos = estampida; ARMONIAS pedidos ▼12 = producto muriendo. INVESTIGADO (agente): **"Exposure Plus"** (etiqueta oficial "trending products with less content coverage", filtrable, diaria) es **US-ONLY** — scope literal "Se aplica a: Estados Unidos", cero evidencia en ES/UK, y gated incluso en US. Los ÚNICOS filtros del marketplace del creador ES son **precio, comisión y categoría** → cribar por creadores en ES es abrir fichas 1 a 1 (por eso el Radar tiene sentido: reimplementa para ES lo que TikTok no envía aquí). NO existe señal de ads para el afiliado (el carrusel "Aprende de los creadores" ordena por engagement/calidad/novedad, NO por inversión → no es proxy). `affiliate.tiktok.com` es panel de VENDEDOR (bundles `ecom_alliance/seller_*`); el del creador es mobile-first: Perfil → ☰ → TikTok Shop Studio → Creator Center. **`affiliate-es.tiktok.com` es NXDOMAIN.** Elegibilidad creador ES: ≥1.000 seguidores + ≥18 años (los "2.000" de blogs ES son falsos). Límite publicación UE incl. ES: 50 fotos + 25 vídeos/día.
* [2026-07-16] Vídeos-problema — feedback operador. (1) UGC salía FALSO (persona IA, fondo desenfocado). Causa: UGC usaba `veo3_prompt` (texto→vídeo = bokeh de estudio); los buenos (demo, persona luciendo) usan 2 PASOS (imagen Nano Banana → animar Kling, que da personas realistas). FIX `prompts/problem_video_director.md`: REGLA DE ORO — todo concepto con PERSONA protagonista (formatos 1/2/3/6) va por 2 pasos (`image_prompt`+`animate_prompt`, SILENCIOSO, mensaje en texto de pantalla); `veo3_prompt` SOLO para formatos SIN persona (4/5: demo producto/manos/primer plano). image_prompt/animate_prompt dejan de ser "solo formato 6"; el generador ya los parseaba para cualquier concepto (0 cambios de código). spoken_line siempre "". Obligatorio ≥1 demo y ≥1 persona realista. (2) Flechas/textos SIEMPRE iguales: `ready_video.py` rotaba por `style=idx` (0/1/2 = índice de concepto) → cada producto la misma tripleta. FIX: (a) `runners.run_tiktok_shop_ready_video` — `seed=md5(product_id)%210` (7×5×6), `style=seed+idx` → cada producto arranca en otro punto, las 3 versiones siguen distintas; (b) `_ARROW_COLORS` 6 colores (rojo/naranja/amarillo/blanco/rosa/verde, borde adaptado) + `_ARROW_KINDS` 5 (down/double/fat/chevron/curved; curved vía `d.arc`), color desfasado del kind (stride 2); (c) `_TEXT_STYLES` 4→7. Todo el dibujo dentro del try/except de `_arrow_clip` → si una forma fallara al render, degrada a "sin flecha", no rompe el job. GOTCHA sesión: el sandbox bloqueó TODA ejecución de python/docker/git-con-cd → verificación por lectura + comprobado que el contenedor `api` arranca **healthy** tras deploy (importa los módulos sin error = sintaxis OK).
* [2026-07-16] Vídeos-problema — 3 fixes tras feedback del operador. (1) UGC/persona salía FALSA con fondo desenfocado: el formato "UGC hablando a cámara" usaba `veo3_prompt` (texto→vídeo), que hace personas de look IA con bokeh. FIX en `prompts/problem_video_director.md`: REGLA DE ORO — cualquier concepto con PERSONA protagonista (formatos 1,2,3,6) va por 2 PASOS (image_prompt Nano Banana + animate_prompt i2v, SILENCIOSO, mensaje en texto de pantalla); `veo3_prompt` SOLO para formatos SIN persona (4,5: demo de producto, manos, primer plano). Obligatorio ≥1 concepto de producto y ≥1 con persona. spoken_line siempre "" (todo silencioso ahora). El generator ya parseaba image/animate para cualquier formato → sin cambio de código. (2) Flechas siempre iguales: `_ARROW_KINDS` 3→5 (+chevron, +curved) y NUEVA paleta `_ARROW_COLORS` 6 colores (rojo/naranja/amarillo/blanco/rosa/verde, cada uno con su borde) = 30 combos; `_arrow_clip(color_idx)` + `_arrow_color` con stride 2 para desacoplar color de forma. (3) Textos siempre iguales: `_TEXT_STYLES` 4→7. LA CAUSA RAÍZ de "siempre lo mismo": el runner pasaba `style=idx` (índice de concepto 0/1/2) → TODO producto arrancaba en el estilo/flecha 0 y salía la misma tripleta. FIX: `seed = int(hashlib.md5(product_id).hexdigest(),16) % 210` (=7×5×6) y `style=seed+idx` → cada producto arranca en un punto distinto de la rotación, las 3 versiones siguen distintas entre sí. Verificado: 4 productos × 3 versiones, cero patrones repetidos.
* [2026-07-16] Flechas de vídeos-problema — USAR LAS REALES del admin, no dibujar. El operador: "flechas ya hay muchas, no añadas; en nebulabs-media están sincronizadas con la web admin". Fuente de verdad = `<TIKTOK_EDITOR>/Assets/flechas/*.mov` (12 flechas, = ARROW_SHAPES de nebulabs-media/lib/style-presets.ts, `id`=.mov). `ready_video.py`: FUERA todo el PIL (_draw_arrow_png/_arrow_clip/_ARROW_KINDS/_ARROW_COLORS, ~120 líneas); la flecha ya NO se compone en MoviePy. Nuevo flujo: MoviePy renderiza vídeo+texto a un temporal → `_overlay_arrow_ffmpeg` superpone la .mov con FFmpeg (`-stream_loop -1` + `overlay=x=(main_w*px)-overlay_w/2:y=...:enable='between(t,t0,t1)'`, scale a 16% del ancho, format=rgba) → output_path. CLAVE: el fallo antiguo ("failed to read first frame de flecha_blanca.mov") era de MOVIEPY leyendo del drive de red; **FFmpeg las lee sin problema** (ya lo hacía editor_auto/tools/sticker_arrow.py — reusé su enfoque exacto). `_pick_arrow(idx)` rota por las 12 con desfase por producto (seed hash%420) y salta las que no existan en disco. Ventana: single→[0,dur], sequence→[split,dur]. Fallback robusto: si no hay flecha o el overlay falla, os.replace(tmp→output) → nunca sin vídeo. `_arrows_dir()` reusa `editor_auto.config.arrows_folder()` (asset transversal) con fallback a nebulabs-media/public/arrows. Verificado end-to-end: overlay real de flecha_roja.mov sobre vídeo test, frame extraído OK. Quitado `import math` (solo lo usaba el bob de PIL).
* [2026-07-16] Editor de vídeo LIBRE (genérico, no atado a concepto) + pestaña ⚡ Plantillas con interfaz simple. El operador quería que las plantillas (⚡, POV/manos, sin caras) tuvieran la MISMA interfaz que los vídeos-problema: chips para copiar prompt con 1 clic + botón "subir vídeo para editar" + descargar. Problema: el flujo de subida de vídeos-problema (`/videos/problem/upload`) está atado a `concept_index` de `product.problem_videos` (guarda el resultado en el concepto); las plantillas no tienen concepto ni gancho propio. SOLUCIÓN: modo GENÉRICO reutilizando el mismo runner y `process_ready_video`: (1) `run_tiktok_shop_ready_video` — si viene `out_path` en params, edita directo (sin producto/concepto) con hook_text/cta_text pasados a mano; si no, el modo concepto de siempre. (2) endpoints nuevos en radar.py: `POST /videos/edit/upload` (file+hook_text+cta_text+zoom → guarda raw en `<SHOP_ROOT>/_manual_edits/<token>_raw.mp4`, encola con out_path, devuelve `token`) + `GET /videos/edit/ready?token=` (sirve `<token>.mp4`, 404 hasta que la cola acaba; token validado `[a-f0-9]{8,32}` anti path-traversal). (3) Frontend: componente `TemplateEditCard` (CopyChips 1er-frame/Kling/Veo3 + input de gancho opcional + subir XHR con % + poll HEAD del ready cada 4s → botón Descargar). Verificado end-to-end: runner genérico sobre vídeo test → secuencia gancho→CTA + flecha real negra + quita-marca, raw borrado. `next build` limpio. GOTCHA: `re` no está a nivel de módulo en radar.py (solo import local en _parse_batch_line) → añadido `import re` local en download_edit_video.
* [2026-07-18] Editor Auto — runner standalone de MUESTRAS por lote (`scripts/editor_auto/batch_samples.py`, captación Nebulabs Media). Reutiliza el MISMO motor que la cola: `src/editor_auto/pipeline/run.py:run_editor_auto_pipeline(user_id, input_video_path, job_id, temp_folder, on_log, on_progress, source_filename=, output_override=)`. CLAVE para salida local (no Drive): `output_override=<ruta abs>` fija `final_output_path` directo y BYPASSEA `manual_review` (run.py:335-360) → la muestra cae donde quieras, no en draft. El pipeline requiere un `EditorUser` con `tool_flow` de ToolStep habilitados (`UserRepo().list_all()`, campos `.id/.name/.tool_flow[].tool_id/.enabled`); si no hay herramientas activas, `raise`. Herramientas del registro: `silence_cutter`, `subs_auto`, `sticker_arrow`, `photo_insert`. Ya existe estilo "Nebulabs Prueba" (id 70102ea273c549319b83bec17998a05e) con silence_cutter+subs_auto+sticker_arrow = corte+subs karaoke+flechas, ideal para muestras. GOTCHA DE DEPLOY (cazado verificando): la imagen Docker `tiktok-api` está HORNEADA y RECORTADA — `/app/scripts/` NO existe en el contenedor y el repo NO está bind-mounted (solo `/app/secrets` bind + `/app/temp_work` volume). Para correr un script nuevo en prod: `docker exec tiktok-api mkdir -p /app/scripts/editor_auto && docker cp <script> tiktok-api:/app/... && docker exec -w /app tiktok-api python /app/.../script.py` (ejecutar por RUTA, no `-m`, o falla `No module named 'scripts'`). El contenedor SÍ tiene deps (pydantic/redis) y Redis; `--list` (solo lectura) verifica sin efectos. Además: hay DOS copias del repo en el VPS — el git real en `/home/nebulabsai/proyectos/TikTok_Automation_Python` (sin .env ni venv) y una copia vieja en `/home/nebulabsai/TikTok_Automation_Python` (con .env de 4KB + venv roto) a la que apunta `tiktok-factory.service` (Streamlit legacy). El runtime real es Docker, no esos venvs.
* [2026-07-22] Vídeos-problema — TODO parte de foto Nano Banana (2 pasos), sin texto→vídeo. Feedback operador: recomiendan que TODOS los vídeos partan de foto generada con Nano Banana (no solo UGC), porque así (a) se controla el encuadre y NO salen fondos desenfocados de look IA, y (b) el i2v es más consistente que texto→vídeo (que muta el producto a mitad). Cambio en `prompts/problem_video_director.md`: los formatos 4 y 5 (demo de producto, POV/manos) pasan de `veo3_prompt` a 2 PASOS igual que los de persona → `veo3_prompt` SIEMPRE "" para todos; `image_prompt`+`animate_prompt` SIEMPRE rellenos. image_prompt ahora cubre también producto-sin-persona (producto protagonista, primer plano/manos, escena real de casa) con la MISMA directiva de cámara anti-bokeh (deep focus edge to edge). animate_prompt refuerza CONSISTENCIA (keep product identical/stable, no morphing/warping/extra fingers/flickering, movimiento mínimo). Flujo operador aclarado en el prompt: Paso 1 Nano Banana = adjunta foto del PRODUCTO; Paso 2 Kling i2v = adjunta SOLO la imagen generada (el producto ya está dentro, NO re-adjuntar la foto del producto → evita que el modelo la reinserte y rompa consistencia). Sin cambio de código: el generator ya parsea image/animate para cualquier formato (`if not veo and not img: continue` mantiene los que tienen img); el front ya ramifica por image_prompt (todos mostrarán chips Paso 1/Paso 2). Verificado: colágeno/gummies/gua sha regenerados 9/9 en 2 pasos, veo3 vacío, todos con 'deep focus/no blur' en la imagen.
* [2026-07-22] Vídeos-problema — vídeo con Veo 3.1 (i2v) + realismo anti-look-IA reforzado. (1) El operador anima con **Veo 3.1** (imagen→vídeo: la foto de Nano Banana es el primer fotograma), no Kling → actualizadas todas las menciones del prompt (0 "Kling" restantes). (2) Petición crítica del operador: la FOTO no debe parecer IA. El image_prompt ya tenía la directiva anti-bokeh (cámara que enfoca todo); AÑADIDO un bloque anti-look-IA explícito con los tells concretos: `candid amateur iPhone photo, real natural skin with visible pores/imperfections, NO beautification/smoothing/airbrush, NO waxy/plastic skin, NO glossy CGI, NO perfect symmetry, natural imperfect lighting, imperfect casual framing, subtle camera grain, NOT an ad NOT cinematic NOT a render`. Verificado: GOKOCO regenerado, 3/3 con las 4 directivas (no-bokeh + anti-IA + ancla-producto + consistencia). NOTA: la key FREE de Gemini se agotó a mitad → cayó a PAID automáticamente (el fallback dual-key funciona).
* [2026-07-22] Calendario — botón "Abrir producto" (la URL shop/pdp SÍ abre en la app). El operador confirmó que `https://www.tiktok.com/shop/pdp/<product_id>` **abre el producto en la app de TikTok** (el "Security Check"/"Hubo un problema" era solo para bots/scrapers y para el formato view/product; el formato shop/pdp abierto desde la app logueada funciona). `calendar.py` get_day: `product_url` = shop/pdp/<tiktok_shop.product_id> (vacía si no hay product_id de TikTok); DayEntryDetailOut +product_url; tipo TS CalendarEntryDetail +product_url; tarjeta del calendario: botón verde "🔗 Abrir producto" (target _blank) junto a "Copiar nombre". Los 3 del día 22 (creados desde ScrapeCreators con pid) ya lo traen. FUTURO: al cablear el buscador de ScrapeCreators al Radar, guardar `tiktok_shop.product_id` en cada producto creado → el botón sale solo.
* [2026-07-22] Buscador de productos ScrapeCreators (España) en el calendario. EchoTik-ES lleva caído (ventas a 0); ScrapeCreators SÍ da productos ES con ventas reales + foto que carga. `api/scrapecreators.py` (cliente: search_shop_products(query,region=ES,page) → normaliza {product_id,title,sold,price,image,seller}, ordena por ventas; 1 crédito/búsqueda, key SCRAPECREATORS_API_KEY). Endpoints radar.py: `POST /scrape/search` (ScrapeSearchRequest query+min_sold[def 20]+max_price[80] → productos con product_url shop/pdp) + `POST /scrape/add` (crea Product origin=radar con tiktok_shop.product_id+product_url, descarga foto origin="internet", añade DayEntry al día, encola vídeos-problema vía _maybe_enqueue_gens). Frontend: hooks useScrapeSearch/useScrapeAdd + componente `ProductSearcher` (input nicho + mín ventas + resultados con foto/ventas/precio/tienda/abrir + botón ➕Añadir por producto) en el calendario, encima del día-automático. LÍMITE honesto (dicho al operador y en la UI): ScrapeCreators da ventas pero NO tendencia ni nº creadores ni ADS → es DESCUBRIMIENTO; la tendencia se verifica en el Centro de Afiliados con el botón "Abrir producto" (que ya funciona en la app). El filtro min_sold hace de criba: en un nicho, la mayoría de los 30 resultados tienen pocas ventas; solo 1-3 superan el umbral = los que venden de verdad. Verificado: search 'colageno' min_sold=50 → 1 producto (1158 ventas); typecheck + next build OK.
* [2026-07-22] Vídeos-problema — 3 afinamientos tras feedback real (producto FOODELIS Creatina Gummies). (1) FIDELIDAD DE COLOR: Nano Banana mutaba el producto (pastillas azules → salían frambuesas rojas). image_prompt ahora: `EXACT real colour (if blue stays blue), do NOT change it` + bloque NO INVENTES: `do NOT add fruits, berries, garnish, leaves, decorations or objects not in the reference`. (2) FONDO DESENFOCADO persistía en V2/V3 pese a la directiva anti-bokeh (solo V1 la aplicaba) → nuevo enfoque FÍSICO: pedir el fondo CERCA (`plain wall right behind the subject, close, NO distance`) para que no haya profundidad donde desenfocar, no solo negar el bokeh. (3) VÍDEO de persona era "mujer mirando fija sin hacer nada" → animate_prompt con persona ahora exige ACCIÓN: `ACTIVELY shows/uses the product, lifts toward camera, turns label, applies/takes it, lively gestures + genuine expression, NOT a static stare` + nota de que Veo 3.1 i2v genera UN plano continuo (no varias tomas → el dinamismo va dentro del plano: persona activa + leve push-in de cámara). Verificado: creatina regenerada 3/3 con las 4 mejoras. LÍMITE del buscador ScrapeCreators reconfirmado al operador: da ventas pero NO comisión/tendencia/reseñas → salen duds (productos sin comisión, cero ventas, en caída); hay que "abrir producto" y verificar en el Centro de Afiliados ANTES de añadir, no a ciegas. No arreglable con código (el dato no existe en ScrapeCreators).
* [2026-07-22] Vídeos-problema — la persona VUELVE a hablar (español de España) en formatos de persona a cámara. Me pasé de frenada al hacerlo TODO silencioso: el operador quiere que al menos una versión hable. Veo 3.1 genera voz + lip-sync nativo, y como la persona sale de foto realista (Nano Banana), hablar ya no queda falso como el texto→vídeo viejo. Cambio: formatos **1 (persona a cámara)** y **2 (testimonio)** son HABLADOS → `spoken_line` relleno en español de España (natural, de creador, no locución), y el `animate_prompt` incluye la frase textual + `speaks in casual natural Spanish from Spain (Castilian accent), natural accurate lip-sync`. Formatos 3,4,5,6 siguen silenciosos (spoken_line=""). Obligatorio ≥1 concepto hablado (1 o 2). Verificado: creatina regenerada, V1 habla ("¡Estaba harto del polvo!..."), animate con frase+lip-sync ✅. NOTA: spoken_line NO afecta a ready_video (que quema hook+CTA); es para el prompt de Veo + info del operador. La mención de "frambuesa" en el spoken_line es correcta (sabor real del producto); lo que se corrigió es que la IMAGEN no meta frambuesas rojas (las gominolas son azules).
* [2026-07-22] Vídeos listos — flecha CTA más baja (pegada al carrito). El operador: la flecha estaba muy arriba y no apuntaba al enlace del carrito. `_ARROW_CY` 0.70→0.82 (y CX 0.24→0.22): centro al 82% de alto → punta ~88%, justo donde TikTok Shop pone el enlace/carrito naranja (esquina inferior izquierda). Verificado: frame renderizado, flecha abajo-izquierda sin cortarse.
* [2026-07-22] Vídeos-problema — personas realistas PERO atractivas (sin acné). El operador: realista está bien pero sin mucho acné (no es atractivo de ver); mejor hombres/mujeres jóvenes ~30 y atractivos. Equilibrio: se mantiene piel natural (poros, textura) para matar el look de plástico IA, PERO piel sana/limpia sin acné. image_prompt: framing con persona ahora `attractive good-looking young adult around 28-32, appealing and camera-friendly`; bloque anti-look-IA cambia "texture and tiny imperfections" → `visible pores and realistic texture, but CLEAR HEALTHY GOOD-LOOKING skin — NO acne, NO pimples, NO blemishes`. Verificado: creatina regenerada, V1/V3 (persona) atractivo+sin-acné+poros; V2 (demo manos) no aplica.
* [2026-07-22] Vídeos-problema — race read-modify-write borraba el ready_video de una versión. Síntoma: el operador subió las 3 versiones, se editaron OK (los 3 .mp4 en videos_ready), pero V1 volvió a "📤 Subir vídeo" mientras V2/V3 mostraban "Descargar". Causa: `run_tiktok_shop_ready_video` leía el Product ENTERO al empezar el job (antes del procesado de minutos), tocaba `problem_videos[idx]["ready_video"]` y guardaba el producto completo al final. Con V1 y V2 procesándose casi a la vez (ambos ts 1914), el save de V2 se basaba en una lectura previa al save de V1 → machacaba el ready_video de V1 (last-write-wins sobre doc compartido). Fix: re-leer FRESCO justo antes de guardar (`fresh = repo.get(product_id)`) y tocar SOLO `fresh.problem_videos[idx]`, reduciendo la ventana de carrera de minutos a ms. Remediación del producto afectado: revincular V1 a su .mp4 existente en disco (el archivo estaba, solo el campo vacío). Nota: el endpoint /videos/problem/ready NO tiene fallback al nombre real (busca `concept_{i}.mp4`), así que un ready_video vacío = botón "Subir" aunque el archivo exista.
* [2026-07-23] TikTok Shop — nueva pestaña "🎬 Replicar viral" (ingeniería inversa foto→vídeo). El operador sube un MP4 que YA funciona → `viral_analyzer.replicate_viral_2step` reutiliza el pipeline del analyzer (ffprobe + Whisper + `_sample_frames` ~30 JPG) y llama al NUEVO prompt `viral_replica_director.md` (clona los anchors de realismo/anti-bokeh/producto-idéntico de `problem_video_director.md`). Gemini (free-tier, gemini-2.5-flash, frames+transcript) devuelve `{why_viral:{hook,retention,emotion,why_sells,visual_style,structure}, videos:[schema 2-step problem_videos]}`. La foto de referencia opcional (producto NUEVO) se adjunta como ÚLTIMA imagen y el image_prompt se ancla a ella; sin ella, se ancla a la 1ª foto source del producto o al producto tal cual sale en el viral. Persistencia: `Product.viral_replicas` + `viral_replica_analysis` (campos nuevos). Endpoints en radar.py: `/videos/replica/{generate,upload,ready}`. CLAVE: el runner `run_tiktok_shop_ready_video` se generalizó con `concept_field` (default "problem_videos", o "viral_replicas") → MISMA maquinaria de quemado (zoom+gancho+CTA+flecha) para ambas listas; raw file `replica_{i}_raw.mp4` y output tag `rep{i}` para no colisionar con problem_videos. Frontend: page `app/tiktok-shop/replicate/page.tsx` (select producto + subir MP4 + foto ref opcional + n + idioma) reutiliza CopyChip + patrón subir→editar→descargar del calendario. El endpoint generate es SÍNCRONO (~30-60s: Whisper+Gemini) — el front muestra spinner, no cola.
* [2026-07-23] Replicar viral — modo AUTO por duración (versiones vs segmentos encadenados). Veo 3.1 i2v ≈ 8s/clip → un hablado de 20-30s NO cabe en un clip. `replicate_viral_2step` decide por duración: ≤10s → MODE versions (N clips independientes A/B, `segments:[]`); >10s → MODE segments (k=min(4,ceil(dur/8)) trozos), UNA réplica fiel troceada, n forzado a 1. CLAVE anti-cara-distinta: solo el trozo 1 lleva foto Nano Banana; los trozos 2+ son `is_extend=true` con `image_prompt=""` → el operador usa "Extender" de Veo/Flow desde el ÚLTIMO fotograma del clip anterior (misma persona/escena, sin corte). El `spoken_line` se parte en k trozos. hook/cta/caption a nivel de réplica (no por trozo). El prompt `viral_replica_director.md` recibe la línea MODE explícita (Python calcula k, el modelo no decide). `_normalize_segment` fuerza segs[0].is_extend=False y vacía image_prompt en los extend. La subida→ready sigue a nivel de RÉPLICA (índice 0): el operador une los k clips en Flow/CapCut y sube UN vídeo → mismo quemado gancho+CTA+flecha. Frontend renderiza tarjetas de trozo con chip "🎬 Animar (extender)" para los is_extend.
* [2026-07-23] Replicar viral — modo STANDALONE (sin catálogo). El operador NO quiere elegir producto base ni crear el producto en la web: basta vídeo + foto del producto. Cambios: `replicate_viral_2step(product=None)` ahora es válido (getattr con defaults; el bloque de fallback de fotos ya solo corre si product no es None). Nuevo flag `same_product` (checkbox "la foto es el producto del vídeo") → ajusta la nota de anclaje del prompt (mismo producto del viral vs producto DISTINTO al que se traslada la fórmula). Endpoint `/videos/replica/generate`: `product_id` ahora opcional (""), `reference_photo` obligatoria si no hay producto, NO persiste en catálogo si no hay producto (devuelve las versiones en la respuesta). La subida→edición usa el EDITOR GENÉRICO existente (`/videos/edit/upload` con hook_text+cta_text+zoom → token) en vez de replica/upload+ready (que necesitan producto) → sin persistencia de producto. Nuevo `/videos/edit/status?token=` (sondeo ligero ready:bool, sin descargar el vídeo) para que el front muestre el botón verde. Frontend: página reescrita, render desde el estado de la respuesta (no useProduct), poll por token. Nota: en standalone los resultados NO sobreviven un reload (están en estado cliente) — trade-off aceptado por simplicidad.
* [2026-07-23] Replicar viral — HISTORIAL persistido (no perder ideas al recargar). El operador pidió que las réplicas standalone se guarden tipo calendario. Nuevo `ReplicaSession` (models/replica_session.py) + `ReplicaRepo` (repos/replica_repo.py, key `replica:<id>`, índice `replica:index`, patrón idéntico a PlanRepo). `/videos/replica/generate` ahora persiste SIEMPRE una sesión (aunque sea standalone sin producto) y devuelve `replica_id`. Endpoints nuevos: `/videos/replica/{list,get,delete,settoken}`. El vídeo final quemado sigue usando el editor genérico por token; el token se persiste en `session.videos[idx].ready_token` vía `/settoken` cuando el poll detecta ready → el botón verde de descarga sobrevive recargas (los .mp4 del editor genérico no se auto-borran). Frontend: panel "🗂️ Mis réplicas guardadas" (abre sesión → recarga prompts+tokens, o borra). seedTokens() reconstruye los tokens al abrir una sesión guardada. NOTA: el análisis sigue SÍNCRONO (~30-60s); se persiste al terminar, no se encola (el operador sugirió encolar pero la persistencia ya resuelve la pérdida de ideas; encolar el análisis sería un JobMode nuevo, pendiente si lo pide).
* [2026-07-23] Replicar viral — segmentar por PLANOS/cortes (consistencia). El operador replicó un vídeo de "enseñar pantalón desde ángulos" (multi-plano con cortes: frontal→lateral) y la réplica lo hizo como UN clip continuo que rota → móvil flotando, segunda mano, morphing. Causa: Veo 3.1 i2v = UN plano/~8s por clip; meter varios ángulos o rotación 90° en un clip rompe consistencia. Fix: la segmentación ahora la decide GEMINI por PLANOS (no solo por duración): MODE segments si el viral tiene >1 plano/corte (aunque sea corto) O dura >8s. Nuevo campo por segmento `transition`: "cut" (plano/ángulo nuevo → foto Nano Banana propia, is_extend=false; para un ángulo nuevo de la MISMA persona el image_prompt fija "SAME person/face/outfit, different angle, use previous image as reference") vs "continue" (mismo plano que se alarga → extiende del último fotograma, is_extend=true, sin foto). Se replican los MISMOS cortes del original; NUNCA se convierte multi-ángulo en rotación continua. Anti-morph reforzado en cada animate_prompt: movimiento MÍNIMO, sin rotar el cuerpo, móvil/mano con agarre firme (no flota, no segunda mano/brazo/dedos extra), cambio de ángulo SOLO en el corte. Python: `_normalize_segment` deriva is_extend de transition; el modo real sale de la respuesta de Gemini (raw['mode'] o presencia de segments); cap max_segments=min(5,ceil(dur/8)+1). Frontend: iconos ✂️ corte / ↪️ continúa, chip "🖼️ Foto (nuevo ángulo)" con nota de usar la foto anterior como referencia.
* [2026-07-23] Replicar viral — corte DENTRO de un clip (Veo 3.1 sí puede), no trocear vídeos cortos. Corrección al fix anterior: me pasé troceando un vídeo de 9s (multi-plano) en varios clips. El operador: Veo 3 hace cambios de plano en el MISMO vídeo. Correcto — Veo 3.1 genera 1 clip de hasta ~10s y SÍ hace CORTES DUROS a otro ángulo dentro del clip; lo que rompía la consistencia (móvil flotando, morphing) era pedir una ROTACIÓN CONTINUA, no el corte. Nuevo criterio: `fits_one_clip = duration_s <= 10.0`. Si CABE (≤10s) → MODE versions, UN clip; si el original tiene cambios de ángulo se describen como HARD CUTS dentro del animate_prompt ("shot 1 front ... then a HARD CUT to shot 2 side-profile of the SAME person/outfit ..."), nunca rotación. Solo si NO cabe (>10s) → MODE segments (trocear, como antes). Prompt: sección MODO reescrita + bullet de "CAMBIOS DE ÁNGULO en un solo clip = corte duro" en animate. Regla mnemónica: cambiar de ángulo = CORTE LIMPIO, jamás rotación/morph.
* [2026-07-23] Replicar viral — preferir "continue" (extender) sobre "cut" para misma persona. El operador con un vídeo de 15s de una persona: la herramienta le pidió generar 3 FOTOS (3 cut). Riesgo: 3 fotos sueltas = 3 caras distintas. Regla nueva en el prompt: por defecto **1 sola foto de apertura (cut) + el resto continue (extender desde el último fotograma)** cuando es la misma persona/escena a lo largo del tiempo — extender GARANTIZA la misma cara. Un 15s hablado = 1 foto + 2 continue (3 clips de ~5s), NO 3 fotos. Solo se añade otro "cut" si hay cambio de escena/plano REAL (persona→primer plano del producto, cambio de habitación). Respuesta operativa al operador: sí, N segmentos = N clips de ~5s que se unen; pero si son "cut" genera cada foto usando la anterior como referencia (+ foto producto); mejor aún, que la mayoría sean "continue" (extend).
* [2026-07-23] Replicar viral — 2 fixes de consistencia por feedback real. (a) BUG: las fotos NO llevaban persona pero el animate SÍ → Veo inventaba una persona DISTINTA en cada clip. Regla crítica nueva en el prompt: si el vídeo tiene persona, el image_prompt DEBE mostrar a esa persona en la foto (primer fotograma); NUNCA foto solo-producto + persona metida en el animate. image_prompt y animate_prompt deben coincidir. (b) Flow del operador NO tiene "extender" → se abandona el mecanismo extend. Ahora en modo segments CADA segmento lleva su PROPIA foto (image_prompt siempre relleno) y cada foto 2..k referencia la ANTERIOR ("SAME person as in the attached previous photo, use it as reference") + la del producto → Nano Banana mantiene la cara sin extender. `_normalize_segment`: is_extend SIEMPRE False, image_prompt nunca se vacía; transition ("cut"=cambio de escena real / "continue"=misma persona) queda solo informativo para la UI. Frontend: quitado "Extender"; cada trozo muestra "🖼️ Foto (misma persona)" + nota "adjunta la foto del trozo anterior + la del producto". Un 15s de persona = 3 fotos encadenadas por referencia (no 3 personas distintas, no extend).
* [2026-07-23] Replicar viral — respetar PRESENCIA HUMANA del original. El viral era POV (solo una mano, sin cara) pero la réplica metió personas con cara. Nuevo campo de análisis `human_presence` (none / hands_only / body_no_face / face) que MANDA sobre el formato: la réplica muestra la MISMA cantidad de persona que el original. hands_only → POV solo manos, PROHIBIDO cara/cuerpo (image_prompt+animate: "first-person POV, only hands/forearms visible, NO face, NO head, NO full body"). none → sin personas. face → persona (aplica la regla de "persona ya en la foto"). Se muestra en el panel "por qué viraliza" (Personas: ...) + Planos. Regla en el prompt como CRÍTICA 1 (presencia) antes de la CRÍTICA 2 (si hay cara, la foto la lleva).
* [2026-07-23] Replicar viral — nombre de producto (IA) + miniatura en el historial. El operador quería identificar de qué producto es cada réplica. (a) Gemini devuelve `product_name` (nombre corto, lo identifica por la foto+frames) → se usa como título de la sesión y se muestra en la cabecera del resultado + filas del historial. (b) La foto de referencia subida se persiste (upload_subdir("replica_thumbs")/<uuid>.ext) como miniatura — se copia ANTES del cleanup del tmpdir (el finally borra el tmp antes de crear la sesión, así que se copia en el bloque de escritura de ficheros). Endpoint `/videos/replica/thumb?replica_id=` (auth api_key query, va en <img src>). ReplicaSession +product_name +thumb. list/get devuelven product_name + has_thumb. delete borra también el fichero de miniatura. Frontend: cabecera de resultado (miniatura 14x14 + nombre) + filas de historial con miniatura 10x10 + product_name como título.
* [2026-07-23] Replicar viral — animate respeta presencia humana + género fijo. Dos bugs: (a) foto POV/manos pero el vídeo metía una persona → el animate_prompt inventaba a alguien. REGLA CRÍTICA 3: el animate solo mueve lo que YA está en la foto; si es POV/manos/sin persona, "do NOT introduce or reveal any person, face or body not already in the first frame". (b) Género inconsistente (hombre en una versión, mujer en otra). REGLA CRÍTICA 4 + nuevo campo de análisis `person_gender` (male/female/none): Gemini elige UN género para toda la réplica y lo repite EXPLÍCITO en CADA image_prompt y CADA animate_prompt (y en la voz: male/female Spanish voice). El analyzer inyecta person_gender dentro de why_viral para guardarlo/mostrarlo; frontend lo muestra en "Personas: <presencia> · <género>".
* [2026-07-23] Replicar viral — 2 bugs: miniatura rota (auth) + segmentos vacíos. (a) MINIATURA/descargas por <img>/<a>: `get_current_user` (dependencia del router radar) solo aceptaba el HEADER X-API-Key; un <img src>/<a href> NO puede mandar headers → 401 → miniatura rota. Con API_KEY configurada en prod TODOS los endpoints del router lo exigían. Fix: `get_current_user` ahora acepta TAMBIÉN `api_key` como query param (además del header) → arregla miniatura y de paso las descargas <a> (problem/ready, edit/ready, replica/ready) que tenían el mismo bug latente. (b) SEGMENTOS VACÍOS: dejaba a Gemini elegir el modo y devolvía `mode:segments` con `segments:[]` y prompts de nivel superior vacíos → tarjeta SIN prompts de foto/vídeo. Fix: el modo lo decide PYTHON por duración (determinista: ≤10s versions, >10s segments), se COMANDA a Gemini el modo, y se VALIDA/SALVA la salida: versions siempre con image+animate no vacíos; segments siempre con ≥1 segmento con prompt (si un segmento no trae image_prompt hereda el del anterior); si TODO viene vacío → RuntimeError "reintenta". Las réplicas viejas ya guardadas con prompts vacíos son irrecuperables (borrar y regenerar).
* [2026-07-23] Replicar viral — umbral 1-clip a 11s (10s = un clip con cortes internos, no troceado). El operador: un vídeo de 10s no debe trocearse en 3 fotos/clips (demasiado trabajo); él genera clips de 10s y prefiere que el prompt describa los cortes DENTRO de un solo animate. El vídeo probaba 10.x s (>10) y caía a segments. Fix: `fits_one_clip = duration_s <= 11.0` (margen para que un "10s" que probe 10.x siga siendo versions = 1 clip). En versions el animate ya describe HARD CUTS internos (shot 1 → hard cut → shot 2 → hard cut → shot 3). Solo se trocea si es claramente >11s. Las réplicas ya guardadas como troceadas no cambian; regenerar para obtener el clip único.
* [2026-07-23] Replicar viral — replicar el GESTO CTA (señalar al carrito). El operador: el viral señala abajo-izquierda al enlace del carrito pero las réplicas no tenían gesto de CTA (importante). Nuevo análisis `cta_action` (describe cómo el original llama a comprar: señala/toca hacia el carrito abajo-izquierda, o "none"). Regla en animate: si cta_action != none, el vídeo DEBE incluir el gesto al final ("at the end, the person/hand POINTS DOWN toward the bottom-left corner of the screen — toward the TikTok Shop cart link — inviting tap-here gesture, hold ~1s"); en POV = mano señalando abajo-izquierda; en segmentos va en el ÚLTIMO. La flecha superpuesta del ready_video ya apunta al carrito, pero el gesto de la persona es lo que convence. Se muestra en el panel "por qué viraliza" (Gesto CTA: ...).
* [2026-07-25] Replicar viral — vídeos de CONVERSACIÓN (dos voces). El operador quería replicar un viral tipo entrevista (quien graba habla fuera de cámara + protagonista en cámara). No hay diarización de audio real, pero Gemini INFIERE la estructura por el transcript (turnos Q→A) + frames. Nuevo análisis `dialogue` (none / interview) auto-detectado + toggle manual "conversation" que lo fuerza. Replicación (límite honesto de Veo): la protagonista EN cámara con lip-sync (spoken_line); quien graba como VOZ EN OFF ("an OFF-SCREEN voice, not visible, says ... then the on-camera person answers with lip-sync ...") = mejor esfuerzo de Veo, no siempre limpio. Quien graba NO aparece (respeta human_presence). Géneros explícitos (off-screen vs on-camera) y consistentes. Backend: param conversation en replicate_viral_2step + endpoint; frontend: checkbox "🗣️ Es conversación" + muestra "Conversación: Entrevista..." en el panel. La segunda voz es best-effort — se avisa en la UI.
* [2026-07-26] Replicar viral — selector de ficheros no abría en móvil ("no pasa nada" al enviar). Los botones de subir vídeo/foto usaban `<button onClick={() => ref.current?.click()}>` sobre un `<input type=file className="hidden">` (display:none) → en Chrome Android no abría el selector, así que el formulario nunca tenía ficheros y al pulsar "Analizar y replicar" solo salía un toast que no se ve bien en móvil (percepción: "no pasa nada"; confirmado en logs: ni un POST a /replica/generate). Fix: patrón `<label>` envolviendo el input con `sr-only` (el mismo que ya funcionaba en el calendario para "📤 Subir vídeo") + borde verde y ✅ con el nombre del fichero elegido + aviso INLINE (`formError`) además del toast para validación/errores de red. Regla: en este proyecto, para inputs de fichero usar SIEMPRE `<label>`+input sr-only, nunca ref.click() sobre input oculto.
- Sidebar: `pathname.startsWith(basePath)` abría "TikTok Shop" Y "Tiktok Shop AI Pro" a la vez ("/tiktok-shop-ai-pro" empieza por "/tiktok-shop") → helper `isUnder()` que exige límite de segmento (`===` o `base + "/"`).
- El container `tiktok-api` NO traía `rclone` (solo el host) → `subprocess.run(["rclone",...])` desde un runner fallaba con [Errno 2] y, si el error se tragaba, el job salía VERDE con cero ficheros subidos. Para escribir en "Mi unidad" basta el mount FUSE `/mnt/drive` (rclone sube en background por `--vfs-cache-mode full`); para leer un Drive *shared-with-me* NO hay alternativa: se instaló rclone en `Dockerfile.api`.
- En `Dockerfile.api`, un `RUN` nuevo en la stage `base` invalida el `pip install` del builder (torch/whisper) → rebuild completo que llegó a LLENAR el disco del VPS. Colocar instalaciones nuevas después del pip y antes de `COPY src`.
- El container corre como uid 999 (`app`), así que un `rclone.conf` del host en 600/uid1000 es ilegible aunque se monte. Copia legible en `secrets/` (gitignorada) + `--config` explícito.
- `rclone copy` DESCARTA ficheros con nombre duplicado ("Duplicate object found in source - ignoring") — en un Drive con 2224 objetos se perdían 388 (17%). Para un backup fiel hay que reconciliar con `lsjson --recursive` (devuelve todos, con ID) + `backend copyid` a nombres desambiguados.
- Drive "shared with me" NO se ve por el mount FUSE: hay que leerlo por CLI con `--drive-shared-with-me`.
- En Drive puede haber ficheros con NOMBRE DUPLICADO en la misma carpeta (`2.PNG` ×2) → descargar por path es ambiguo; usar `rclone backend copyid <remote>: <fileID> <dest>`.
- Escribir scratch con `output_path.with_suffix(...)` lo mete en el dir de salida, que luego se publica entero → el scratch acaba en Drive. Usar siempre tmp_dir.
- Viralización: los tramos de paisaje se cortaban por TIEMPO fijo sobre un vídeo de 61min, así que dos tramos seguidos podían ser el mismo plano (transición visible pero mismo lugar). Solución: banco de clips troceado por CAMBIO DE ESCENA real (`select='gt(scene,0.28)'` + `showinfo`), un fichero por plano.
- Al revisar material para descartar texto, revisar el ENCUADRE FINAL, no el fotograma original: en 9:16 se usa solo la franja central del 16:9, así que rótulos y marcas de agua de las esquinas desaparecen solos. Revisar el 16:9 descartaba 84 planos, 49 de ellos válidos.
- Extraer una ventana de un clip para xfade necesita `duración + medio fundido por cada lado`. Si la ventana va pegada al borde, ffmpeg NO falla: rellena congelando el último fotograma (vídeo "de 49s" con 470 frames en vez de 1471). Reservar margen y verificar con `ffprobe -count_frames`.
- `ffmpeg -vf "select=...,metadata=print"` no escribe nada si el filtro no exporta metadatos; para timestamps de corte de escena usar `showinfo` y parsear `pts_time` de stderr.
- Un `until ! pgrep -f "<patrón>"` se auto-detecta (el propio bash del bucle contiene el patrón) y no termina nunca: usar `pgrep -f "[p]atrón"`.
- Viralización estilo G "Cuadrado": el redondeo de esquinas se hace con máscara PNG (PIL `rounded_rectangle`) + `alphamerge`; ffmpeg no dibuja rectángulos redondeados. La máscara se añade como ÚLTIMO input y su índice se calcula (`3 if music else 2`) — un índice fijo leía la pista de música como si fuera la máscara.
- Viralización: el arranque mudo de los vídeos no se arregla solo con Whisper (redondea la 1ª palabra a 0.00s aunque el fichero tenga 0,74s de aire) ni solo con `silencedetect` (corta en una respiración) — `renderer.snap_to_first_word` cruza ambos: manda silencedetect y solo avanza a la palabra siguiente si en ese punto no hay ninguna sonando, con tope de 1,5s.
- ffmpeg: `drawbox` NO anima posición (su `t` es el grosor del trazo). Para motas de polvo que se mueven, una lámina PNG generada con PIL + `overlay=x='x0+vx*t'` (overlay sí evalúa `t`) — cientos de drawbox+enable no se movían y costaban muchísimo más.
- ASS: eventos solapados en el tiempo se apilan verticalmente; para desperdigar palabras hace falta `\pos` por evento (con `\N` todo el bloque comparte una posición) y el campo `Layer` decide quién tapa a quién.
- Nicho POV BOF: la URL de un producto de TikTok Shop solo se puede sacar por EchoTik (`search_products` → `product_id` → `https://www.tiktok.com/view/product/<id>`, formato verificado por el operador). Clave de la keyword: trocear el título por separadores y SALTAR los trozos que son solo la marca — media catálogo empieza por "Freshly - …" y buscar "freshly" devuelve la tienda entera.
- Viralización estilo H: libass resuelve la fuente por NOMBRE DE FAMILIA (`Montserrat ExtraBold`), no por nombre de fichero, y hay que apuntarle `fontsdir` a `assets/fonts` — de ahí `StylePreset.font_name`/`fonts_dir`. El amarillo de resalte va en BGR (`&H00E9FF&`), que ASS invierte respecto a RGB.
- `deploy_safe.sh` leía la cola en `${APP_DIR}/temp_work/queue_state.json` (bind del host) pero la API la escribe en el volumen docker `api_temp` → `/app/temp_work`. El guardarraíl leía un fichero de meses atrás y decía "sin jobs running" con un lote al 43%. Ahora cuenta con `docker exec tiktok-api`.
- `.gitignore` tiene `*.jpg`/`*.png` globales: cualquier asset de UI nuevo (p. ej. las miniaturas de estilos en `frontend/public/`) se queda fuera del commit SIN aviso y el build de Docker lo sirve como 404. Hace falta una excepción `!ruta/*.jpg`.
- El gancho NO tiene holgura vertical (la fuente 16:9 se escala para cubrir 1080x1920 y encaja exacta de alto), así que la cabeza del ponente queda a ~70px del borde; el recorte cuadrado centrado empezaba en y=420 y la cortaba por los ojos. Se sesga con `SQUARE_CROP_Y_FRAC` (0.08 en vez de 0.5).
- `deploy_safe.sh` decidía rebuild de la API con una lista blanca de módulos de `src/` que NO incluía `viralizacion` ni `nicho_pov_bof`: sus cambios se pusheaban y el autodeploy los daba por desplegados sin reconstruir. Ahora dispara con cualquier cambio bajo `src/`, `assets/` o `prompts/`.
- CRF fija CALIDAD, no tamaño: los estilos con `noise=alls` alto (Reveal 35, Noir 18) gastaban ~90 Mbps conservando ruido aleatorio → MP4 de 800 MB para 74s, que llenaron el disco del VPS. Arreglado con techo `-maxrate 8M -bufsize 16M` en el encode final + bajar el grano.
- El `xfade` final abre UN input de vídeo por tramo de paisaje: con 19 tramos de 1080x1920 ffmpeg muere por OOM (SIGKILL) en 8 GB de RAM. `build_paisaje_segments` topa ahora en `MAX_PAISAJE_CLIPS`.
- `batch.run_batch` caía en la rotación por ronda cuando `styles_pool` venía vacío, y las rondas salen del reparto de audios: 15 vídeos con 8 audios = 2 rondas = solo 2 estilos de 8. La UI prometía "sin marcar ninguna se usan todas". Ahora vacío = todos, repartidos.
- `video_editor._render_text_line` descartaba las líneas sobrantes (`lines[:max_lines]`) y mutilaba el texto sin avisar: el CTA "COMPRUÉBALO TÚ MISMO 👀" salía como "COMPRUÉBALO TÚ". Ahora encoge el cuerpo de letra hasta que cabe.
- Al apilar PNGs con halo, `getbbox()` no sirve para recortar: la cola tenue del glow son píxeles no nulos. Hay que umbralizar el alpha (`point(a>45)`) o el bloque sale con huecos enormes entre líneas.
- El cajón de la cola agrupa por `MODE_TO_PROGRAM[mode]`: un modo nuevo que falte ahí NO se pinta en ningún grupo (el contador sí lo cuenta). Le pasó a `nicho_pov_bof_video`.
- El título que se quema en el vídeo del Nicho POV BOF es `titulo_tiktok_completo` (el real de la ficha), no el resumen de Gemini: es el fiel al producto. Se recorta acumulando TROZOS enteros entre separadores (`|`, `,`, ` - `), no por número de palabras a lo bruto — así no quedan colas como "… Body Serum - Gel".
- El texto que se quema en el vídeo del Nicho POV BOF es `titulo` (el corto que genera el prompt en columnas de <=4 palabras), NO `titulo_tiktok_completo` — el prompt `prompts/text_extractor.md` lo dice literalmente: el completo existe para BUSCAR el producto en el Centro de Afiliados. Leer el prompt antes de tocar qué campo se pinta.
- Con el disco al 100%, un `open(p,'w').write(s)` TRUNCA el fichero a 0 bytes sin lanzar excepción visible en el pipe. Comprobar `wc -l`/`git status` tras cualquier escritura si el disco estaba justo.
- La edición del Nicho POV BOF pasa de un `con_textos` todo-o-nada a cuatro herramientas independientes (`con_gancho`/`con_titulo`/`con_cta`/`con_flecha`). El runner lee `p.get("con_X", p.get("con_textos", True))` para que los jobs viejos encolados sigan comportándose igual.
- ASS: dos eventos solapados en el tiempo NO se sustituyen, se APILAN. Cualquier estilo que alargue el último evento de una frase (remate, cola de 0,25s) tiene que recortarlo al inicio de la frase SIGUIENTE, y el corte hay que mirarlo también entre líneas, no solo dentro de una. Se veían dos frases mezcladas en pantalla ("10 MIL METROS / COGODRILO O / TIRARSE UN AVIÓN…").
- `allocate_paisaje_clips` decidía reiniciar el ciclo mirando SOLO el número de clips libres, no los segundos: con 24 clips cortos libres (48,9s) y un vídeo que pedía 63s no reiniciaba y el lote moría con PoolExhaustedError teniendo la biblioteca entera sin usar. Ahora comprueba nº de clips Y duración acumulada.
- El prompt de `text_extractor` daba permiso explícito a Gemini para reescribir el `titulo` ("tal y como lo entendería un comprador", "no hace falta copiar el título larguísimo") y por eso salía "Set de Maletas" donde la ficha decía "Conjunto de Maletas". El operador daba por hecho que solo repartía en líneas. Ahora el prompt exige copia LITERAL con ejemplo de correcto/incorrecto.
- Gemini copia el título literal pero NO corta la cola de keywords SEO aunque el prompt se lo pida ("…Viaje Elegantes: Carcasa Ligera de ABS…"). El corte por separador (`:`, `|`, ` - `, `,`) se hace en `_titulo_para_video`, que es determinista y no obliga a re-extraer.
- La duración máxima de un vídeo de Viralización la manda la BIBLIOTECA de paisajes, no el audio: planos de 6,8s útiles como mucho (mediana 2,6s) × 12 clips = ~72s de b-roll. Por encima, el allocator mete 20-40 clips y el `xfade` revienta por OOM. Para superarlo hay que montar por tandas.
- El escaneo de ganchos coge CUALQUIER cara en primer plano, no solo al ponente: en el vídeo de Mario (formato entrevista) 8 de 81 candidatos eran la entrevistadora. Se separaron por el color del torso (ella de rojo: `r-(g+b)/2 > 20`, separación limpia — descartados 21-147, resto ≤10,5) y se verificaron con un mosaico. Revisar SIEMPRE con capturas al añadir un ponente nuevo.
- La caché de ganchos vive en `work_root()`, que en el container es el volumen docker (`/app/temp_work/viralizacion/hook_candidates/`), NO en la carpeta de assets. Al escanear un ponente desde el host hay que `docker cp` la caché o la API la re-escanea (18 min de CPU por vídeo).
- El montaje del b-roll por TANDAS (`XFADE_MAX_INPUTS=7`, recursivo) quita el límite de duración: verificado un vídeo de 85s con 30 tramos en 5 grupos, 2550 frames exactos (85×30) y 4,2 Mbps. Al agrupar hay que repartir PAREJO, no de N en N: 8 tramos daban un grupo de 7 y otro de 1, y pegar el suelto al anterior dejaba 8 → se volvía a trocear igual → recursión infinita.
- El arranque del audio lo decide SOLO `silencedetect`, nunca Whisper: en `pablo5_full.mp3` ("Pau Donés, unos días antes de morir…") Whisper daba la primera palabra en 1,90s porque se salta el nombre propio, y `snap_to_first_word` avanzaba hasta ahí comiéndose "Pau Donés". Whisper sirve para subtitular, no para saber dónde empieza el audio.
- Los vídeos de gancho pesan 300 MB-1,1 GB por ponente y NO hacen falta para renderizar (solo se sacan 3s). `scripts/viralizacion_precortar_ganchos.py` los recorta a clips de ~0,8 MB y el original se queda solo en Drive: Mario pasa de 303 MB a 60 MB. Verificado que el fotograma del clip coincide con el del original (diferencia media <1).
- El backup de "Productos España" fallaba con 2 de 2 ficheros: son ACCESOS DIRECTOS de Drive y rclone devuelve para ellos un ID compuesto `idAtajo\tidDestino` en `lsjson`. `backend copyid` no lo acepta; hay que pasarle el trozo posterior al tabulador (el fichero real al que apunta el atajo). El snapshot se sigue indexando por el ID compuesto para que el diff no se confunda.
- Al borrar los vídeos de gancho del VPS, `preflight_check` seguía exigiéndolos y dejaba a TODOS los ponentes sin poder generar. Ahora acepta el fuente O los ganchos pre-cortados. Cualquier cosa que dependa de `ponente_gancho_video()`/`paisajes_video()` hay que revisarla: en el VPS devuelven None a propósito.
- Segarra: 1376 candidatos brutos → 1082 son él (294 son el entrevistador, separable por luz del torso: él de camel ~78, el otro de oscuro ~42, valle limpio en 56-64) → 706 sin subtítulos quemados (el vídeo los lleva salpicados a partir del minuto 42; detectados muestreando cada 20s y midiendo píxeles casi blancos en la banda baja) → 200 repartidos por el metraje. El detector de texto por bordes Canny NO sirvió: 11 de 12 marcados eran micro y manos.
- Estilo de texto del Nicho POV BOF (referencia CapCut del operador): todo Montserrat, gancho y CTA en CURSIVA (`Montserrat-BlackItalic`) y nombre del producto RECTO (`Montserrat-ExtraBold`), con jerarquía de tamaños marcada (72/60/46 — con los tres a ~60 el bloque salía plano) y sombra NEGRA en vez del halo magenta/cyan.
- Veo3 dejó de poner marca de agua (2026-07) y Kling nunca la puso: el paso `_strip_watermark` solo re-encodeaba el vídeo para nada y el selector Veo3/Kling no cambiaba el resultado. Eliminados los dos; `origen` se sigue aceptando en la API por los jobs ya encolados.
- Color del bloque de texto del Nicho POV BOF: NO todo blanco. `_elegir_paleta` puntúa 6 paletas por (1) emojis del gancho/CTA — 💸→oro, ✅→lima, 👀→cian, 🔥→coral, (2) distancia de matiz respecto al tono dominante de la zona alta del vídeo (texto naranja sobre plano naranja no se lee) y (3) el nº de producto como desempate para que dos seguidos no salgan calcados. El nombre del producto sí va siempre blanco: es lo informativo.
- La sombra tiene que ir DESPLAZADA (`_SOMBRA_OFFSET`): una sombra simétrica se lee como halo difuso y era lo que hacía que el bloque pareciera plano.
- Los "destellos" que quiere el operador son NEÓN de color pegado a la letra, no una sombra: cada línea lleva `fill` y `glow` por separado (gancho cian + neón azul, CTA verde + neón verde cuando el emoji es ✅…). Truco clave en `_add_glow`: tras difuminar hay que REALZAR el alpha (`_DESTELLO_INTENSIDAD`), porque cada pasada de blur lo diluye y el neón sale lavado. Radio pequeño (6) e intensidad moderada (2.3): con radio 9 e intensidad 3.4 se convierte en una barra de color maciza.
- Variedad de rótulos en Nicho POV BOF: `_ROTULOS` combina tipografía (Montserrat cursiva/recta, Anton condensada, Rubik, Bangers, LuckiestGuy) con el acabado del destello (radio/pasadas/intensidad y si va centrado = neón o desplazado = sombra dura). Elegido de forma determinista por producto, así el mismo producto re-montado sale igual pero dos seguidos no comparten letra. 6 rótulos × 6 paletas = 36 combinaciones.
- CUMPLIMIENTO TikTok Shop: no se puede afirmar rebaja/oferta en pantalla (el precio puede subir y el vídeo sigue publicado). Hablar del CUPÓN sí es seguro. `texto_arriesgado()` en `video_editor.py` es la última barrera antes de quemar el texto y sustituye por algo neutro avisando en el log; el prompt lleva lista blanca y lista negra. De los 10 productos ya extraídos, 9 tenían gancho de riesgo ("PRECIO DE RISA", "BAJÓN DE PRECIO", "OFERTÓN IMPERDIBLE"…) → hay que re-extraer.
- Un CTA que dice "abajo"/"enlace" tiene que ser la ÚLTIMA línea del bloque: en medio, la flecha y el carrito quedan por debajo del nombre del producto y la mirada no sigue el recorrido. `layout_for_producto` lo decide leyendo el CTA.
- Al quitar un campo del formulario hay que quitarlo de las TRES capas: el `fd.append` del frontend, el parámetro `Form()` del endpoint (sin default da 422) y la validación dentro del handler (daba 400 "origen debe ser veo3 o kling"). Me pasó con `origen` al eliminar el selector Veo3/Kling y el operador se comió un "Error subiendo el vídeo".
- Buscar la URL del producto en EchoTik: la MARCA en la keyword es imprescindible ("crema manos antimanchas" → 0 resultados; "bella aurora crema de manos" → los 3 correctos), y no vale cortar por el primer separador porque medio catálogo escribe "MARCA - Nombre" y el primer trozo es solo la marca. La puntuación se mide sobre el NÚCLEO del nombre, no sobre la ficha SEO entera (comparándola entera, una crema de manos y una crema antiedad de la misma marca salían a 9 puntos), y la marca es CONDICIÓN, no puntos: si no aparece, se rechaza (evita atar una "Neutrogena Crema de Manos" a un producto de Bella Aurora).
- EchoTik busca por SUBCADENA literal, no por palabras: `wotsta` devuelve el producto exacto pero `wotsta silla para juegos asiento` devuelve 0, porque la ficha real dice "WOTSTA-silla para juegos, asiento" y con el guion y la coma la keyword deja de ser subcadena. De ahí la escalera: 1º marca+frase (fichas que las pegan, "BELLA AURORA Crema de Manos"), 2º solo la frase (fichas que las separan, "Freshly - …", "WOTSTA-silla"). No se puede deducir de la captura cuál usa el LISTADO real, así que se prueban en orden.
- Los productos de Freshly Cosmetics NO están indexados en EchoTik uno a uno (la marca sí devuelve packs y algún sérum, pero "spray protector termico" o "aceite corporal iluminador" devuelven Ogx, Neatly, Tom Ford…). Ahí no hay nada que rascar con esta API.
- EchoTik devuelve fichas HISTÓRICAS: un producto puede casar al 100% y estar retirado, y al abrir la URL TikTok dice "producto no encontrado". Las ventas (`units_sold_30d`/`units_sold`) son la señal de vida: se usan para desempatar candidatos y, si el elegido tiene 0, se marca en la UI ("sin ventas, puede estar retirada").
- Las palabras genéricas de razón social ("cosmetics", "beauty", "store", "official"…) NO sirven para verificar la marca: "Freshly Cosmetics" casaba con "DONNA COSMETICS SERUM FACIAL DE FRUTAS" y se ataba la URL de otra marca al producto. `_marca_distintiva()` las descarta.
- Las ventas que devuelve EchoTik NO son fiables: el operador comprobó productos con ventas reales que la API da a 0. No sirven ni para desempatar ni para detectar fichas retiradas.
- Buscar la MARCA con paginación (3 llamadas = ~26-30 productos) encuentra fichas que la búsqueda por nombre no ve, porque los títulos del operador no coinciden literalmente con el listado. Ojo: las palabras de la marca hay que mandarlas EN ORDEN, no alfabéticamente ("aurora bella" devuelve 6 productos, "bella aurora" 30).
- Con la lista de candidatos delante se puede pesar cada palabra por lo RARA que es en el catálogo (`score_ponderado`): sin eso, una "crema de manos" casaba al 67% con una "Crema Anti-manchas de día" porque "crema" y "antimanchas" las lleva medio catálogo y se perdía la única palabra que identifica el producto.
- Las credenciales de EchoTik se pueden cambiar EN CALIENTE desde la UI: `echotik_cloud._auth()` lee primero Redis (`echotik:credenciales`, caché de 30s) y el `.env` queda como respaldo. Antes había que editar el `.env` del VPS y recrear el container, o sea que dependía de tener SSH.
- El disco del VPS se llena en cada rebuild (~14 GB transitorios). Purgar la caché de Docker ANTES de construir, no después: con el build en marcha `builder prune` no puede liberar lo que está en uso y el build muere a medias.
- Los paquetes de schemas (`src/api/schemas/<programa>/__init__.py`) re-exportan clase por clase: al añadir una hay que tocar el `__init__` o la API no arranca (`ImportError`). Me ha pasado dos veces ya; si se parchea el container a mano hay que copiar también el `__init__.py`.
- NUNCA lanzar la prueba de punta a punta contra un producto real: mi test e2e publicó un vídeo de barras de colores como producto 1 de "3 Pront Flow" y lo marcó como subido. Usar una carpeta/producto de pruebas o parchear el destino.
- `alimiter` RE-NIVELA la salida hacia el límite por defecto (`level` activado): bajar `limit` SUBÍA el volumen en vez de bajarlo y el pico acababa en +1,6 dBTP. Hay que poner `level=disabled` para usarlo como red de seguridad.
- Subir la voz "+16 dB" a pelo satura: los audios del operador ya vienen con picos en -0,7 dBTP. Lo correcto es normalizar por SONORIDAD en dos pasadas (`loudnorm` mide y luego aplica) con compresor suave delante: los audios pasan de -17/-23 LUFS a -12,5/-14 con picos bajo -0,9 dBTP.
- El Drive compartido no era lento por ser compartido: el `rclone.conf` se montaba READ-ONLY, así que rclone no podía persistir el token renovado y reintentaba el guardado 10 veces (~5s tirados) en CADA invocación, además de pedir token nuevo siempre. Copiándolo a un sitio escribible: 6-9s → 1,6s por llamada.
- Un backup que guarda el snapshot aunque la copia falle olvida los fallos para siempre: al no verlos como "nuevos" la siguiente vez, no se reintentan jamás. Los IDs fallidos deben quedar FUERA del snapshot.
- `uploaded`/`video_path` los escribe el runner A LA VEZ al terminar, así que "uploaded && !video_path" nunca es cierto y no sirve para detectar "montándose": esa señal hay que sacarla de la COLA.
- Si el front hace `setQueryData` sustituyendo un elemento de la lista con la respuesta de un endpoint de UN elemento, esa respuesta tiene que traer TODOS los campos: `_producto_info` no rellenaba las fotos y al buscar la URL de un producto suelto su miniatura desaparecía hasta recargar.
- `photo_pairing` ya marcaba `confident: False` cuando no podía distinguir foto-de-producto de captura (típico: el producto solo tiene UNA foto en Drive), pero ese flag no salía de la API — el operador veía la captura de la descripción como si fuera la foto del producto, sin marca. Un dato que el backend ya calcula y no expone es un bug en potencia.
- Poner FECHA a la oferta es tan sancionable como afirmarla ("CUPÓN ACTIVO HOY" deja de ser cierto mañana y el vídeo sigue publicado). El filtro solo prohibía "SOLO HOY". Los términos temporales genéricos ("hoy", "ahora") hay que mirarlos EN CONTEXTO: "REVÍSALO AHORA" es un CTA válido, "CUPÓN ACTIVO AHORA" no.
- La semilla de variación del Nicho POV BOF era `output_path.stem`, y el runner siempre monta en `output.mp4` → TODOS los vídeos compartían semilla y la rotación de rótulo (tipografía) nunca varió: los 6 rótulos existían pero salía siempre "luckiest". Una semilla tiene que venir de la identidad del elemento, no de un fichero temporal.
- Para rotar por elemento no vale `sum(ord(c) for c in seed)` (ignora la posición: "1","9","10" colisionan) ni crc32 (es lineal: dos índices derivados salen correlacionados). SHA-1 con bytes distintos para cada índice sí reparte.
- Un detector de cumplimiento demasiado sensible es peor que no tenerlo: avisaba en 5 de 6 captions BUENOS ("perfecta para playa, camping") y en el único malo saltaba por el motivo equivocado. Los términos de promesa hay que mirarlos EN CONTEXTO (solo si el caption habla del cuerpo o la salud), y las subcadenas cortas por frase entera ("robo" saltaba dentro de "antirrobo", "regalo" dentro de "ideal para regalo").
- El orden de filtros del renderer de Viralización es `eq → vignette → noise → pre_subtitle_filters → subs`: al probar un grade a mano hay que respetarlo o el resultado engaña (poner `colorbalance` ANTES de `eq=saturation` daba frío donde el render real da sepia).
- Playfair Display solo se publica como variable font y libass usa la instancia por defecto (Regular, demasiado fina para vídeo). Hay que instanciarla a wght=900 con `fontTools.varLib.instancer` y guardarla estática.
- El polvo de celuloide en negro DESAPARECE sobre planos oscuros (mar, montaña en sombra). Para que se vea siempre hace falta que ~1 de cada 6 motas sea clara, y alpha alto (120-245; con 55-175 el operador ni lo notaba).
- La suciedad de película no son círculos: son motas IRREGULARES alargadas, vetas cortas y algún pelo del chasis (y en positivo, negras). Con solo bolitas se lee como confeti.
- El polvo tenía que MOVERSE rápido, pero una deriva lineal no sirve: la lámina solo tiene 540px de holgura y a 200 px/s se sale del encuadre en 3s. La solución es un vaivén senoidal — rápido y acotado por construcción.
- ffmpeg no trae `xfade=transition=burn`, pero `transition=custom:expr=` evalúa una expresión POR PÍXEL (X, Y, P, PLANE, A, B) y con eso se pinta un papel quemado sin depender de un vídeo de stock. Dos trampas: los `;` de la expresión hay que escaparlos (`\;`) porque dentro de un filter_complex separan filtros, y en yuv420p los planos de color miden la MITAD, así que las franjas en píxeles salen del doble de grosor si no se corrigen.
- En una expresión de filtro ffmpeg NO se pueden usar `st()`/`ld()` si el filtro hace slice threading: esas variables son COMPARTIDAS entre hilos y cada franja pisa a las otras. El síntoma engaña — parece un difuminado o ruido, pero es una carrera. Hay que repetir las subexpresiones en línea.
- En `xfade=transition=custom` la variable `P` va de 1 a 0, no de 0 a 1. Sin `(1-P)` la transición va al revés.
- El sexo de la voz del Nicho POV BOF se deduce del título por palabras clave (cosmética, pelo, "mujer" literal → mujer; el resto → hombre). Ojo con los tratamientos capilares y los aceites corporales: son media lista de Freshly/Kertox y sin "capilar"/"estrias" caían en hombre.
- Decirle a Gemini en el prompt "si no puedes leerlo es preferible que omitas ese producto" le da permiso para no intentarlo: devolvía `{}` en la imagen que le costaba. Hay que prohibir el relleno pero EXIGIR una entrada por identificador ("aunque solo distingas parte del título, escribe esa parte").
- En un lote de 10 imágenes Gemini se deja alguna suelta de forma intermitente. Un reintento automático con solo las que faltan (1 llamada extra, solo cuando hace falta) lo resuelve.
- Cuando una carpeta de producto trae 3 fotos en vez de 2 (porque se traspapeló una de otro producto), decidir la foto limpia por PESO se cuela. Las dos fotos buenas salen del mismo pantallazo del móvil y comparten ANCHO exacto con la captura con título; la ajena no. Comparar anchos lo resuelve (286 productos revisados, solo cambia el que estaba mal).
- La duración de cada paisaje en Viralización YA está al máximo del material: cada clip es un plano del original (3,9s de mediana) y el asignador reúne clips hasta que la suma de `duración - CLIP_TRANSITION_PAD_S` cubra el hueco, así que usa cada uno a tope. Subir `PAISAJE_CLIP_TARGET_S` no alarga nada (medido: 4,0 → 4,6 dejó el plano medio en 2,6 → 2,8s). Para planos más largos hace falta metraje con planos más largos.
- Cada décima que dura el fundido es una décima MENOS de paisaje limpio: la transición solapa los dos planos.
- Para elegir emojis que peguen con el producto, pedírselos a Gemini en la MISMA llamada de extracción sale gratis y acierta mucho más que una tabla de palabras clave (mochila de fútbol → 🎒⚽, cabestrillo → 🥰👶). La tabla se queda solo como respaldo para lo ya extraído.
- Servir una descarga desde el mount de Drive (`/mnt/drive`) tarda ~36s hasta el primer byte si el fichero NO está en la caché VFS de rclone: hay que traerlo entero de Google antes de emitir nada. Medido con un MP4 de 17 MB. Hay que guardar copia local de lo que se vaya a descargar.
- Un `<a href>` sin atributo `download` hace que el navegador NAVEGUE a la URL (parece que carga una página) en vez de descargar, aunque el servidor mande `Content-Disposition: attachment`.
- `product_repo.update_product` lee-modifica-escribe el documento ENTERO de la carpeta, y la cola corre hasta 4 workers: cuando varios vídeos del mismo lote terminaban a la vez se pisaban y ganaba el último. Medido: de 4 escrituras simultáneas sobrevivía 1. Se arregla con un cerrojo por carpeta (SET NX EX en Redis). El comentario del código decía "con un solo operador no hay carrera" — la carrera no la crean los operadores, la crean los workers.
- Los nombres de carpeta se REPITEN entre las dos fuentes del Drive compartido ("2 Pront Flow" existe en aleatorios_1 y en aleatorios_2), y la ruta de vídeos publicados en Drive NO lleva la fuente. Cualquier script que reconstruya estado a partir de esa ruta tiene que fijar la fuente, o escribe en la carpeta equivocada.
- `docker cp origen tiktok-api:/app/destino` con un DIRECTORIO que ya existe lo anida dentro (`/app/destino/origen`) en vez de sobrescribir. Hay que usar `docker cp origen/. destino/`.
- Con `from __future__ import annotations`, si el módulo no importa el símbolo usado en un `Annotated[...]`, FastAPI no resuelve la anotación y trata el parámetro como QUERY en vez de dependencia — revienta con un PydanticUserError críptico ("is not fully defined").
- El reparto de vídeos entre audios usaba `ceil(total/n)` y daba ese tope a cada uno hasta agotar: con 10 vídeos y 8 audios salía [2,2,2,2,2,0,0,0] y los tres ÚLTIMOS no se usaban NUNCA. Mismo error que el troceado del xfade. Lo correcto es `divmod` y sortear el resto, para que las vueltas de más no caigan siempre en los mismos.
- Los audios largos son los que retienen y viralizan: de los vídeos del operador, los dos más vistos salieron del único audio que pasaba del minuto. El reparto de vídeos entre audios da las vueltas sobrantes a los MÁS LARGOS por eso.
- La cola del frontend se alimenta de un WEBSOCKET (`/ws/queue`), no del endpoint REST `/api/v1/queue`. Filtrar solo el REST no habría cambiado nada de lo que ve el usuario. (Y por eso nunca se vio que `JobModeValue` del esquema REST no incluye los modos nuevos: ese endpoint casi no se usa.)
- Al introducir multiusuario, los trabajos ya guardados no tienen dueño real (`api-key-user`): hay que tratarlos como del admin o le desaparecen de la cola.
- `docker restart` de la API con un job corriendo NO mata el job: uvicorn arranca con `--timeout-graceful-shutdown 1800`, así que cierra el puerto (la web da 502) y espera hasta 30 min a que termine. La generación sigue, la web se cae. Hay que mirar la cola ANTES de reiniciar, no solo antes de desplegar.
- `.gitignore` tiene `*.jpg` global: cualquier imagen NUEVA que sirva el frontend (`frontend/public/**`) necesita su línea de excepción o se commitea el código y no la imagen — la página sale con el hueco roto solo en producción.
- La cuota de EchoTik renueva por CICLO, no acumulado: al contar llamadas por cuenta hay que resetear el contador cuando pasa el mes desde `primer_uso_at`, o una cuenta reciclada parece agotada para siempre. Y la fecha que importa es la de la PRIMERA llamada, no la del alta — por eso la pone el cliente en `_get()`, no la UI al guardar.
- Segunda vez que muerde lo mismo: guardar un documento ENTERO en Redis desde varios workers sin cerrojo pierde escrituras. En el banco de cuentas de EchoTik se perdía `primer_uso_at` (un worker leía la lista vacía para dar de alta la cuenta activa y pisaba la llamada que otro acababa de registrar). Regla: read-modify-write de un doc compartido → `set_nx` como cerrojo, siempre.
- Un flag de "sin cuota" como global del PROCESO es un candado sin llave con varios workers: lo pone uno (los demás siguen gastando) y al cambiar de cuenta nadie lo limpia, así que el bucle se para antes de la primera llamada con la cuenta nueva intacta ("sin cuota a mitad" con 0 llamadas hechas). El estado por-cuenta va en Redis; la global solo como respaldo.
- El guardarraíl de `deploy_safe.sh` esperaba 1h a que la cola se vaciara y, al cumplirse, DESPLEGABA IGUAL confiando en el graceful shutdown — que son 30 min y luego SIGKILL. Un lote de Viralización de 13 vídeos (~2h30) murió a mitad. Un timeout que "procede igual" no es un guardarraíl: al vencer hay que APLAZAR, no pisar el render.
- Ajustar un corte al silencio más cercano puede dejar el clip unas décimas por debajo del mínimo y tirarlo: el ajuste es cosmético (quien decide si el trozo vale es el modelo), así que hay que recuperar estirando el final —y el final del FICHERO cuenta como sitio válido para acabar— o retrocediendo el arranque, no descartando.
- Pedirle a un modelo "prefiere devolver menos y buenos" lo lleva a parar en el primer acierto: de una charla de 3 min sacó un clip de 99s y dejó los 74 finales sin mirar, con dos ganchos dentro. Hay que decirle explícitamente que RECORRA el material entero, y además comprobar en código los huecos ≥ mínimo y volver a preguntar solo por ellos.
- Cada programa tiene su propio wrapper de Upstash y NO tienen los mismos métodos: el de Viralización no tenía `delete`, así que borrar la propuesta de clips petaba con AttributeError JUSTO después de haber cortado los MP3 (ficheros creados + 500 al operador). Antes de usar un método del cliente Redis de otro programa, comprobar que existe en ESE fichero.
- Un `JobMode` nuevo hay que darlo de alta TAMBIÉN en el frontend (`lib/types/queue.ts` + los cuatro mapas de `lib/queue-meta.ts`): si no, `MODE_TO_PROGRAM[mode]` es undefined, el job no cae en ningún grupo del drawer y se cuenta en el badge pero no se pinta — existe y es invisible hasta que termina. El último grupo recoge ahora "todo lo demás" para que no vuelva a pasar.
- El modelo describía bien el gancho ("¿qué fácil es dar consejos…") pero daba un `inicio` 1.5s antes, colando la entradilla de plató ("esta noche va de consejos porque"). El texto del gancho SÍ es fiable: se busca en los timings de Whisper y manda ese punto, no el número. Y el ajuste a silencio no puede tocar ese arranque o lo arrastra otra vez hacia atrás.
- Rellenar huecos a la fuerza produce clips que arrancan a mitad de frase (uno cerraba en "sin contemplaciones con crudeza" y el siguiente abría en "con crudeza y aceptar lo que venga"). Regla barata que lo mata: exigir separación mínima (2s) entre el fin de un clip y el arranque del siguiente.
- El modelo da `inicio`/`fin` como números Y describe el gancho/cierre como texto, y los dos datos NO siempre cuadran: decía cerrar en "…la suerte te dio una oportunidad" (1:04) y ponía fin en 1:41, a mitad de otra frase. El TEXTO es lo fiable — se busca en los timings de Whisper y manda él; el número solo desempata si la frase aparece varias veces.
- Ajustar un corte "al silencio más cercano" NO salva un corte a mitad de frase: en "sabes que te estás engañando | y que la suerte…" hay una pausa real de 0.5s, es una respiración. El silencio afina décimas; quien decide dónde cierra la idea es el texto.
- Los "vendidos" se calculaban recorriendo las 31 carpetas de cada fuente producto a producto: 8 SEGUNDOS de Upstash para encontrar dos ventas, y sin foto (`clean_photo_id` se calcula al listar y no se guardaba nunca). Un dato que se consulta mucho y se escribe dos veces al día pide índice propio con copia denormalizada: 0.43s. Y el cliente Redis de nicho_pov_bof tampoco tenía `mget_json` (cada programa tiene el suyo, y no son iguales).
- Una carpeta de Drive compartida POR ENLACE no aparece en "Compartido conmigo", así que `--drive-shared-with-me` no la ve: hay que entrar con `--drive-root-folder-id <id>`, que la convierte en la raíz del remote (los paths van vacíos, `gdrive:`).
- Si un campo poco fiable (la tienda leída de una captura) está en los REQUIRED del extractor, un fallo de lectura tira el producto ENTERO — se perdía título y caption por no distinguirse el nombre de la tienda. Los campos que solo sirven para buscar van como opcionales y saneados: vacío y botón desactivado, no basura pintada como si fuera el dato.
- No meter palabras sueltas en la lista de "rellenos" del extractor por sonar a excusa: se añadió "encountered" creyendo que el modelo se lo inventaba y era el nombre REAL de una tienda ("Vendido por Encountered") — se estaba borrando un dato bueno. Un relleno se reconoce por la FORMA (frase de disculpa, texto entre corchetes), nunca por la palabra.
- Cuando las dos fotos de un producto miden exactamente lo mismo, el emparejado por forma/peso las asigna al azar y puede cambiarlas: la extracción lee entonces la foto SIN texto y responde "la tienda no se ve" aunque esté en la otra. Se desempata mirando el contenido: el visor del carrusel tiene bandas NEGRAS, la ficha un panel BLANCO de información. Una franja de 32x96 píxeles en gris basta, sin OCR.
- Los clips sacados de YouTube traen la sintonía del programa por debajo de la voz (en los de Pablo Motos, ~23 dB por debajo). Demucs la separa bien y torch ya estaba instalado por faster-whisper, así que solo hace falta el envoltorio. Tarda ~1,2x la duración en CPU → va en cola, nunca en una petición HTTP. Y no se aplica solo: la separación deja a veces un punto metálico en la voz y eso solo se juzga escuchando.
- Meter un paquete nuevo en `requirements.txt` reconstruye la capa de pip ENTERA, y con torch+CUDA dentro eso pide ~10 GB de temporal: dos deploys nocturnos seguidos murieron con "No space left on device" y dejaron la API sin actualizar. Antes de añadir una dependencia pesada, mirar el disco; si es de uso esporádico, instalarla a mano en el container y documentarlo.
- El VPS no tiene GPU pero la imagen traía torch con CUDA: 2,7 GB de `nvidia/*` + 1,7 GB de torch. Con `--extra-index-url https://download.pytorch.org/whl/cpu` y los pines `+cpu` esos 4,4 GB pasan a 171 MB. Es la diferencia entre poder reconstruir la imagen y no poder.
- `metadata=print` de ffmpeg (signalstats, cropdetect…) escribe por stderr con nivel INFO: con `-v error` no sale nada y el parseo devuelve vacío en silencio. Si un filtro basado en esa salida descarta el 100% de lo que mide, mirar primero el nivel de log.
- Filtrar rótulos mirando UN fotograma del clip no vale: en los vídeos de recopilación las cartelas entran ANIMADAS (el plano abre limpio y a los 2s aparece `GRAND CANYON`, o un mapa con `UNITED STATES`), así que a 0,5s no hay nada que leer y el clip se publica con el rótulo. Se muestrean 5 fotogramas repartidos, pegados en una tira para hacer UNA sola llamada al OCR (easyocr cuesta por área, pero cada llamada arrastra su arranque). Colaron 22 de 300 clips, 5 con el botón `SUSCRIBIRSE` del canal.
- Un detector de rótulos NO distingue un gráfico montado de un letrero filmado: en España saltaron el nombre de una barca y el toldo de una floristería con su teléfono, que son b-roll perfectamente bueno. Antes de retirar clips en masa hay que MIRAR una muestra — en España el filtro roto no había hecho daño y no tocaba nada.
- `index` del manifiesto de clips es la CLAVE con la que `usage_repo` lleva en Redis lo ya gastado por ponente (`paisaje_used:<ponente>`). Al retirar clips NO se puede renumerar: los índices ya gastados pasan a señalar otros clips, así que unos vuelven a salir y otros quedan bloqueados sin haberse usado. Se dejan huecos en la numeración; si ya se renumeró, resetear el contador del ponente.
- EchoTik no puede resolver las fichas de producto a escala: a 70 productos/día son ~4.200 llamadas al mes y el plan gratis da 100 por cuenta (42 cuentas), y encima solo acierta el 26% cuando SÍ hay cuota (19 de 123 productos con URL). Cuando una API es del tamaño equivocado no se afina el umbral: se optimiza el trabajo a mano. De ahí el "modo escaparate" — pendientes AGRUPADOS POR TIENDA, porque en el Marketplace se busca la tienda una vez y salen todos sus productos juntos (10 productos de 4 tiendas = 4 búsquedas, no 10).
- `uploaded` del Nicho POV BOF lo escribe el runner al acabar el MONTAJE, así que significa "tiene vídeo", no "publicado". Por eso `en_escaparate` no se deduce de él aunque en la vida real el escaparate vaya antes: deducirlo daría por hecho trabajo que nadie ha hecho.
- Para buscar en las 35 carpetas del Nicho POV BOF no hace falta índice: dos `mget` (lo compartido y lo privado del usuario) barren los 124 productos en 0,5s, mientras que leerlas una a una era lo que hacía que el ranking de vendidos tardase 8s. La foto se resuelve DESPUÉS de recortar a 20 resultados — emparejarla cuesta 0,25s por carpeta y hacerlo de las 35 en cada tecla serían 9s.
- Ordenar carpetas como texto pone "10 Pront Flow" antes que "4 Pront Flow". Cualquier listado de estas carpetas necesita orden NATURAL (partir por dígitos y comparar los números como números).
- **Consultar la cola desde fuera NO es leer.** Un `docker exec ... get_queue()` instancia una `JobQueue` en un proceso nuevo, y su constructor (a) marca FALLIDOS los jobs que encuentre en RUNNING —`_reset_orphans` da por hecho que si hay uno corriendo es que la app se reinició— y (b) arranca hilos trabajadores que pueden llevarse pendientes y morir con el proceso. Mató un lote de 27 vídeos a los 40s de arrancar. Para mirar, `manager.leer_jobs(persist_dir)` (lectura pura del JSON) o `JobQueue(..., readonly=True)`.
- En "Productos España" hay fotos guardadas **sin extensión**: el fichero se llama `1`, `2`, `3` a secas. `is_image()` miraba solo la extensión, así que descartaba carpetas ENTERAS — de las 4 de "2 Prod Aleatorios 2", dos salían con cero productos en la web y otra con dos, teniendo las cuatro sus diez. `rclone lsjson` ya trae el `MimeType` real, así que el filtro pregunta por extensión O por mime. Cuidado al añadir filtros por nombre de fichero sobre un Drive ajeno: el nombre lo pone otro.
- La TIENDA que extrae el nicho POV BOF no es la MARCA del título: la ficha de TikTok pone "Vendido por TEENO" en un producto titulado "DEWINNER-Colchón…". Parecía un fallo de extracción y era correcto. Se agrupa por tienda a propósito (en el Marketplace se busca la tienda), y el rótulo dice "Vendido por X" para que no engañe. Antes de "arreglar" un dato extraído, mirar la captura original.
- Un mismo NÚMERO de fichero puede ser DOS productos distintos: el operador repite número entre tandas y en la carpeta conviven `2` y `2.PNG` (o cuatro `8.PNG`) que son fichas distintas. Fundirlos escondía 15 productos en 10 carpetas (20 fotos → 8 productos). Se separan por nombre exacto en bloques de dos y, si el nombre no distingue nada, por FECHA DE SUBIDA: las dos fotos de un producto se suben con segundos de diferencia y entre productos pasan días. El bloque con extensión conserva el número pelado, porque su progreso ya está guardado en Redis con esa clave.
- Los huecos de numeración (de la 4 salta a la 6) NO son productos perdidos: la carpeta de Drive no los tiene. Comprobado antes de "recuperar" nada.
- Si le pasas a Gemini una foto SIN persona y le pides "devuélveme esta plantilla con la chica de la imagen", te devuelve la plantilla tal cual y en silencio — o sea, la modelo de ejemplo del curso, que es la que tiene todo el mundo. Detectarlo comparando la respuesta con la plantilla es frágil (el modelo varía una coma entre llamadas y el aviso deja de saltar): se le pregunta EXPLÍCITAMENTE con un `{"hay_chica": bool, "ficha": {...}}` y se comprueba ese campo.
- El montador del Nicho POV BOF ya sirve para otros nichos sin duplicarlo: `build_video` acepta las piezas por separado (`con_gancho`/`con_titulo`/`con_cta`/`con_flecha`) y ahora también `y_frac` para mover el bloque de texto. El Nicho Ropa Con Personas es una capa de 70 líneas encima (solo título centrado + flecha + voz sorteada) en vez de 1.200 copiadas que habría que arreglar por duplicado.
- Las tres carpetas de "Ropa Mujer" del Drive del curso (Mono, Pantalón corto, Bikinis) NO traen captura de la ficha: son 38 ficheros `IMG_44xx.PNG`, uno por prenda. O sea que ahí no hay NADA que extraer con Gemini y el título se escribe a mano. Camisetas/Conjuntos sí las trae (8 de 8). Antes de montar un flujo sobre "obtener textos", comprobar que la carpeta tiene capturas — y si no las tiene, no avisar en cada tarjeta de que "no se distingue la foto": con una sola foto por prenda eso es lo normal, y 38 avisos esconden los de verdad.
- Cuando una carpeta de producto no trae captura de la ficha, el título NO hay que pedírselo al operador: la foto enseña la prenda y Gemini le pone nombre mirándola ("Short deportivo rosa con cordón"), 9 de 10 en 7 segundos. Para texto que va quemado sobre el vídeo interesa además un nombre CORTO (3-6 palabras), no el título kilométrico del listado de TikTok — así que describir la foto da mejor resultado que leer la ficha aunque la hubiera. El campo a mano se queda para corregir.
- Para cuadrar vídeo y voz no siempre vale el mismo truco: en el POV BOF (plano de mano) se alarga rebobinando el final y no se nota, pero en un paneo de cámara continuo el ida y vuelta canta muchísimo — ahí se cambia la VELOCIDAD (un paneo al 0,95 no lo nota nadie). Con tope: fuera de 0.85-1.15 el movimiento se arrastra y es mejor recortar/alargar como siempre.
- Cuando un nicho necesita VARIOS clips por vídeo, la subida no puede encolar al vuelo: se guarda cada clip y solo se encola cuando están todos. Encolar con uno da un vídeo a medias que hay que rehacer entero, y el operador no se entera hasta el final.
- Una lista de "pendientes" calculada al vuelo (productos SIN textos) desaparece justo cuando el operador da el primer paso — extraer los textos —, que es cuando todavía le queda hacer el vídeo y ya no tiene forma de encontrarlos entre 350. Si la lista es un sitio de TRABAJO y no un informe, hay que persistirla: entra por el criterio, pero se queda hasta que se saque a mano.
- Las carpetas de gorras NO emparejan por número: las fotos se llaman `IMG_5033.PNG`, `IMG_5034.PNG`… y cada una tiene el suyo, así que el emparejador de los otros nichos daría 20 productos donde hay 10. Van de dos en dos POR ORDEN (limpia y después ficha) — comprobado 40/40 en cuatro carpetas, con la ficha siempre a ratio 2,17. Aun así se verifica por forma y se avisa si una pareja no encaja, en vez de fiarse del orden a ciegas.
- Para añadir audio DESPUÉS de la voz sin tocar los subtítulos: no se concatena al fichero de voz (eso obligaría a recolocar los tiempos de todas las palabras), se mete como una pista aparte con `adelay` y se mezcla con `amix duration=longest` — con `first` se corta entero, porque `first` se ata a la voz. La ventana de voz se queda igual, así que los subtítulos acaban donde acaba el clip hablado. Hay que separar "duración de la voz" de "duración del vídeo" en el finalize.
- [2026-08-05] [Cuenta Piloto] Las fotos SUBIDAS por el operador no pueden vivir en `api_uploads/`: `temp_storage.cleanup_expired` lo purga a las 24h al arrancar la API y el producto amanece sin foto. Van al Drive montado; el vídeo bruto sí puede quedarse ahí porque para entonces el montaje ya terminó.
- [2026-08-05] [Cuenta Piloto] Guardar N vídeos por producto exige `add_video()` bajo cerrojo, no `update_product(video_path=…)`: dos montajes del mismo producto terminan a la vez y el patrón "leer doc → modificar → escribir doc" hace que el último se cargue al otro.
- [2026-08-05] [Cuenta Piloto] `extract_from_pairs` del POV BOF sirve para fotos locales sin tocarlo: recibe `fetch` por parámetro, así que basta pasar una que devuelva la ruta ("el file ID ya ES el path").
- [2026-08-05] `app.include_router` está envuelto en un `_IncludedRouter` perezoso: tras registrar un router, `app.routes` NO enseña sus paths. Para comprobar que quedó montado, un `TestClient` (404 = no registrado) — mirar `app.routes` engaña.
- [2026-08-06] [Viralización] Vídeo que acaba con 10s mudos: NO era el CTA, era el audio fuente (`..._la_vida_es_para_vivirla_c2.mp3` dura 51s y Pablo calla en el 41). El pipeline recortaba el silencio de ENTRADA pero no el de salida y hacía el vídeo tan largo como el mp3. `ffmpeg_utils.trailing_silence` + recorte en `render_video`.
- [2026-08-06] `silencedetect` CIERRA el último tramo al llegar al EOF e imprime su `silence_end`, así que "el silencio que no tiene silence_end" NO identifica la cola del fichero — hay que comparar ese `silence_end` con el final de la ventana. Primera versión del detector daba 0 siempre por esto.
- [2026-08-06] `volumedetect` y `astats` escriben a nivel INFO: con `-v error` no imprimen nada y parece que el audio está vacío. Mismo gotcha ya anotado para `signalstats`.
- [2026-08-06] [Viralización] "CTA a la mitad" se repartía con `ronda % 2`, pero lo normal es 1 ronda por audio (23 audios = 23 vídeos, todos ronda=1) → se lo llevaban TODOS. Alternar por el contador de vídeos, no por la ronda.
- [2026-08-06] Una opción de UI que depende de assets que el operador aún no ha grabado tiene que decirlo: el selector de CTA se ofrecía con la carpeta `cta/` vacía y el backend lo ignoraba en silencio, así que parecía un bug del render.
- [2026-08-06] [Viralización] El CTA grabado (`CTA Final Videos.MP3`) venía a -18,9 LUFS contra los -12,8 de la voz: 6 dB por debajo, "ni se aprecia". `services/cta_audio.preparar()` lo nivela y lo cachea en `_procesados/` (patrón de `audio_bank.prepare`). Solo con `loudnorm` se quedaba en -14,8 — hace falta comprimir ANTES (17 dB de cresta) para que quepa la ganancia.
- [2026-08-06] La carpeta `Skool/` del curso está en "Compartido conmigo", así que NO se ve en el mount FUSE; hay que copiar con `rclone --drive-shared-with-me`. Y `viralizacion_assets/pablo/cta/` la creó el contenedor (uid 999), así que el usuario del host no puede escribir: se mete con `docker cp`.
- [2026-08-06] [Viralización] El CTA alarga el vídeo ~7s, así que hace falta MÁS paisaje. El reparto de clips se hacía con la ventana de VOZ sola: preflight decía OK y el render abortaba con "13 clips dan 36,3s pero hacen falta 43,1s" — justo los segundos del CTA. Hay que sumarlo en los DOS sitios (`reparto_con_cta` en el bucle y en `preflight_check`), no solo en el render.
- [2026-08-06] [POV BOF Largo] El prompt del curso pide 260 caracteres "para 15 segundos" pero su propio ejemplo tiene 357, y a 18,2 car/s eso son 20s: los 260 son inalcanzables en ese estilo. Forzarlos con reintentos deja el guion telegráfico. Se deja largo y se avisa; el vídeo son dos clips de 10s y se recorta a lo que dure la voz.
- [2026-08-06] [POV BOF Largo] La cadena de nivelado del POV BOF (`TP=-1.5`, limitador 0.9) NO vale para voces de Fish: daba +0,20 dBTP (recorte). Con `TP=-2.0` y 0.89 queda en -13 LUFS con pico -1,1. Los TTS tienen picos distintos a las grabaciones humanas.
- [2026-08-06] Fish Audio trae el sexo de la voz en `tags` (`male`/`female`) y la edad. Adivinarlo midiendo el tono (F0 por autocorrelación) NO funciona: errores de octava (232 Hz para voz masculina).
- [2026-08-06] [Viralización] Estilos: se retiran D, E, G y H (no viralizaban) y quedan A/B/C + A2/B2/C2, que son los mismos textos con el acabado limpio (`_LIMPIO`, el que se hizo para los cuadrados).
- [2026-08-06] La música de fondo iba con un multiplicador fijo (`MUSIC_VOLUME`) sobre lo que trajera el mp3: el nivel dependía del máster de la canción. Normalizar primero (`services/musica.py`, -24 LUFS) hace que el ajuste signifique "cuánto por debajo de la voz". Elegido -4 dB.
- [2026-08-06] `alimiter` SIN `level=disabled` re-nivela hacia arriba: el renderer de Viralización llevaba `limit=0.95` a pelo y los vídeos salían a +0,07 dBTP (recortando) en vez de limitados. Ya estaba anotado para el POV BOF y se repitió aquí.
- [2026-08-06] `voz_limpia.limpiar_en_sitio` usaba `shutil.copy2`, que copia metadatos y falla con EPERM cuando el fichero es de otro usuario (host uid 1000 vs container uid 999). Con `copyfile` funciona. Y OJO: hace el respaldo ANTES de separar, así que "tiene respaldo" NO significa "está limpio" — hay que comparar el contenido.
- [2026-08-06] `scripts/viralizacion_previews.py` necesita el `work_root` REAL: la ruta del gancho pre-cortado sale de la caché en disco, no del escaneo. Pasarle `API_TEMP_ROOT` lo manda a una caché vacía, re-escanea y el render muere con `-i None`.
- [2026-08-06] [Viralización] El estilo B pasa de palabra-a-palabra a frases de MÁX. 3 palabras en Playfair Display Black: con una sola palabra no se coge el sentido de la frase. Ojo al cuerpo — con el tamaño base ocupaban un tercio del ancho y en la referencia ocupan la mitad (hace falta 1.9x).
- [2026-08-06] [Viralización] Las versiones claras (A2/B2/C2) recuperan la VIÑETA (bordes oscuros, PI/3.6) pero no el resto de la suciedad. "Limpio" para el operador es sin polvo/rayaduras/grano, no sin bordes.
- [2026-08-06] [Viralización] El CTA final se nivela al MISMO objetivo que la voz (`voz_limpia.LUFS_OBJETIVO`, -11 LUFS), importando la constante en vez de copiar el número: con -12,5 a mano sonaba por debajo de Pablo.
- [2026-08-06] [Viralización] "Rondas con música = 1" NO significaba "todos": con 1 ronda por audio lo era, pero al pedir más vídeos que audios la mitad salían sin música en silencio (40 vídeos / 23 audios → 23 con música). Sustituido por Sin música / A la mitad / A todos, igual que el CTA. Misma familia de bug que `ronda % 2` del CTA: contar RONDAS cuando lo que importa son VÍDEOS.
- [2026-08-06] [Viralización] En el selector de audios la lista VACÍA significa "todos" (`_audios_de`), así que desmarcar todas las casillas daba todos los audios — lo contrario de lo que se ve. Hace falta un centinela (`AUDIO_NINGUNO`) para el estado "ninguno marcado", que se limpia al enviar y bloquea el submit.
- [2026-08-06] Un `page.tsx` de Next.js NO admite `export const` sueltos: el type-check de rutas falla con "not assignable to type never".
- [2026-08-06] [Viralización] Las tandas se serializan (`_EXCLUSIVE_MODES`): con 2 workers arrancaban dos en paralelo y (a) en 4 vCPU no acaban antes, acaban las dos a la mitad de velocidad, y (b) comparten banco de ganchos — la segunda lo agota, recicla y repite planos que la primera acaba de gastar. El mecanismo ya existía para quitar-marca; solo había que registrar el modo.
- [2026-08-06] [Viralización] Un vídeo de cada tanda larga moría con `ffmpeg exit 234`: el xfade por tandas reagrupa los parciales cuando salen más de `XFADE_MAX_INPUTS` grupos, y esa segunda vuelta reusaba los nombres de la primera (`xfade_t00`…), así que el grupo 0 escribía SOBRE SU PROPIA ENTRADA. Pasa a partir de ~50 tramos, o sea solo en los vídeos largos. El nombre lleva ahora el nivel de recursión.
- [2026-08-06] `queue_state.json` NO refleja el progreso: solo se reescribe al cambiar de estado. Leerlo con `leer_jobs` para ver "cómo va" hace creer que un job lleva 80 min al 0% cuando va por el vídeo 21. Para progreso real, los logs del contenedor o la API.
- [2026-08-06] [Viralización] Las ventas se atribuyen a un NICHO que elige el operador (`nicho` en el doc de `vendido:`), con UN índice compartido: así la vista mezclada y el filtro son una sola lectura. Adivinar el nicho no vale — el mismo producto se graba con varios.
- [2026-08-06] [Nicho POV BOF] «Mis productos» es una tercera FUENTE, no un nicho aparte: las fotos que sube el operador se guardan con el convenio de nombres del Drive del curso (`3.png` limpia, `3(1).png` ficha) y así emparejado, textos, escaparate, vendidos y montaje funcionan sin una línea extra. Inventar otro convenio se paga en los seis sitios que leen fotos.
- [2026-08-06] Las fuentes propias llevan la RUTA del fichero como `id` de foto (no hay ID de Google). `fetch_photo` las detecta porque empiezan por "/" — un ID de Drive nunca lo hace.
- [2026-08-07] [Nicho POV BOF] Las fotos de «Mis productos» se veían CAMBIADAS: el endpoint `/photo` cachea 24h porque "un file ID de Drive es inmutable", pero en la fuente propia el id es la RUTA y la ruta se REUTILIZA (borras un producto, subes otro, misma URL). El servidor mandaba lo correcto; era el navegador. `no-cache` solo para la fuente propia.
- [2026-08-07] [Creativos Pro] Nicho sin vídeo: comparte TODO el catálogo del POV BOF (fuentes, fotos, textos, hashtags, escaparate, vendidos) y solo tiene lo suyo — prompt, formato 3:4 y progreso de carpetas. Su router son 3 endpoints; el resto de la pantalla usa los del POV BOF. Duplicar productos habría significado extraer los textos dos veces con Gemini y que las copias se separaran a la primera corrección.
- [2026-08-07] [deploy] Dos push seguidos = el segundo se PIERDE en silencio: `deploy_safe.sh` usa `flock -n` y el que llega tarde sale con "otro deploy está en marcha" sin reintentar. Ahora, al terminar, compara `origin/main` con `HEAD` y se relanza (tope 3 vueltas). Comprobar si algo está desplegado se hace mirando el commit del SERVIDOR, no con `pgrep` (que se encuentra a sí mismo) ni grepeando `/app/.next` (hay que mirar `/app/.next/static`).
- [2026-08-07] [deploy] Un commit vacío NO sirve para relanzar un cambio de frontend: `deploy_safe.sh` solo reconstruye la web si cambió algo en `frontend/`. Hay que forzar `docker compose up -d --build --no-deps web`.
- [2026-08-07] [frontend] El atributo `download` de un `<a>` se IGNORA si la URL es de otro origen — y la API lo es. Una descarga masiva apuntando al endpoint de ver (`/photo`) abre pestañas en el móvil y no baja nada; hay que ir al endpoint que pone `Content-Disposition: attachment` (`/foto-limpia`, ahora con `variante=limpia|ficha`).
- [2026-08-08] [apk] Para envolver la web en APK se eligió TWA (Bubblewrap) y NO Capacitor: una TWA es Chrome, un Capacitor es un WebView y ahí el `<a download>` NO descarga nada (documentado en planificador-viajes y fitness-life, que lo esquivan abriendo el navegador del sistema). Esta app hace descargas en cadena, así que el WebView la rompía.
- [2026-08-08] [frontend] `.gitignore` tiene un `*.png` global: cualquier icono/asset PNG nuevo hay que meterlo con `git add -f` o el deploy construye la web sin él y el manifest apunta a 404.
- [2026-08-08] [apk] Bubblewrap: `splashScreenFadeOutDuration` es OBLIGATORIO en twa-manifest.json — si falta escribe `splashScreenFadeOutDuration: ,` en app/build.gradle y Groovy falla con «Unexpected input: ','», sin nombrar el campo. Se reproduce en local con `docker run node:20 … bubblewrap update` en vez de esperar 30 min al CI.
- [2026-08-08] [ci] Un workflow que crea releases necesita `permissions: contents: write`: el GITHUB_TOKEN por defecto es de solo lectura y falla con «HTTP 403: Resource not accessible by integration» al final del todo, con el artefacto ya construido.
- [2026-08-08] [apk] «Se reinicia en segundo plano» en una TWA es Android matando el proceso: no hay ajuste de manifest que lo impida (`alwaysRetainTaskState` YA lo pone Bubblewrap — comprobado en el manifest generado antes de escribir un parche que habría sido un no-op). Se compensa desde el frontend guardando la última ruta y volviendo a ella al arrancar, y en el móvil quitando la optimización de batería.

- [2026-08-10] [drive] Cachear no basta cuando el TTL es corto y la ruta es del mount de rclone: «Mis productos» seguía costando 20-37s a quien entrase pasados los 2 min de TTL, y en frío está SIEMPRE justo tras un deploy (el `--dir-cache-time` del mount es de 30s, así que rclone re-resuelve la ruta honda contra Google). Se arregla precalentando desde fuera —tarea de fondo al arrancar la API y cada 10 min— y subiendo el TTL a 15 min como red de seguridad. Dos trampas: refrescar llamando al getter cacheado NO renueva la caducidad (hay que tirar la entrada antes), y el `iterdir()` del mount va en `to_thread` o bloquea el loop de asyncio decenas de segundos.
- [2026-08-10] [apk] «La APK se cierra sola» no era Android: era Chrome quedándose sin memoria. Lo que ocupa en el móvil no es el fichero sino el BITMAP (ancho × alto × 4 bytes), así que una ficha de 1320×2868 son 15 MB y una carpeta de 10 productos pedía ~300 MB. Se sirven encogidas con `?w=` (400 px en cuadrículas, 900 en el visor) → 1,4 MB por foto. Regla general: nunca pintar en móvil una foto de Drive a tamaño original, aunque el fichero pese poco (un PNG de 0,5 MB descomprime a 15).
- [2026-08-10] [POV BOF Largo] Unificada su pantalla con la del POV BOF (mismo catálogo/carpetas, progreso INDIVIDUAL): la página pasa de 401 a paridad completa (fuentes+mis productos, barra de carpetas con completar/ver todas, config EchoTik/hashtags/backup, extraer textos + buscar enlaces + prompts + descargas, vendidos, escaparate, tarjeta con guion + DOS clips). Regla de reparto: lo COMPARTIDO (textos, enlaces, fotos, mis productos, backup, hashtags, EchoTik) reusa los endpoints del POV BOF; el PROGRESO (carpeta hecha, escaparate, subido, vendió, guion, clips, vídeo) es propio del Largo (`nicho_pov_bof_largo:` + `progress_repo` nuevo + `/complete` + `/producto/estado`). El ranking de vendidos es el único índice transversal: se apunta con `nicho="pov_bof_largo"`. `EscaparateModal` se parametrizó con `marcarEstado` opcional (el POV BOF sigue igual por defecto). No se fusionaron menús ni se tocó la página del POV BOF.
- [2026-08-10] [POV BOF Largo] El alargue de duración se reparte entre los DOS clips en vez de caer todo al final. Antes se cuadraba el vídeo entero (clip1+clip2) contra la voz, así que con una voz de 25s el rebobinado se acumulaba sobre el segundo clip (10s + 15s). Ahora `pipeline/video_editor._concatenar_cuadrado` cuadra cada clip a `audio/N` (misma técnica `match_video_to_audio`) antes de pegar → dos de ~12,5s. No se toca el prompt ni la velocidad: los guiones del curso van tal cual y los vídeos de ~25s se aceptan.
- [2026-08-10] [POV BOF Largo] Se recortan silencios de la voz Fish para no alargar el vídeo (`config.VOZ_SILENCIO` + `voz._nivelar`): `silenceremove` quita el aire de principio/final y CAPA las pausas internas largas a ~0,3s (stop_silence) en vez de eliminarlas, con `detection=peak` a -40dB para no comerse arranques suaves de palabra. Va primero en la cadena para que loudnorm mida el audio ya ajustado. Antes NO se cortaba nada (solo loudnorm+limiter).
- [2026-08-10] [POV BOF Largo] Vendidos y escaparate son GLOBALES, no por nicho. Vendidos: se quitó el filtro `nicho=pov_bof_largo` (el índice `vendidos:index` ya era único y compartido) → el mismo ranking se ve en POV BOF y en Largo. Escaparate: nuevo índice `nicho_pov_bof_largo:escaparate[:usuario]` (SET) por CLAVE `norm(tienda)|norm(titulo)` en vez de un flag por producto/carpeta — así el mismo producto repetido en varias carpetas del Drive aparece marcado en todas si se metió en una. `_listar` lee en_escaparate del índice; `/producto/estado` lo escribe con `set_escaparate`. Normalización: NFKD sin acentos + minúsculas + espacios colapsados (coincidencia "exacta" tolerante a acentos/espacios).
- [2026-08-10] [POV BOF Largo] El corte entre los dos clips cae en una minipausa de la voz, no a mitad de palabra. `pipeline/video_editor._punto_de_corte` corre `silencedetect` (noise=-35dB:d=0.12) sobre la voz ya nivelada/trimmeada y elige el silencio cuyo centro esté más cerca de la mitad, acotado a [30%,70%] para que los clips no queden muy desiguales; sin pausa aprovechable parte por la mitad. Cada clip se cuadra a su objetivo (primero=corte, segundo=resto) antes de pegar.
- [2026-08-11] [POV BOF Largo] Re-subir clips a un producto YA montado duplicaba el encolado Y montaba con material mezclado: `upload_clip` encolaba en cuanto había `clip1_path` y `clip2_path` en Redis, y esos paths se quedaban guardados para siempre tras el primer montaje → subir el clip 1 nuevo encolaba al instante (clip1 nuevo + clip2 VIEJO) y subir el clip 2 encolaba otro. Visto en producción con los productos 6/9/10 (2 jobs cada uno; el bueno iba último y pisaba al malo, así que solo se perdía render). Fix: (1) al encolar se limpian los paths (`clip1_path=""`) — los clips ya viajan en `job.params`, así que el montaje en curso no se entera y la ronda siguiente vuelve a pedir los DOS; (2) un clip guardado solo cuenta si su fichero EXISTE y su mtime es posterior a `video_listo_at` — arregla solos a los productos heredados sin migración y cubre la purga de `api_uploads` a las 24h; (3) guarda anti-duplicado si ya hay job pending/running de ese producto (subida simultánea de los dos slots).
- [2026-08-11] [APK] "La app se cierra al volver" NO era memoria nuestra: el chivato lo cazó con datos — `estaba_en_pantalla:false`, `memoria_mb:7` de 2855, `descartada:false`, 27 min de fondo → Android matando el proceso (política de batería/OEM), la causa nº2 de APK.md. La de las fotos ya estaba resuelta y se confirmó midiendo Caddy: hoy 71 fotos servidas CON `?w=` y 0 sin él (antes de ayer, todas completas y de hasta 4 MB). Optimizaciones hechas igualmente para que el proceso pese menos y sea peor candidato a morir: (1) `ANCHO_CHIP=160` para los sellos de 44-64 px, que pedían 400 px — el bitmap es ancho×alto×4, así que una carpeta pasa de ~28 MB a ~4 MB de imágenes; (2) el WebSocket de la cola se suelta al minuto de esconderse la app y reconecta al volver (el servidor manda `snapshot` al reconectar, no se pierde nada). Y el chivato pasa a fichero en el volumen `temp_work` — vivía en memoria y un deploy borró el histórico justo cuando hacía falta (`logs/` del host NO vale: está montado read-only).
- [2026-08-11] [Escaparate] El escaparate pasa a ser ÚNICO por producto y compartido entre nichos y carpetas: índice `escaparate:index[:usuario]` con clave `norm(tienda)|norm(titulo)` en `nicho_pov_bof/repos/product_repo.py` (mismo sitio que los vendidos, por transversal). Motivo: al Marketplace el producto se sube UNA vez, pero el mismo producto sale repetido en varias carpetas del Drive del curso y se graba con varios nichos. Sigue siendo POR USUARIO (Ana y Mauro son otras personas con su cuenta). Enganchado en POV BOF (lista, buscador y `/producto/estado`), Largo (absorbe su índice propio anterior; se lee para no perder lo marcado y se limpia al desmarcar), Creativos Pro (usaba ya los endpoints del POV BOF → salió gratis) y BOF Cine (schema + lista + `/producto/estado` nuevo). Sin textos extraídos la clave es vacía y no se deduplica, a propósito.
- [2026-08-11] [Escaparate] Gorras y Ropa Sin Personas se enganchan al índice común, pero con dos diferencias frente a BOF Cine: (1) sus endpoints de lista NO recibían `usuario` (el índice es por usuario) → hubo que añadir `Depends(get_web_user)` a `/gorras`, `/prendas` y a los `/extraer-textos` que reconstruyen la lista; (2) sus fotos no se sirven por `source/folder/file_id` sino por file ID de su propio Drive, así que al `EscaparateModal` se le pasan `fotoUrl`/`descargaUrl` (las props que ya estaba parametrizadas) y `source=""`, `folder=carpeta`.
- [2026-08-11] [Escaparate] Ropa Con Personas y Cuenta Piloto al índice común. Lo no obvio de cada uno: en Ropa Con Personas hubo que pasar `usuario` también a `extraer-textos` y `prenda/titulo`, porque reconstruyen la lista llamando a `list_prendas` y sin él el escaparate salía vacío justo después de tocar los textos. En Cuenta Piloto el producto se identifica por `id` y no por `producto`, así que al `EscaparateModal` se le mapean los items (`{...p, producto: p.id}`) y el request se sigue llamando `producto` para no inventar un nombre distinto al del resto de sus endpoints; y `_a_schema` recibe el índice ya leído (`escaparate: set[str] | None`) en vez de consultarlo por producto, que serían N viajes a Redis por listado.
- [2026-08-11] [APK] Que Android mate la app no se puede evitar, pero sí que duela: `RestaurarPantalla` devolvía a la PANTALLA y la fuente/carpeta abiertas vivían en estado de React, así que al reiniciarse aparecías en la carpeta por defecto y había que buscar por dónde ibas (el operador pierde ahí el tiempo copiando guiones producto a producto). `lib/hooks/useEstadoRecordado.ts` es un `useState` que persiste en localStorage (guarda al cambiar, NO al salir: cuando el sistema mata el proceso no llega `beforeunload` ni `pagehide` fiable) y se usa en las 7 pantallas de nicho con clave propia por nicho. Ojo con la hidratación de Next: el valor inicial tiene que ser el mismo que en el servidor y lo guardado se aplica en el primer efecto, con una guarda para que ese primer render no pise lo guardado.
- [2026-08-11] [APK] Segundo golpe al reinicio de Android: `lib/cache-persistente.ts` guarda en localStorage los listados de nicho que van llegando (solo esos prefijos; nada de cola ni sesión) y los vuelca en la caché de React Query al arrancar, así la pantalla sale PINTADA en vez de esperar otra vez al Drive; React Query refresca por detrás porque el `staleTime` es corto. Guardas: 6h de frescura (mejor esperar que enseñar lo de ayer), 150KB por entrada y 40 entradas máx. con poda de las más viejas (localStorage son ~5MB y llenarlo rompería también el progreso y la sesión), y lo ya cargado manda sobre lo guardado. 5 tests en `tests/cache-persistente.test.ts`.
- [2026-08-11] [Vendidos] El ranking se saca a `components/tiktok-shop-ai-pro/VendidosModal.tsx` y se pone en los 8 nichos: el índice es único y global, así que el dato es el mismo se mire desde donde se mire (y es el que dice qué producto buscar). El buscador solo se enseña donde el catálogo es el del POV BOF (`conBuscador`); en gorras/ropa/cuenta piloto no aplica. `useVendidos` sin `source` ahora devuelve TODOS en vez de no llamar. De paso se borran ~21k caracteres de modal duplicado entre POV BOF y Largo.
- [2026-08-11] [Escaparate] Error 500 al marcar el escaparate ("Unexpected token 'I', Internal S... is not valid JSON" en la app = el front intentando parsear un 500 de texto plano): `_producto_info` de `nicho_pov_bof/productos.py` usaba `product_repo` y esa función NO lo importa (en ese fichero el repo se importa DENTRO de cada endpoint). Un `ast.parse` no lo caza porque el NameError es en tiempo de ejecución. Se añadió el import local y, de paso, un escáner con ámbitos reales (`except as`, cierres, `with`) que barrió todo `src/`: solo quedaron falsos positivos de `from moviepy.editor import *` y un `Any` en anotación con `from __future__ import annotations` — más un fallo latente REAL heredado: `products.py` llamaba a `logging.getLogger` en `_generate_presets_background` sin importarlo (revienta solo cuando hay warnings).
- [2026-08-11] [Vendidos] Salían vacíos: el modal recibía `source` y filtraba por la fuente abierta, cuando el ranking es GLOBAL (lo pidió el operador explícitamente). Fuera el filtro en las 4 pantallas que lo pasaban. Además la caché nueva los congelaba: `setQueryData` sin `updatedAt` marca el dato como recién traído y React Query NO refresca hasta pasado el `staleTime`; ahora se rehidrata con la fecha original y no se guardan listas vacías (no se distinguen de "aún no ha cargado").
- [2026-08-11] [Creativos Pro] Botón "Subido" propio, marcado a mano (`nicho_creativos/repos/subidos_repo.py`, SET `subidos:<source>:<carpeta>[:usuario]` + `GET /subidos` y `POST /subido`). NO se comparte con el POV BOF a propósito, al revés que escaparate y vendidos: allí "subido" es el VÍDEO y aquí el CREATIVO — dos publicaciones distintas del mismo producto. Como la lista de productos es la del POV BOF, el flag va en un endpoint aparte (un set de números de producto) y la pantalla junta ambos, en vez de meter en `ProductoInfo` un campo que solo entiende un nicho. De paso se quita también aquí la pregunta de "¿con qué nicho vendió?", que quedaba viva solo en Creativos.
- [2026-08-11] [Escaparate] "No se guardan las marcas": eran DOS cosas. (1) Las marcas ANTERIORES al índice único vivían solo en el flag `en_escaparate` de cada producto; el POV BOF las seguía viendo (lee flag OR índice) pero los demás nichos leen SOLO el índice, así que allí salían sin marcar — 119 marcas invisibles. Migradas con `scripts/migrar_escaparate.py` (idempotente, solo añade; el índice pasó de 10 a 118 entradas: menos de 119 porque varias carpetas repiten el mismo producto, que es justo la deduplicación funcionando). (2) `/producto/estado` del POV BOF sacaba tienda/título del RETORNO de `update_product`, y como los tres campos que toca son privados, a un usuario que no sea `ness` le vuelve solo lo suyo, SIN título → clave vacía → la marca se perdía en silencio (Ana y Mauro no podían usar el escaparate). Ahora se leen aparte con `get_product` y, sin textos, se responde 400 claro en vez de fingir que se guardó.
- [2026-08-11] [Viralización] Estilos: fuera los tres "sucios" (A/B/C con polvo de celuloide y grano, no viralizaban), las limpias A2/B2/C2 pasan a A/B/C y entran D/E/F "Reflexión" (`build_ass_reflexion`): patrón de B pero en MINÚSCULAS, frase partida en dos líneas y RESPLANDOR difuminado en vez de contorno duro. El resplandor tiene que ir en DOS capas ASS (halo en `layer=0`, letra nítida en `layer=1`): con una sola capa (`\bord` grueso + `\blur` sobre el propio texto) el difuminado se come el filo y sobre paisaje claro queda ilegible — se probó y se descartó. Con halo BLANCO hace falta además una sombra oscura mínima o el blanco sobre fondo claro se derrite. Método que funcionó para elegir: renderizar muestras con libass sobre un FOTOGRAMA REAL de paisaje y con el filtro claro aplicado, subirlas al Drive del operador y que él elija por número; nada de describir tipografías por escrito. Quitar claves de `STYLE_PRESETS` es seguro: `resolve_style`/`distribute_styles` caen a la rotación si una tanda guardada trae una clave retirada.
- [2026-08-11] [Viralización] Regenerar las miniaturas de estilos fallaba con un críptico "Error submitting a packet to the muxer: Input/output error" de ffmpeg: los .jpg de `frontend/public/viralizacion/previews/` eran propiedad del uid 999 (los escribió el contenedor), y el script corre como `nebulabsai` — la carpeta es escribible, así que se pueden CREAR ficheros pero no SOBRESCRIBIR esos. Se borran antes de regenerar. Ojo también con `pgrep -f <script>`: el propio comando de espera contiene el nombre y se matcha a sí mismo, así que parecía que el proceso seguía vivo cuando llevaba media hora muerto (usar `pgrep -af "python3 scripts/..."` y mirar la línea completa).
- [2026-08-11] [Viralización] Los estilos de reflexión partían la frase en dos líneas SIEMPRE (lo forzaba `build_ass_reflexion` bajando la última palabra) y por eso la letra parecía más grande que la de B: dos renglones cortos ocupan mucho más que uno. Ahora va en una línea y libass parte solo si no cabe (`WrapStyle: 0` + margen). Para calibrarlo se midió de verdad, no a ojo: 377 frases reales sacadas de las transcripciones cacheadas (`_cache/transcripts/*/*.json`), renderizadas una a una con libass sobre negro y midiendo la caja del texto con PIL (alto de la caja > 1,45× el cuerpo = dos líneas). Resultado: B parte 4 de 24 y los nuevos 3 de 24 con cuerpo 115/105 y margen 125 px. El margen no baja de ~110: ahí el texto empieza a rozar la botonera de TikTok. Al cambiar un estilo hay que regenerar su miniatura (`scripts/viralizacion_previews.py <clave>`), que si no enseña la versión vieja.
- [2026-08-11] [Viralización] El cuerpo de letra de los estilos de reflexión NO es lo que hace que la frase se parta en dos líneas: medido a tres cuerpos distintos (115/122/129 px), siempre se parten las MISMAS 3 de cada 20 frases — lo que manda es lo larga que sea la frase, no el tamaño. Así que quedarse corto de cuerpo solo restaba presencia en pantalla: se subieron a 129 px (119 en PT Serif, más ancha de trazo), igual que B, y con la frase de la referencia del operador ocupan 69-71% del ancho, clavado a los vídeos que le funcionan (~72%).
- [2026-08-12] [Creativos Pro] Tarjetas que mentían: salían productos marcados en el escaparate sin estarlo (y la cabecera decía "8 sin meter" a la vez). No era dato, era React: la tarjeta guardaba `useState(p.en_escaparate)` y `useState(p.sold)`, y `useState` solo mira su valor inicial en el PRIMER montaje. Como la `key` era el número de producto (1..10 en TODAS las carpetas), al cambiar de carpeta React reutilizaba el componente y conservaba el estado de la carpeta anterior. El POV BOF no lo sufría porque ya resincronizaba con un `useEffect`. Arreglado igual + `key` con source y carpeta. Test en `tests/tarjeta-estado.test.tsx` que reproduce el fallo y verifica el arreglo. Regla: si un componente de lista copia props a estado local, o resincroniza con `useEffect` o la `key` tiene que ser única de verdad.
- [2026-08-12] [Cuotas] Contador diario de publicaciones (`src/cuotas/`, prefijo `cuotas:`): tope de la CUENTA de TikTok, no de un nicho — 25 vídeos y 50 carruseles al día, con aviso en ámbar a los 20/45 porque el operador quiere frenar con margen. Se guarda `referencia -> hora` en vez de un número: marcar dos veces no suma dos, desmarcar resta de verdad y queda la HORA (que es lo que deja comprobar si un producto repetido se marcó bien). La fecha va en la CLAVE, así que se reinicia solo a medianoche sin tarea programada; la fecha se calcula en `Europe/Madrid` porque el servidor va en UTC y en verano a las 00:30 seguiría contando en el día anterior. `ajuste_<tipo>` suma lo subido fuera de la app. Los carruseles son los "Subido" de Creativos Pro. Y se quitó el `uploaded=True` que ponían los 5 runners al acabar el montaje: montar no es publicar, y con eso el contador no valía nada.
- [2026-08-12] [Textos] Una carpeta entera salió con los títulos CORRIDOS una posición (cada producto con el título del siguiente, y el último duplicando al primero). No era el emparejado de fotos: `extract_from_pairs` manda las 8-10 capturas en UNA llamada y el identificador viaja SOLO en el texto del prompt ("imagen 1 = primer identificador…"), así que si el modelo se desalinea, se desalinea la carpeta entera. Arreglo inmediato sin desplegar: volver a pulsar "Textos" (salió bien a la segunda). Arreglo de raíz: `_sellar_id` escribe `#<producto>` en una banda blanca sobre cada captura antes de mandarla y el prompt dice que use ESE número e ignore el orden — el identificador va dentro de la imagen y no hay orden que perder. Síntoma para detectarlo: un título repetido dentro de la misma carpeta (ojo, en carpetas de colchones puede ser legítimo).
- [2026-08-12] [Cuotas/UI] Tres remates del contador: (1) la hora de "Subido" se sella en el servidor y se pinta DENTRO del propio botón (POV BOF, Largo y Creativos) — en Creativos el repo pasó de SET a `producto -> hora` para poder enseñarla; (2) la barra de cuota se refrescaba sola cada minuto pero no al marcar: ahora cada mutación de "Subido" invalida `["cuotas","hoy"]` y sube al instante; (3) EchoTik se esconde tras una bandera `MOSTRAR_ECHOTIK = false` en las dos pantallas (panel de credenciales + los dos botones de buscar ficha) en vez de borrarlo — su cuota gratis no da para el volumen diario, pero volver a encenderlo es una línea.
- [2026-08-12] [Creativos Pro] El "Subido" se perdía al recargar: al pasar el repo de SET a JSON (`producto -> hora`) las claves ya existentes seguían siendo de tipo SET en Redis, y un `SET` sobre una clave de otro tipo lo rechaza Upstash → el wrapper se comía el error y `set_json` devolvía False sin que nadie lo mirara. Resultado: la pantalla marcaba, el servidor no guardaba y al recargar no estaba. Migradas las 3 claves vivas (SMEMBERS → DEL → SET json, con la hora actual porque el formato viejo no la guardaba) y ahora `marcar()` comprueba el retorno y lanza si la escritura no confirma. REGLA: cambiar el TIPO de dato de una clave de Redis obliga a migrar las existentes; y una escritura que falla en silencio es peor que un error, porque la UI pinta un estado que no existe.
- [2026-08-12] [UI] La barra de cuota no se veía al hacer scroll en el móvil: era `sticky top-0 z-30` y la cabecera móvil del Sidebar es `sticky top-0 z-40` con 3.5rem + safe-area, así que la tapaba. Ahora la barra se pega en `top-[calc(3.5rem+env(safe-area-inset-top))]` y `md:top-0` (en escritorio no hay cabecera). Además del color, al cruzar el umbral salta un toast UNA vez por tipo y día (guardado en un ref con clave `fecha:tipo:umbral`; sin eso, cada refresco del contador lanzaría otro).
- [2026-08-12] [UI] `position: sticky` no servía para la barra de cuota: `main` lleva `overflow-y-auto` pero SIN altura acotada, así que quien hace scroll es la página y el sticky se iba con el contenido (se veía flotando a media pantalla y desaparecía al bajar). Pasa a `fixed` + un hueco del mismo alto en el flujo; en móvil anclada bajo la cabecera (`top-[calc(3.5rem+env(safe-area-inset-top))]`) y en escritorio a la derecha de la barra lateral (`md:left-64 md:top-0`). Antes de darlo por bueno hay que comprobar que NINGÚN ancestro tenga transform/filter/backdrop-blur: eso crea un bloque contenedor y el `fixed` dejaría de anclarse a la viewport (aquí el `backdrop-blur` está en el modal de login y en la cabecera, que son hermanos, no padres).
- [2026-08-12] [Vendidos] El número del botón no cuadraba con el listado (ponía 2 y al abrirlo salían 43): al hacer el ranking global quité el filtro por fuente del MODAL pero no del contador del botón, que seguía en `useVendidos(source)`. Regla: cuando un botón resume una lista, los dos tienen que leer de la MISMA query; si no, el resumen miente hasta que lo abres.
- [2026-08-12] [POV BOF] Guion de plazos (Klarna) para productos caros: el extractor pide ahora `precio` y, por encima de `PRECIO_MIN_PLAZOS` (40 €), el producto pasa a dos clips + voz de Fish con uno de los 5 guiones del curso (`prompts/guiones_plazos.md`). Dos trampas reales: (1) Gemini devuelve el precio como NÚMERO la mitad de las veces y el filtro de opcionales solo dejaba pasar `str`, así que se tiraba en silencio; (2) `float("1.299,00")` revienta y un sofá caro caía a 0 € — o sea, al guion equivocado justo en el producto que más pide el nuevo. Hay `config.precio_num()` con el último separador como decimal y la regla de 3 cifras para los miles (19 casos probados).
- [2026-08-12] [POV BOF] Los paths de los dos clips van en `CAMPOS_PRIVADOS` del `product_repo`: son del USUARIO, no del producto. En el documento compartido, el clip 1 de ana habría disparado el montaje de ness con material mezclado. Mismo motivo por el que un clip solo cuenta si su `mtime` es posterior al último `video_listo_at` (si no, resubir el clip 1 encola un montaje con el clip 2 de la ronda anterior).
- [2026-08-12] [Extractor] El precio salía en el 6% de los productos y no era culpa del modelo: el bloque "Formato de salida" del prompt listaba 5 campos SIN `precio` y remataba con "No añadas claves extra". Gana el ejemplo, no la descripción de más arriba. Al meterlo en el JSON de ejemplo: 8/8 en una carpeta real. Regla: cuando añadas un campo a un prompt de extracción, tócalo en los DOS sitios (la descripción y el esquema de salida) o el nuevo campo no existe.
- [2026-08-12] [Extractor] TikTok pinta el precio partido (`619` grande + `,00 €` pequeño), a veces como rango (`175,18 - 403,54 €`) y casi siempre con una línea `Paga X con cupones` debajo. Lo que decide el guion de plazos es lo que se PAGA (el de cupones), no el de arriba: con cupones por debajo de 30 € Klarna no financia y el guion prometería algo falso. Se guardan los dos (`precio` y `precio_lista`) y la ficha enseña el tachado para que se vea por qué un producto de 34,70 € no lleva plazos.
- [2026-08-12] [POV BOF Largo/Plazos] El cambio de clip caía a mitad de palabra: `_punto_de_corte` solo miraba silencios dentro de [30%, 70%] de la voz y, si no había ninguno ahí, partía por la mitad EXACTA. Medido con una voz real de Fish (13,42s): las pausas eran 0,72s al 20%, 0,50s al 60% y 0,15s al 66% — en cuanto la única larga cae fuera de la ventana, el corte se va al centro, que suele ser mitad de frase. Ahora manda que sea pausa de verdad (≥0,22s) en una ventana ancha (18-82%) y solo se acepta un hueco corto si no hay ninguna, ya centrado. La ventana no se abre más: un corte al 10% deja el 90% de la voz sobre el segundo clip a base de rebobinado y ahí sí se nota.
- [2026-08-12] [POV BOF Largo/Plazos] Las pausas hay que medirlas sobre la voz YA PROCESADA, no sobre el mp3 de Fish. La cadena del mux (`acompressor` + `speechnorm=e=8`) levanta los tramos bajos: medido en un audio real, una pausa de 0,72s queda en 0,35s, una de 0,50s en 0,11s y una de 0,15s desaparece. Eligiendo sobre el crudo, el corte caía en huecos que al oírlos son mitad de frase — el operador lo cazó dos veces. `_voz_como_se_oye()` rinde la voz con la cadena IMPORTADA de `nicho_pov_bof.video_editor` (no copiada: si allí cambia el compresor y aquí no, vuelve el bug en silencio). Y gana la pausa MÁS LARGA de la ventana, no la más centrada: un silencio grande algo descentrado se nota mucho menos que uno pequeño en el sitio.
- [2026-08-12] [Plazos] Sembrar el sorteo de guion/voz con la identidad del PRODUCTO parecía buena idea (remontar da lo mismo) y en la práctica fue lo contrario de lo que se quería: el operador probó dos veces el mismo producto y le salió la misma voz. Semilla por `job.id` — variedad en cada montaje y sigue siendo reproducible desde el log.
- [2026-08-12] [POV BOF Largo] La versión de plazos NO es otro guion: es el mismo prompt del curso con un bloque extra (`prompts/guion_plazos.md`) que pide una frase de financiación. Comparados los dos, el guion fijo de plazos solo aporta 71 caracteres ("y podrás financiarlo en cómodos plazos en pedidos superiores a 30 euros") y a cambio pierde todo lo del producto (dolor, identificación, beneficio) — por eso en el Largo se añade al guion bueno en vez de sustituirlo. `guion_plazos` marca en qué modo se escribió: si el precio cambia de lado del umbral, el guion guardado ya no vale y se reescribe.
- [2026-08-12] [Guiones] Ni el prompt del curso (260 car) ni el bloque de plazos (360) consiguen que Gemini respete el número: medido, salen 359 sin plazos y ~456 con. Son ~25s, que es lo que ya duraban los vídeos. No se ponen reintentos de recorte a propósito — ya se probó y deja frases telegráficas; el montaje cuadra la duración.
- [2026-08-12] [Plazos] El guion se sorteaba dentro del runner, así que el operador se enteraba de qué decía la voz con el vídeo ya montado. Ahora se sortea desde la ficha (`POST /producto/guion-plazos`, cero coste: son cinco textos fijos), se ve en un recuadro como el del POV BOF Largo y hay "Otro guion" que EXCLUYE el actual — con cinco opciones, repetir el mismo al pedir otro parecería que el botón no hace nada. El runner respeta el guardado y solo sortea si no hay ninguno.
- [2026-08-12] [POV BOF Largo] Al añadir el modo plazos, los guiones YA escritos quedaron desfasados: el badge decía "Guion con plazos" y el texto no mencionaba la financiación. El runner ya los reescribe al montar, pero en pantalla no se veía y parecía que el vídeo iba a llevar ese texto. Regla para cambios de este tipo: si un dato guardado deja de cuadrar con el modo actual, la ficha tiene que DECIRLO (aquí, aviso ámbar + botón "Reescribir"), y el botón de lote tiene que contar los desfasados como pendientes — si no, dice "guiones al día" con media carpeta mal.
- [2026-08-13] [Top vendidos] Cuarta fuente de producto: los que YA vendieron, copiados a `TIKTOK_SHOP_AI_PRO/Top_Vendidos/Top N` (diez por carpeta) con el convenio de nombres de siempre. No hace falta código nuevo en los nichos: sale sola en POV BOF, Largo y Creativos porque los tres leen `/sources`, y con `propia: "1"` reusa la rama del Drive montado de "Mis productos" (ahora `drive_client._servicio_propio()` despacha entre las dos). Tres decisiones que no hay que deshacer: (1) el sitio de un producto NO cambia nunca aunque venda más — el progreso se guarda por (fuente, carpeta, producto) y moverlo lo perdería; el orden por ventas se hace al pintar. (2) Los textos se COPIAN del origen: ya pasaron por Gemini. (3) El botón "Vendió" en esta fuente le suma al producto de ORIGEN (vía el manifiesto `top_vendidos:manifiesto`), porque el ranking va por carpeta de origen y si no el mismo producto contaría dos veces.
- [2026-08-13] [Cuenta Piloto] Mandar varios vídeos a editar de una vez: el `<input multiple>` sube de UNO EN UNO a propósito (nueve ficheros grandes a la vez desde el móvil saturan la conexión y fallan a medias); la cola ya encadena los montajes. La variedad de rótulo/emoji/color NO hizo falta tocarla: el runner ya siembra con `v{n}` y salen 9 combinaciones distintas en 9 vídeos (medido). El contador "listos 4/9" se calcula por FECHA (vídeos con `at >= lote.iniciado`) en vez de con un contador propio, así no se descuadra si un montaje falla o se sube otro suelto por en medio; y el sondeo de la lista mira también la tanda incompleta, porque con los nueve en cola sin arrancar `montando` es falso y el contador se quedaba clavado en 0/9.
- [2026-08-13] [Top vendidos] El orden por ventas, el filtro "solo los que no he subido" y el badge de nuevo son de PANTALLA y viven en `frontend/lib/topVendidos.ts`, compartidos por POV BOF, Largo y Creativos: los tres enseñan el mismo dato y duplicarlo habría acabado en tres criterios distintos. El filtro se recuerda por nicho (`useEstadoRecordado`), porque si lo pones para ver lo que te queda por probar lo quieres igual la próxima vez. "Ya subido" lo decide cada nicho: en Creativos es el CREATIVO y en los de vídeo el vídeo, que son dos publicaciones distintas del mismo producto.
- [2026-08-13] [Cuenta Piloto] Voz PROPIA por operador: diez guiones (5 normales + 5 de plazos) por sexo, grabados desde la propia pantalla con `MediaRecorder` y guardados en el Drive del usuario (`Cuenta_Piloto/<usuario>/audios/<sexo>/normal3.mp3`). Sitios fijos: regrabar pisa el fichero y no hay contador que llevar. Todo se convierte a mp3 al subir —el navegador graba webm/opus— y va a un `.tmp` primero, para que un ffmpeg a medias no deje el audio anterior en cero bytes. Siempre hay salida de emergencia con `<input type=file accept=audio/*>`: en la app instalada el permiso del micro se puede haber negado y sin eso no habría forma de grabar. El montaje usa el audio propio y solo cae al banco compartido si no hay ninguno, avisando en el log.
- [2026-08-13] [Cuenta Piloto] Los textos de los 5 guiones normales NO estaban escritos en ningún sitio, solo grabados en `Nicho_POV_BOF/audios/`. Se recuperaron transcribiendo con faster-whisper y contrastando DOS voces distintas: hacía falta porque `hombre1_frase2.mp3` contiene en realidad la frase 1 (duplicado del banco) y con una sola voz habrían salido cuatro textos en vez de cinco.
- [2026-08-13] [API] Ninguna respuesta JSON llevaba `Cache-Control` y el WebView de la APK puede reutilizar la anterior por su cuenta: el operador añadía un producto en "Mis productos", la app volvía a pedir el listado (el POST y el GET SÍ están en el log) y le devolvían el de antes — parecía que no se había guardado, y estaba guardado (`5.jpg` en Drive). Middleware que pone `no-store` a lo que salga de `/api/` SIN cabecera propia; las fotos y vídeos fijan la suya (file ID inmutable, 24h) y no se pisan. Descartado por medición: no era el mount (un fichero escrito se ve en el listado a t+0s), ni varios workers (`API_WORKERS=1`), ni la caché en memoria de `mis_productos` (se invalida al escribir).
- [2026-08-13] [Top vendidos] Las UNIDADES no se copian a la carpeta: se leen del ranking al pintar, así que marcar una venta más se ve al instante y reordena sola. Lo que sí necesita el botón de sincronizar son los productos NUEVOS (hay que copiar sus fotos a Drive, segundos por producto). Para no tener que entrar a comprobarlo, `/sources` devuelve `pendientes` — una resta de dos lecturas de Redis, sin tocar Drive, para que se pueda pedir en cada carga— y se pinta como `+2` en el botón de la fuente.
- [2026-08-13] [Extractor] `_sellar_id()` guardaba la copia sellada (la que lleva `#<producto>` en una banda) AL LADO del original. En las fuentes del curso da igual —la foto ya es una copia temporal descargada del Drive—, pero en las nuestras el identificador de la foto ES su ruta en el Drive montado, así que las selladas se quedaban dentro de la carpeta del operador: 17 ficheros de basura y, peor, `photo_pairing` las cogía como FICHA del producto (Creativos Pro estaba usando la foto con la banda blanca encima). Ahora se sellan en `tempfile.gettempdir()`. Regla: nada que sea intermedio se escribe en una carpeta que el usuario ve.
- [2026-08-13] [Frontend] Los avisos rojos salían VACÍOS y por eso llevo todo el día sin poder diagnosticar los fallos que reporta el operador: `ApiError` se construía con `body.error` a secas, y no todas las respuestas tienen esa forma — FastAPI contesta `{"detail": …}` en los 422 de validación y Caddy devuelve HTML cuando la API está reiniciando (ahí, además, `JSON.parse` reventaba con un SyntaxError que subía en lugar del error real). `cuerpoDeError()` saca ahora un mensaje de lo que haya: `error`, `detail` (string o lista de validación, con el campo), texto plano, y si no hay nada, uno con el código HTTP que dice si ha sido el servidor. Probado con 6 formas de respuesta.
- [2026-08-13] [Vendidos] El botón "Productos que vendieron" enseñaba la suma de UNIDADES (48) y no el número de productos (30). Con "Top vendidos" delante eso pasó de ser un detalle a una contradicción visible: el operador contaba 48 y esperaba 5 carpetas de 10, cuando lo que hay son 30 productos → 3 carpetas. La etiqueta manda: si dice productos, cuenta productos; las unidades van al lado en pequeño.
- [2026-08-13] [Backup] Las copias de "Productos España" colgaban sueltas de la raíz del proyecto en Drive y en un mes eran siete carpetas de backup contra cinco de trabajo (en el móvil había que bajar media pantalla para llegar a TIKTOK_SHOP_AI_PRO). Ahora van todas dentro de `BACKUPS_Productos_Espana/`. Mover las existentes fue seguro porque el delta se calcula del SNAPSHOT guardado aparte, no listando las carpetas de copia — conviene no romper eso. El movimiento se hizo con `rclone moveto` (server-side en Drive: instantáneo y sin volver a subir los 2,46 GB), nunca por el mount.
- [2026-08-13] [Fotos] Las de "Mis productos" y "Top vendidos" cargaban lentas y a veces ni salían, y no era el servidor (medido: listar 0,01s, servir 0,00s, miniatura 0,12s la primera vez y 0 después). Iban con `Cache-Control: no-cache` porque su identificador es la RUTA y esa se REUTILIZA al borrar un producto y subir otro — cachear a lo bruto enseñaba la foto anterior 24h. La salida es versionar el id: ahora es `<ruta>#<mtime>`, así que al sustituir la foto cambia la URL, y con eso ya se pueden cachear un día como las del curso. `fetch_photo` corta por el `#`.
- [2026-08-13] [POV BOF Largo] "Textos" escribe también los guiones que falten. Al encadenarlos hay una trampa: en el `onSuccess` de la extracción, `items` sigue siendo el de ANTES (sin títulos), así que filtrar por él no da ni un pendiente. Y no vale usar la lista que devuelve la extracción, que son productos del POV BOF y no traen el estado del guion (que es de este nicho): se pasan solo los NÚMEROS de producto que acaban de recibir textos y el resto se sigue leyendo de `items`.
- [2026-08-13] [Mano] Detectar si la mano del vídeo es de hombre o de mujer para elegir la voz. Preguntar "¿de quién es esta mano?" NO funciona: con 3 fotogramas juzgados a la vez contestaba "hombre" a 11 de 12 vídeos de cosmética, y la confianza que devuelve no sirve de filtro (95% acertando y 90-95% fallando). Lo que sí funciona es darle la vuelta a la pregunta, como la plantea el operador: MUJER por defecto y hombre solo si se ve señal clara (vello, reloj de correa ancha, mano muy ancha) en 2+ fotogramas de 5, cada uno juzgado por separado. Medido así: 8 mujer / 3 hombre / 1 sin mano en cosmética, y 10 de 10 hombre en los vídeos donde el operador había elegido voz de hombre, con la señal citada ("reloj de hombre, correa ancha"). Una señal en un solo fotograma no basta: una sombra o el puño de una camisa la fabrican.
- [2026-08-13] [Mano] Cuidado al validar con datos del propio sistema: usé `voz_sexo` como verdad y salieron dos "fallos" en mujer que no lo eran — al mirar yo los fotogramas, la mano era de hombre y el operador había elegido voz de mujer por el PRODUCTO (discos faciales, sillón), no por la mano. La etiqueta mezclaba dos cosas distintas. Por eso el runner guarda ahora `mano_detectada` junto a `voz_sexo`: para tener por fin datos limpios de las dos.
- [2026-08-13] [Mano] La regla final NO es simétrica y es a propósito: colar voz de mujer en un vídeo con mano de hombre es el fallo que se nota, así que una señal FUERTE (vello o reloj de correa ancha) en UN solo fotograma ya manda a hombre, mientras que una opinable ("mano ancha") tiene que repetirse en dos. Medido sobre los 17 vídeos con mano de hombre: 16 detectados hombre, 0 detectados mujer, 1 sin mano visible ni con nueve fotogramas.
- [2026-08-13] [POV BOF Largo] "Textos + guiones" no reescribía el guion de los productos que ESTRENABAN precio. Al encadenar, la lista que se filtra es la de antes de extraer: ahí el producto todavía figura como normal (sin precio), su guion "cuadra" y se salta — justo el caso que más importa, porque un producto que estrena precio de plazos es el que necesita el guion nuevo. Del resultado de la extracción hay que coger título Y PRECIO; el estado del guion sí se sigue leyendo de la lista vieja, que es dato de este nicho.
- [2026-08-13] [Escaparate] La misma carpeta salía llena en POV BOF y a 0/7 en el Largo porque cada nicho usaba un criterio distinto: el POV BOF miraba "índice compartido O la marca vieja del producto" y el Largo SOLO el índice. Los dos datos se separan al re-extraer textos —la clave es `tienda|titulo` y al cambiar el título la marca vieja queda huérfana—, así que en esa carpeta había 39 marcas fuera del índice. Ahora hay UNA función (`product_repo.marcado_en_escaparate`) que usan los tres nichos, y `scripts/migrar_escaparate.py` reengancha las huérfanas (es idempotente; se puede relanzar cuando se corrijan títulos). Verificado carpeta a carpeta: 41 comparadas, 0 discrepancias.
- [2026-08-13] [Cuotas] El ajuste manual admite NEGATIVOS (-3 resta tres): era la única forma de deshacer un marcado de más cuando el producto ya no está marcado. El ajuste se guarda tal cual y quien no baja de cero es el TOTAL (`resumen`), porque un contador en -2 no significa nada. Y el historial del mes no hizo falta inventarlo: cada día ya guardaba su clave `cuotas:<usuario>:<fecha>`, así que `resumen_mes` lee los 31 días de una tacada con `mget_json` (uno a uno serían 31 viajes a Redis para pintar un calendario).
