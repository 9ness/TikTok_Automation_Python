# `packaging` arriba del todo para que pip lo resuelva antes que cualquier
# wheel que lo importe al instalarse (silero-vad 5.x lo necesita en su
# __init__.py). Si va al final, ciertos pip resolvers lo dejan en una
# ubicación que el COPY del runtime stage no recoge bien.
packaging>=23.0
google-generativeai
streamlit
moviepy<2.0
python-dotenv
Pillow
numpy<2.0.0
openai
requests
# DuckDuckGo image search para auto-rellenar fotos candidatas del producto
# en TikTok Shop. Sin API key. Sucesor de duckduckgo-search.
ddgs
# BeautifulSoup para scraping de URLs TikTok Shop / TikTok / OG tags
beautifulsoup4
# OpenCV headless (sin libs GUI, ideal para Docker server). Usado por
# `viral_analyzer._sample_frames` para muestrear frames de vídeos virales
# que el user sube en "Replicar viral". Versión headless = ~30MB extra
# vs ~80MB de opencv-python normal.
opencv-python-headless>=4.9
# Quitar Copy (video_remover.py) — OCR de subtítulos originales para enmascararlos.
easyocr>=1.7
streamlit-authenticator>=0.4.0
bcrypt
faster-whisper
pydantic>=2.6
httpx>=0.27
tenacity>=8.2
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
websockets>=11.0
python-multipart>=0.0.9
# Silero VAD requiere torchaudio. La 2.5.x es la compatible con torch 2.5.1
# que ya está pinneado por el resto del stack; versiones >=2.10 dan
# `OSError WinError 127` al cargar `_torchaudio.dll`.
torchaudio==2.5.1
silero-vad>=5.1
onnxruntime>=1.20
# Google Drive API — compartir carpetas entrada/salida del usuario con
# emails concretos. Wrapper en src/editor_auto/services/drive_sharing.py.
# `google-generativeai` ya está arriba pero usa otra capa distinta de
# auth (API key); estos dos paquetes son los específicos de Drive API.
google-api-python-client>=2.150
google-auth>=2.35
