google-generativeai
streamlit
moviepy<2.0
python-dotenv
Pillow
numpy<2.0.0
openai
requests
streamlit-authenticator>=0.4.0
bcrypt
faster-whisper
pydantic>=2.6
httpx>=0.27
tenacity>=8.2
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
websockets>=11.0
python-multipart>=0.0.9
# Silero VAD requiere torchaudio. La 2.5.x es la compatible con torch 2.5.1
# que ya está pinneado por el resto del stack; versiones >=2.10 dan
# `OSError WinError 127` al cargar `_torchaudio.dll`.
torchaudio==2.5.1
silero-vad>=5.1
onnxruntime>=1.20
# `packaging` lo usa silero-vad (lo importa al cargar) y aunque suele
# venir con pip/setuptools, en algunas imágenes slim no está disponible
# para `import packaging` desde el proceso uvicorn. Lo pinneamos explícito
# para evitar ImportError en producción.
packaging>=23.0
# Google Drive API — compartir carpetas entrada/salida del usuario con
# emails concretos. Wrapper en src/editor_auto/services/drive_sharing.py.
# `google-generativeai` ya está arriba pero usa otra capa distinta de
# auth (API key); estos dos paquetes son los específicos de Drive API.
google-api-python-client>=2.150
google-auth>=2.35
